# 방탈출 테마 추천기 (RAG) 기획서 — 수정본

> **이 문서의 역할** — 예약 모니터 서버(이 저장소)와 Floduler 위에 "테마 추천 검색"을 얹을 수 있는지,
> 얹는다면 어디를 새로 만들어야 하는지.
> 원본 기획서를 **실제 저장소를 읽고 고쳤다.** 고친 자리는 ⚠️ 로 표시했다.
> **초안이다 — 착수가 확정되지 않았다.** 이 저장소에 있는 이유는 §12 에 적었다.
> **2026-09-18 갱신 — 0-A(리뷰 소스 심사)가 끝났고, 답은 부정이다.** §3·§9·§11 이 그 결과로 바뀌었다.
> 최종 수정: 2026-09-18

---

## 💸 요금 경고 — 이 프로젝트가 **오라클 바깥에 처음 돈을 내는** 건이다

```
💸 요금 경고 — LLM API (요약 · 임베딩 · 답변 생성)
   지금        $0.00 — 오라클 상시 무료 안에서만 돌고 있다 (배포.md 실측 2026-08-31)
   무료 한도    없다. LLM API 는 무료 한도가 아니라 종량제다
   작업 후      1회 전량 인덱싱   156테마 × 리뷰 20개 ≈ 60만 토큰  → 저가 모델 기준 1,000원 미만 (추정)
                임베딩 156건                                      → 수십 원 (추정)
                검색 1회당 답변 생성                                → 건당 수 원 (추정) ← 유일하게 계속 나가는 자리
   넘으면       개인 사용(하루 수십 질의)이면 월 수천 원 수준 (추정).
                3차 공개 후 rate limit 이 없으면 여기만 선형으로 폭증한다
   대안         ① 답변 생성 없이 검색 결과(프로필 카드)만 보여 준다 — 지속 비용이 0 에 수렴
                ② 요약은 1회만 하고 캐시한다 (§4 파일 중간 저장이 이미 그 구조)
                ③ 안 한다 — §11 의 판정을 먼저 본다
```

**테마 수 156 은 실측**(README 매장표 합, 2026-09-08 기준)이고, **토큰·원화는 전부 추정**이다.
⚠️ 원본의 "테마 200개" 는 156 으로 고쳤다.

**오라클 쪽 추가 과금은 없다.** A1 1 OCPU · 6GB 는 얼마나 굴려도 0원이고, 지금 CPU 부하 0.10 ·
디스크 6.9GB/45GB 다(배포.md 실측). 다만 `raw/` 와 `chroma/` 는 배포.md 가 경고한
**"자라기만 하는 것"** 에 정확히 해당한다 → §6 에 보관 규칙을 못 박았다.

---

## 1. 배경과 문제 (원본 유지)

예약 모니터는 **"언제 자리가 나는가"** 는 답하지만 **"어떤 테마를 갈 것인가"** 는 답하지 못한다.

- "무섭지 않으면서 스토리 좋은 테마"
- "둘이 가기 좋고 장치 위주인 곳"
- "난이도 높고 여운 남는 테마"

⚠️ **다만 "태그에는 공포·추리 정도만 있다" 는 전제는 절반만 맞다.**
이 서버의 `Theme` 엔티티는 이미 `genre · capacity · runningMinutes · horrorLevel · difficulty · posterUrl`
를 갖고 있고 조회 API 로 그대로 나간다. 리뷰가 필요한 건 **숫자로 안 오는 쪽**이다 —
분위기 · 여운 · 추천 대상 · 지뢰 포인트. §5 를 그 기준으로 다시 잘랐다.

---

## 2. ⚠️ 기존 자산 — 원본 표는 **대부분 이 프로젝트의 것이 아니다**

원본이 가정한 스택(파이썬 · Selenium · 드라이버 풀 · Discord)은 **이 저장소에 없다.**
이 서버는 **Kotlin 2.3 / Spring Boot 4.1 / JDK 21** 이다.

| 원본이 적은 자산 | 실제 | 재사용 가능한가 |
|---|---|---|
| Selenium 크롤링 프레임워크 | **없다.** jsoup 1.21.1 + HTTP 클라이언트 | ❌ — 그리고 **필요 없다.** 40지점이 전부 정적 HTML·JSON·조각 응답이라 브라우저를 띄울 이유가 없다 |
| 드라이버 풀 | **없다** (위와 같은 이유) | ❌ |
| 사이트별 `type → check` 매핑 | `StoreAdapter.plan(dates): List<FetchUnit>` | ⚠️ **패턴만.** 이 인터페이스는 **날짜 축**을 전제한다. 리뷰에는 날짜 축이 없어 여기 안 들어간다 |
| ThreadPoolExecutor 병렬 | **있다.** `StoreCollector` 고정 풀 (`collector.site-concurrency=16`) | ⚠️ 같은 JVM 안에서만 |
| Discord 웹훅 | **없다.** FCM 이다 (`NOTIFY_CHANNEL=log\|fcm`, 아키텍처 D16) | ❌ — 인덱싱 알림은 별도로 정한다 |
| Oracle Cloud 서버 | **있다.** A1.Flex 1 OCPU · 6GB · aarch64 · Ubuntu 24.04 | ✅ |
| 모니터링 대상 업체 목록 | **있다.** 10매장 40지점 **156테마** | ✅ — §9 참고 |

### ⚠️ 승격 — 속도 규칙이 프로세스가 둘이 되면 깨진다

`HostRateLimiter` 는 **JVM 안의 객체**다. 호스트별 락으로 "매장당 동시 1개 · 1200ms 간격" 을 지킨다.

```
별도 파이썬 프로세스가 같은 호스트를 두드리면 → 락이 안 걸린다 → 규칙이 조용히 2배가 된다
```

`HostRateLimiter` 주석이 경고하는 실패 모드 그대로다 — **"규칙이 구조가 아니라 관례가 된다."**
원본 §9 의 *"기존 모니터의 요청 간격 정책 준수"* 는 **프로세스가 둘이면 관례로는 달성 불가**다.

→ **리뷰 소스 호스트가 감시 대상 매장 호스트와 겹치는지를 먼저 가른다.**
겹치지 않으면(커뮤니티·블로그) 문제가 없다. 겹치면 둘 중 하나다:
① 리뷰 수집을 서버 JVM 안에 넣어 같은 `HostRateLimiter` 를 태운다
② 리뷰 수집 시간대를 수집 주기(300초)와 완전히 분리한다 — **권장하지 않는다. 관례다**

### ⚠️ 핵심 판단 (원본 뒤집음)

> 원본: "크롤링 인프라가 이미 있으므로 신규 개발 범위는 크지 않다. 새로 만드는 건 수집 이후 단계다."

**틀렸다.** 재사용되는 건 **서버(오라클)와 매장 목록**뿐이고, 크롤링 코드는 한 줄도 재사용되지 않는다.
그리고 **"수집 이후" 가 아니라 서버 쪽에도 새 작업이 있다** — §3-0 을 본다.

---

## 3. ⚠️ 범위 — 0차를 새로 만들었다

### 0차 (새로 추가) — 착수 조건 두 개

이게 없으면 MVP 가 성립하지 않는다.

| | 무엇 | 상태 |
|---|---|---|
| **A** | **리뷰 소스 심사** | ❌ **끝났다. 다섯 곳 전부 탈락** (2026-09-18 · [정리 문서](방탈출%20지점%20요청_응답%20정리.md) "리뷰 수집처 심사") |
| **B** | **`GET /api/themes` 신설** (서버 작업) | 지금 API 로는 테마 전량 목록을 못 받는다 — 아래. **A 가 막혀 당장은 쓸 데가 없다** |

> **0-A 가 MVP 를 막았다.** 후기 플랫폼 다섯 곳을 매장과 같은 기준으로 심사했고
> (`robots.txt` · 약관 · 보호장치), **크롤링으로 리뷰 본문을 얻을 수 있는 곳이 없었다** —
> 둘은 명시적 차단(잼핏 `Disallow: /` · 디시 `ClaudeBot` 지목), 둘은 Flutter Web 이라
> **읽을 HTML 자체가 없고**, 남은 하나는 리뷰 본문 보유가 확인되지 않은 데다 경쟁 서비스다.
> **§3 의 1차 MVP 는 이 상태로 성립하지 않는다.** 남는 것은 §11 에 적었다.

**지금 API 로 테마 목록을 뽑을 수 없는 이유:**

```
GET /api/branches   → 지점과 "고를 수 있는 날짜" 만 준다. 테마가 없다
GET /api/schedule   → (지점, 날짜) 한 쌍에 그날 나온 테마만 준다
```

`ScheduleDto` 는 **그 날짜에 실제로 뜬 테마만** 담는다. 그래서 지점 × 수집된 날짜를 전부 훑어도
**"수집 창 안의 어느 날에도 안 뜬 테마" 와 "그런 테마가 없다" 가 구분되지 않는다.**
이건 실제로 일어나는 일이다 — 정기 휴무가 그렇다
(제로월드 부산점 5테마 · 월요일 휴무, [정리 문서](방탈출%20지점%20요청_응답%20정리.md) 제로월드 항목).

그리고 훑어서 모으면 **키가 이름으로 되돌아간다.** 필요한 건 D8 이 정한
`(store, external_id)` 로 고정된 목록이다 (§5-0). 그래서 **목록은 서버가 낸다.**

### 1차 (MVP)

- 156테마에 대해 리뷰 수집 (소스는 0-A 가 정한 곳)
- LLM 으로 테마별 속성 프로필 생성 — ⚠️ **이미 있는 값은 안 만든다** (§5)
- 벡터 DB 인덱싱
- CLI 검색

### 2차

- 3일 주기 자동 인덱싱 (cron)
- 검색 API 노출 — ⚠️ **새 포트를 열 필요 없다** (§10 인프라)
- 웹 프론트 (본인용)

### 3차 (조건부)

- 지인 공개 — ⚠️ **rate limit 이 선행 조건이다.** 지속 비용이 여기서만 선형으로 는다
- 예약 모니터와 통합 — **"무섭지 않은 감성 테마 중에 이번 주말 자리 나는 곳"**

⚠️ **3차 통합은 붙일 자리가 이미 있다.** Floduler 가 이 서버의 회차·매진을 이미 먹고 있고
(Floduler 기획 §4.30), 테마 선택 화면이 이미 `horrorLevel · difficulty · genre` 를 받아 쓴다.
추천 결과에 `theme_id` 만 실려 있으면 **화면 쪽은 필터 하나 추가하는 일**이 된다.
→ 그래서 §5 의 프로필은 처음부터 `theme_id` 를 키로 잡는다.

---

## 4. 아키텍처

원본의 **두 파이프라인 분리 · 단계별 파일 중간 저장 · 임베딩 모델 동일 유지**는 그대로 맞다.
(임베딩 모델이 다르면 에러 없이 검색 품질만 조용히 망가진다 — 이 프로젝트가 계속 경계하는
"조용한 오작동" 과 같은 종류다.)

### ⚠️ 갈림길 — 어디에 붙이나 (하나 고르면 나머지는 미뤄도 된다)

| | 방식 | 좋은 점 | 나쁜 점 |
|---|---|---|---|
| **㉠ 추천** | **별도 레포 · 파이썬 + Chroma.** 서버와는 `GET /api/themes` 계약으로만 잇는다 | **D7 선례가 이미 답이다** — Floduler 와 저장소를 안 합치고 API 로만 이은 그 판단. 실험 사이클이 빠르고, 망해도 서버가 안 다친다 | 테마 이름↔ID 매칭을 **인덱싱 시점에 한 번** 해야 한다 (§5-0) |
| ㉡ | **기존 Postgres + pgvector · 인덱싱을 Spring 잡으로** | 매칭이 **SQL 조인**이 되어 §5-0 이 통째로 사라진다. 백업·운영이 한 곳 | D7 을 거스른다. 벡터 검색 실험이 느려지고, 서버 한 바퀴(300초)와 배치가 같은 JVM 을 나눠 쓴다 |

**㉠ 을 고른다.** 이유는 속도가 아니라 **실패 격리**다 — 이 서버는 이미 Floduler 를 먹여 살리고 있어서,
검증 안 된 LLM 배치가 같은 프로세스에 들어오면 취소표 감지가 같이 멈출 수 있다.
㉡ 은 **추천이 실제로 쓸 만하다고 판정된 뒤에** 다시 본다 (그때는 데이터가 이미 있으니 이관이 싸다).

### 파일 구조

```
escape-rag/                (별도 레포)
├── fetch_themes.py   # GET /api/themes → themes.json   ← 서버에서 받아오는 유일한 지점
├── crawl.py          # 리뷰 수집 → raw/
├── summarize.py      # LLM 요약 → profiles.json
├── index.py          # 임베딩 + 저장 → chroma/
├── search.py         # 검색 함수 (CLI 는 얇은 껍데기)
├── eval.py           # 평가셋 자동 검증
├── api.py            # (2차)
├── raw/              # 크롤링 원본
├── profiles.json     # LLM 요약 결과
└── chroma/           # 벡터 DB (파일 기반)
```

⚠️ **`raw/` 와 `chroma/` 에 보관 규칙을 둔다** (배포.md "자라기만 하는 것"):

```
raw/        테마당 최신 1벌만 남긴다. 재크롤링 방지가 목적이지 이력 보관이 아니다
chroma/     재생성 가능하다. 백업 대상이 아니다
profiles.json  이것만 유일하게 아깝다 (LLM 비용이 들어 있다) → git 에 넣는다
```

---

## 5. ⚠️ 데이터 설계 — 절반은 서버가 이미 준다

### 5-0. 키 (새로 추가 · 여기가 제일 위험하다)

원본 스키마의 `"theme": "테마명"` 이 **최대 공사를 숨기고 있었다.**

```
서버         자연키 (store, external_id) · 외부 노출은  play33-konkuk:18   ← 이름이 바뀌어도 안 갈라진다 (D8)
리뷰 소스     "테마명" 문자열뿐
```

156테마 · 10브랜드에는 **동명 테마가 섞여 있다.** 이름 매칭은 다대다이고 애매하다.

> **그래서 매칭은 인덱싱 시점에 딱 한 번 한다.** `fetch_themes.py` 로 받은 목록과 맞춰
> **프로필에 `theme_id` 를 박아 둔다.** 검색 때는 이름을 아예 안 본다.
> 매칭 못 한 리뷰는 **버리지 말고 `unmatched.json` 에 남긴다** — 조회 실패와 "결과 없음" 을
> 같은 값으로 표현하지 않는다는 이 프로젝트의 규칙과 같은 이유다.

### 5-1. 크롤링 결과 (파싱, LLM 없음)

```json
{
  "theme_id": "play33-konkuk:18",
  "theme": "테마명",
  "venue": "플레이33 건대점",
  "reviews": ["리뷰 원문 1", "리뷰 원문 2", "..."]
}
```

⚠️ `location` 은 **넣지 않는다.** 서버가 `place` 를 이미 주고, 두 벌 적으면 어긋난다.

### 5-2. LLM 요약 결과

⚠️ **이미 있는 값은 LLM 이 만들지 않는다.**

| 속성 | 어디서 오나 |
|---|---|
| `genre · capacity · runningMinutes · posterUrl` | **서버 (매장 표기 그대로)** |
| `horrorLevel · difficulty` | **서버 — 단, 채워진 곳만** (플레이33 · 지구별 2곳. 나머지 8매장은 `null`) |
| `분위기 · 추천대상 · 주의사항` | **LLM** — 리뷰에만 있는 정보 |
| `공포도 · 장치난이도` (서버가 `null` 인 테마) | **LLM 이 빈 곳만 채운다** |

```json
{
  "theme_id": "play33-konkuk:18",
  "horrorLevel": 2.0,        // 출처 표기 필수: site | llm
  "horrorLevelSource": "site",
  "deviceLevel": 3,          // 정수 1~5 — 아래 이유
  "storyLevel": 5,
  "분위기": "잔잔하고 감성적, 엔딩에서 여운이 남음",
  "추천대상": "커플, 감성 테마 선호",
  "주의사항": "장치 위주를 원하면 실망할 수 있음",
  "리뷰수": 20
}
```

⚠️ **척도는 두 갈래다. 섞지 않는다.**

| 축 | 척도 | 왜 |
|---|---|---|
| `horrorLevel` · `difficulty` | **`Double` · 0.5 단위** | 서버와 같은 값이어야 한다. **사이트가 `2.5` 를 실제로 준다** — 아키텍처 D10. 원본의 정수 1~5 를 그대로 두면 3차 통합에서 어긋난다 |
| `deviceLevel` · `storyLevel` | **정수 1~5** | **사이트에 원본이 아예 없다. 순수 LLM 판정이다.** 여기에 0.5 칸을 주면 없는 정밀도를 지어낸다 — D10 의 0.5 는 "사이트가 그렇게 쓰니까" 지 정밀해서가 아니다 |

> **공짜 캘리브레이션 세트가 생긴다.** 플레이33·지구별 30테마는 **사이트 실측 공포도**가 있다.
> LLM 판정을 그 값과 대조하면 §7 단계 0 의 "요약 품질 검증" 이 **감이 아니라 숫자**가 된다.
> 이건 원본에 없던 이득이고, 단계 0 을 훨씬 싸게 만든다.

### 5-3. 인덱싱 시 분리 (원본 유지)

| 필드 | 처리 |
|---|---|
| 공포도 · 장치난이도 · 스토리강도 · `theme_id` · `place` · `brand` | 메타데이터 — 필터/정렬 |
| 분위기 · 추천대상 · 주의사항 | 임베딩 대상 |
| 테마명 · 매장명 | 임베딩 제외 (노이즈) |

지역은 **반드시 필터**다. 벡터로 하면 "홍대" 질문에 강남 테마가 섞여 들어오고, 조용히 틀린다.

---

## 6. ⚠️ 기술 스택

| 구성요소 | 선택 | 비고 |
|---|---|---|
| 리뷰 수집 | **requests + BS4** | ⚠️ Selenium 아니다. 있지도 않고, 필요하지도 않다 |
| LLM 요약 | 상용 API 저가 모델 | 판정 난도가 낮다 |
| 임베딩 | API 임베딩 모델 | 폐쇄망이 아니다 |
| 벡터 DB | ChromaDB | 파일 기반 · 서버 프로세스 불필요 = 메모리 6GB 를 안 문다 |
| 답변 생성 | 상용 API | **지속 비용이 여기서만 난다** |
| 스케줄링 | 서버 cron | ⚠️ 서버 수집 주기(300초)와 겹치지 않는 시간대로 |
| API (2차) | FastAPI · **루프백 바인드** | ⚠️ Caddy 경로 분기로 낸다 (§10) |

---

## 7. ⚠️ 단계별 계획 — 소스 심사를 맨 앞에 끼웠다

| 단계 | 내용 | 산출물 |
|---|---|---|
| ~~0-A~~ | ~~리뷰 소스 심사~~ | ✅ **완료 · 전부 탈락** (2026-09-18 · [정리 문서](방탈출%20지점%20요청_응답%20정리.md)) |
| 0-B | 테마 5개 리뷰를 **수동으로 붙여넣어** 요약 품질 검증 | LLM 프롬프트 확정 |
| 0-C | ⚠️ 캘리브레이션 — 플레이33·지구별 공포도와 LLM 판정 대조 | 오차 숫자 |
| 1 | 평가셋 작성 (질문 10개 + 기대 결과) | `eval.json` |
| **1.5** | ⚠️ **서버에 `GET /api/themes` 추가** | 서버 커밋 (아키텍처·작업명세서 같이) |
| 2 | 리뷰 크롤러 | `crawl.py` |
| 3 | 요약 + 인덱싱 | `profiles.json` · `chroma/` |
| 4 | 검색 + 평가 자동화 | `search.py` · `eval.py` |
| 5 | 검색 품질 튜닝 | threshold · top_k 확정 |
| 6 | cron 등록 | |
| 7 | 검색 API + 프론트 | |

**0-B 를 먼저 하는 이유** (원본 유지): 크롤링이 제일 오래 걸리는데 성패는 요약 품질에 달려 있다.
크롤러를 다 만든 뒤 요약이 별로면 시간이 통째로 낭비된다.

**1 을 2 보다 먼저 하는 이유** (원본 유지): 측정 도구 없이 튜닝하면 감으로 판단하게 된다.

⚠️ **0-A 를 맨 앞에 두는 이유**: 이 프로젝트의 규칙이다 — **"매장을 추가하기 전에 정리 문서에 먼저 적는다.
코드보다 이게 앞선다."** 리뷰 소스도 남의 서버라 똑같이 적용된다.
그리고 소스가 안 정해지면 §9 의 "리뷰 수집량 부족" 리스크는 **평가 자체가 불가능**하다.

---

## 8. 검증 기준 (원본 유지 · 하나 추가)

- 평가셋 = 질문 10개 + "이 테마가 상위에 떠야 함" 매핑
- 성공 기준 = **10개 중 7개 이상에서 기대 테마가 top 3 에 포함**
- 평가셋은 실제로 다녀온 테마로 (좋았던 것 5 · 별로였던 것 3)
- ⚠️ **추가: 공포도 캘리브레이션 오차** — 사이트 값이 있는 30테마에서 평균 오차 **±1.0 이내**
  (0.5 단위 척도이므로 두 칸). 이걸 못 맞추면 리뷰가 부족한 것이지 프롬프트 문제가 아니다

---

## 9. ⚠️ 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| ~~리뷰 소스 심사 탈락~~ | — | ❌ **터졌다. 가정이 아니라 사실이다** (2026-09-18). 아래 줄들은 그 위에서만 의미가 있다 |
| 리뷰 수집량 부족 (테마당 5개 미만) | 요약 품질 저하 | 테마 수를 줄이고 리뷰 많은 곳 우선 |
| **이름 매칭 실패** | 프로필이 예약 정보와 못 붙는다 = 3차 가치 상실 | `theme_id` 를 인덱싱 시점에 박는다. 못 맞춘 건 `unmatched.json` 에 남긴다 (§5-0) |
| **속도 규칙이 프로세스 둘로 쪼개져 깨짐** | **남의 서버에 부담** | 리뷰 소스 호스트가 매장 호스트와 겹치는지 먼저 가른다 (§2) |
| LLM 이 리뷰를 그대로 반복 | 벡터 품질 저하 | 0-B 에서 검증 · 0-C 로 숫자 확인 |
| 벡터 검색이 키워드 검색보다 못함 | 프로젝트 근거 상실 | 1단계 평가셋으로 조기 발견 |
| 리뷰 데이터 외부 공개 | 법적 회색지대 | 3차 공개 시 **요약만 노출 · 원문 비노출 · 출처 표기** |
| 공개 후 API 비용 폭증 | 과금 | **rate limit 이 3차 선행 조건** (💸 블록) |
| `raw/` · `chroma/` 가 자란다 | 디스크 | §4 보관 규칙 |

---

## 10. ⚠️ 원본 §10 "확인이 필요한 사항" — **전부 확인했다**

### 인프라

| 질문 | 답 |
|---|---|
| 인스턴스 사양은? | **오라클 도쿄 · A1.Flex · 1 OCPU · 6GB · aarch64 · Ubuntu 24.04 · 디스크 45GB** (배포.md). ARM 이다 — 그런데 **ChromeDriver 질문 자체가 소멸한다.** Selenium 을 안 쓴다 |
| 상시 실행 중인가 · 여유가 있나 | **돌고 있다** (300초 주기). **여유는 충분하다** — CPU 부하 0.10 · 앱 363MB / 5.9GB (실측 2026-08-31) |
| 디스크 여유는? | **6.9GB / 45GB (16%)**. 수백 MB 는 문제없다. 다만 §4 보관 규칙 |
| 인바운드 포트 개방 가능한가? | ⚠️ **열 필요가 없다.** 80·443 만 열려 있고 앞에 **Caddy** 가 있다. 새 서비스는 **루프백 바인드 + Caddy 경로 분기**로 낸다 — `/api/*` 만 통과시키고 나머지는 404 로 막는 지금 구조 그대로 |

### 코드

| 질문 | 답 |
|---|---|
| 기존 크롤러 구조에 "리뷰 수집" 타입 추가 가능한가? | ⚠️ **안 맞는다.** `StoreAdapter.plan(dates)` 는 **날짜별 요청 계획**을 내놓는 인터페이스다. 리뷰에는 날짜 축이 없다. 억지로 넣으면 "요청 하나 = 작업 하나"(D15)가 깨진다 |
| 드라이버 풀 · ThreadPoolExecutor 공유? | ⚠️ 드라이버 풀은 **없다.** 스레드 풀은 있지만 **JVM 안에 있어 별 프로세스가 공유할 수 없다.** 진짜 문제는 풀이 아니라 **`HostRateLimiter` 다** (§2) |
| 같은 레포 vs 별도 레포 | ⚠️ **별도 레포.** 아키텍처 **D7** 이 Floduler 로 같은 판단을 이미 내렸다 (§4 갈림길) |
| Discord 웹훅 재사용? | ⚠️ **Discord 가 없다.** FCM 이다. 인덱싱 성공/실패 알림에 FCM 을 쓰는 건 과하다 — **cron 로그 + 실패 시에만 알림**으로 충분하다 |

### 데이터

| 질문 | 답 |
|---|---|
| 테마 수는? | ⚠️ **6개 업체가 아니라 10매장 40지점.** **156테마** (실측 — README 매장표 합 `9+32+2+21+9+32+25+2+13+11`, 2026-09-08 기준) |
| 리뷰를 어디서? | ⚠️ **네이버예약은 제외다.** `m.booking.naver.com/robots.txt` 가 `Disallow : /` 이고, 이 규칙 때문에 **육향찻집을 이미 대상에서 뺐다**(2026-09-04). 같은 기준을 리뷰에도 적용한다 → **남은 후보는 커뮤니티·블로그이고, 0-A 에서 심사한다** |
| 테마당 확보 가능한 리뷰 수? | **모른다.** 0-A 가 소스를 정해야 셀 수 있다 |

### 기획

| 질문 | 답 |
|---|---|
| 속성 목록이 실제 선택 기준과 맞나? | ⚠️ **절반은 서버가 이미 준다.** 리뷰가 필요한 건 분위기·추천대상·주의사항이다. 탈출률·체감시간은 어느 사이트도 안 주므로 **전적으로 리뷰 의존** — 0-B 에서 뽑히는지 먼저 본다 |
| 3차 통합을 처음부터 염두? | ⚠️ **염두에 둔다. 비용이 거의 0 이다** — `theme_id` 를 키로 잡는 것 하나뿐이고, 그건 §5-0 때문에 어차피 해야 한다 |

---

## 11. ⚠️ 결론 — **2026-09-18 판정이 바뀌었다**

> **전 판정(2026-09-17): 조건부 GO.** 조건은 "리뷰 소스가 심사를 통과할 것" 하나였다.
> **그 조건이 깨졌다.** 아래는 그 뒤의 결론이다. 전 판정은 지우지 않고 남긴다 —
> 무엇이 GO 를 NO 로 바꿨는지가 다음 사람을 구한다.

**착수 판정: 원안(리뷰 RAG)은 NO.**

리뷰 본문을 **합법적으로 자동 수집할 수 있는 곳이 없다.** 후기 플랫폼 다섯 곳을 매장과 같은
기준으로 심사한 결과이고, 근거는 [정리 문서](방탈출%20지점%20요청_응답%20정리.md) "리뷰 수집처 심사" 에 있다.

```
잼핏            Disallow: /                  ← 네이버예약과 같은 판정
디시 방탈출 갤   ClaudeBot · GPTBot 지목 차단   ← 우리 용도가 정확히 그것이다
빠방 · 전국방탈출  Flutter Web + CanvasKit       ← 글자가 DOM 에 없다. 사설 API 역공학 외에 길이 없다
자리요           Allow: / 인데 Disallow: /api/  ← 리뷰 본문 보유 미확인 · 약관 9조 4항 · 경쟁 서비스
```

**§3 의 1차 MVP 는 리뷰 본문 위에 통째로 서 있다.** 그 바닥이 없다.
**우회해서 세우지 않는다** — 이 프로젝트가 육향찻집을 뺄 때 이미 같은 값을 치렀다.

### 그래도 살아남은 것 둘

**이 심사가 프로젝트 전체를 죽이지는 않는다.** 두 가지가 남고, 둘 다 남의 데이터가 필요 없다.

| | 무엇 | 지금 되나 |
|---|---|---|
| **①** | **수동 검증(0-B)** — 브라우저로 읽은 리뷰를 **사람이 붙여넣는 것은 크롤링이 아니다.** 5테마 × 10개면 *"LLM 이 쓸 만한 프로필을 뽑는가"* 는 답이 나온다 | ✅ **오늘. 법적 노출 0** |
| **②** | **구조화 필터** — 156테마의 `genre · capacity · runningMinutes · horrorLevel · difficulty` 를 **API 가 이미 준다** | ✅ 서버 작업 없이 |

②가 뼈아픈 자리다. 원본 §11 이 *"30개 미만이면 단순 테이블로 충분하니 재검토"* 라고 적었는데,
**리뷰를 못 얻는 상태에서는 테마가 156개여도 만들 수 있는 것이 그 단순 테이블이다.**
의미 검색이 아니라 필터다. 그게 값어치가 있는지는 **써 보면 안다** — `horrorLevel` 이
2매장 30테마에만 채워져 있어(§5-2) 필터로서도 아직 성기다.

### 다시 GO 가 되는 조건

**하나뿐이다 — 리뷰 본문을 주는 쪽이 생기는 것.** 크롤링 말고:

- **①의 결과가 좋으면** 수동 수집으로 **테마 20~30개짜리 작은 추천기**는 성립한다.
  전량(156)은 아니지만, 다녀온 테마 위주라면 그게 오히려 쓸 만할 수 있다
- **제휴** — 매장이나 플랫폼이 데이터를 주는 길. [기획.md](기획.md) "제휴라는 대안" 과 같은 논리다
- **사용자가 직접 쓴 후기** — 내 기록을 내가 넣는 것. 남의 데이터가 아니다

**크롤링으로 푸는 길은 닫혔다. 다시 열릴 때까지 ②와 ①만 한다.**

---

## 12. 이 문서가 왜 여기 있나 — **임시 거처다**

**여기가 제자리가 아니다.** 착수가 확정되면 새 저장소 `escape-rag` 의 `기획.md` 로 옮기고
**이 파일은 지운다.** 지금 이 저장소에 둔 이유는 하나뿐이다 — **기기를 옮겨 가며 이어서 보려고.**

그래서 CLAUDE.md 문서표에 **`초안 · 검토 중`** 으로 한 줄 달아 뒀다.
표에 없는 루트 `.md` 는 **아무도 갱신 시점을 안 봐서 제일 먼저 썩는다** — CLAUDE.md 가
직접 겪고 적어 둔 실패 모드다. 임시로 두더라도 표에는 올린다.

### 착수가 확정되면 (순서대로)

1. **새 저장소** `escape-rag` — 이 파일이 그쪽 `기획.md` 가 된다
2. **이 파일과 CLAUDE.md 문서표의 그 줄을 지운다** ← 빼먹으면 죽은 문서가 남는다
3. 이 저장소 **CLAUDE.md "자매 프로젝트"** 에 한 줄 (지금 Floduler 가 있는 자리)
4. **아키텍처.md D27** — "추천기와는 저장소가 아니라 API 계약으로 잇는다"
   (D7 과 같은 논리 · §4 갈림길에서 ㉡(pgvector)을 왜 버렸는지까지)
5. **작업명세서.md** 에 `GET /api/themes` (단계 1.5) 를 항목으로

### 착수가 엎어지면

**이 파일을 지운다.** 남겨 두면 "하다 만 것" 으로 보이고, 그게 §9 가 말한
*"낡은 데이터는 없는 것보다 나쁘다"* 의 문서판이다.
