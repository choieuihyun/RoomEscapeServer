# 작업 명세서

> **이 문서의 역할** — 무엇을 어디까지 했고, 다음에 뭘 하는지.
> 작업을 끝낼 때마다 체크하고, 새 작업이 생기면 여기에 먼저 적는다.
> 관련 문서: [기획.md](기획.md) (무엇을 왜) · [아키텍처.md](아키텍처.md) (어떻게 생겼나)
> 최종 수정: 2026-08-26

---

## 진행 현황

```
M1   수집          ██████████ 완료
M2   저장·감지     ██████████ 완료 (Postgres 실기동 확인)
M2.5 조회 API      ██████████ 완료 (로컬 연동 확인 · 공개는 M4 이후)
M3   알림          ████████░░ 판정·등록 API 완료 · FCM 발송만 남음
M4   배포          ███████░░░ 준비물 완료 · 서버 확보만 남음
M5   매장 확장     ██░░░░░░░░ 키이스케이프 조사 완료 · 구현 전
```

> **M2.5가 이 서버를 처음 쓰게 만든다.** 자매 프로젝트 [Floduler](기획.md)(`~/RoomEscapeScheduler`)에
> 회차와 매진 여부를 먹인다. 알림(M3)보다 먼저 쓸 사람이 생긴다.

---

## ✅ M1 — 플레이33 슬롯 수집 (완료 · 2026-08-26)

**목표:** 매장 사이트에서 예약 가능 여부를 코드로 읽어낸다.

| | 작업 | 산출물 |
|---|---|---|
| ✅ | 예약 페이지 요청 구조 조사 | `GET /reservation?branch=&date=` · 인증 불필요 확인 |
| ✅ | 지점·테마 목록 확보 | `Play33Branch.kt` — 4지점 / 총 9테마 |
| ✅ | HTTP 조회 | `Play33Client.kt` |
| ✅ | HTML 파싱 | `Play33Parser.kt` |
| ✅ | 결과 검증 | `Play33Crawler.kt` — 날짜 대조 후 불일치 시 예외 |
| ✅ | 회귀 테스트 | `Play33ParserTest.kt` + 실제 HTML 픽스처 (8개 통과) |
| ✅ | 동작 확인 | `GET /debug/play33?branch=KONKUK&date=2026-09-01` |
| ✅ | 프로젝트 정리 | `.gitignore` 정비, 임시 SSRF 엔드포인트 삭제 |

**확인된 사실** (상세는 [기획.md](기획.md) 도메인 지식 절)
- 예약 오픈 범위 7일 = `[오늘, 오늘+6]` (9/1은 200, 9/2는 302로 확인)
- ⚠️ "매일 20:00 신규 오픈"은 숨은 필드에서 읽은 **추정**이며 아직 미검증 — M2-5에서 확인
- 매진 슬롯도 목록에 그대로 표시 → 전이 감지가 단순해짐
- 범위 밖 날짜는 302 → 검증 필요 ([아키텍처.md](아키텍처.md) D2)
- 전체 커버에 28요청이면 충분 → 전량 수집 방침 ([아키텍처.md](아키텍처.md) D3)

---

## ✅ M2 — 저장 + 전이 감지 (완료 · 2026-08-26)

**목표:** `예약 불가 → 예약 가능` 으로 바뀐 순간을 로그로 찍는다.
**이게 이 프로젝트의 심장이다.** 여기까지 되면 나머지는 껍데기다.

### 2-1. DB 띄우기
- [x] `docker-compose.yml` 작성 (PostgreSQL 17, 루프백 바인딩, 볼륨)
- [x] `spring-boot-starter-data-jpa` + `postgresql` 드라이버 + `kotlin("plugin.jpa")`
- [x] DB 접속 정보를 **환경변수로** 주입 (`${DB_URL}` / `${DB_USER}` / `${DB_PASSWORD}`)
- [x] `docker compose up -d` 후 실제 기동 확인 — 런타임은 **Colima** (Docker Desktop 라이선스 회피, 더 가벼움)

> **완료 기준:** `docker compose up -d` 후 앱이 정상 기동한다.

### 2-2. 테이블 설계
- [x] `TimeSlot` — 자연키 `(theme, slot_date, slot_time)` 유니크 제약
- [x] `Theme` — 자연키 `(store, external_id)`. **이름이 아니라 사이트 테마 ID**를 키로 (이름이 바뀌어도 안 갈라짐)
- [x] `Store` — 자연키 `store_key` (`play33-konkuk`). 조회 API 의 `branch` 값과 같다

> **완료 기준:** 같은 슬롯을 두 번 저장해도 행이 하나다. ✅ 테스트 + 실사이트 확인

### 2-3. 저장
- [x] 자연키 기준 **upsert** (변경 감지로 UPDATE)
- [x] 전체 삭제 후 삽입 안 함
- [x] 크롤 실패(예외) 시 아무것도 쓰지 않음 — `Play33Crawler`가 먼저 끊는다

> **완료 기준:** 두 번 수집해도 행 수가 늘지 않고 `available`만 갱신된다. ✅

### 2-4. 전이 감지
- [x] 저장 **전에** 이전 `available`을 읽어 `false → true`만 추출
- [x] 전이를 로그로 출력 (`🎟️ 자리 남 — …`)
- [x] **위생 검사** — 이전 매진 4개 이상 중 절반 넘게 한 번에 풀리면 **격리 + 저장 안 함** ([아키텍처.md](아키텍처.md) D6)

> **완료 기준:** 실제 취소가 발생했을 때 로그에 한 줄 찍힌다.
> 로직은 테스트 7건으로 검증. **실제 취소 목격은 아직 없다** (기다려야 한다)

### 2-5. 스케줄러
- [x] `@EnableScheduling` + `fixedDelay` 5분으로 `4지점 × 7일` 순회
- [x] 요청 사이 1.2초 간격 → 초당 1회 미만
- [x] 한 지점·하루가 실패해도 나머지는 계속 진행
- [x] 사이트가 예약 범위를 7일에서 바꾸면 경고 로그
- [x] `collector.play33.enabled=false` 로 끌 수 있고, **꺼도 수동 수집은 가능** (`Play33Collector` 분리)
- [ ] **먼저 확인:** 20:00 이전/이후에 `date=오늘+7`을 각각 조회해 `302 → 200`으로 바뀌는지 본다
      - 바뀌면 → 그 시각 전후 집중 폴링을 넣는다
      - 안 바뀌면 → `reservation_range_open_time` 추정을 폐기하고 [기획.md](기획.md)를 고친다

> **완료 기준:** 앱을 켜두면 사람이 아무것도 안 해도 로그가 쌓인다.

### 2-6. 검증 현황

| 항목 | 상태 |
|---|---|
| 저장·전이·위생검사 로직 | ✅ 테스트 7건 (`ScheduleSyncServiceTest`, H2) |
| 실사이트 → 파싱 → 저장 → 조회 | ✅ 건대점·대전점 실제 수집 확인 |
| 두 번 수집해도 행 안 늘어남 | ✅ 테마당 10행 유지 확인 |
| 범위 밖 날짜는 저장 안 됨 | ✅ 예외로 끊김 확인 |
| **Postgres 에서의 동작** | ✅ 스키마 생성·저장·조회 확인 |
| **전이 감지 (실 DB)** | ✅ 슬롯을 매진으로 되돌린 뒤 재수집 → `🎟️` 로그 확인 |
| **격리 (실 DB)** | ✅ 전 회차를 매진으로 만든 뒤 재수집 → 격리되고 저장 안 됨 확인 |
| **격리 복구 (실 DB)** | ✅ 3회 연속 → 저장하되 알림 없음 → 4회차 정상 복귀 확인 |
| 실제 취소표 전이 목격 | ⏳ 미확인 (스케줄러를 며칠 돌려야 나온다 — 집에서) |

### 실행 중 발견해서 고친 것

**격리가 스스로 못 빠져나왔다.** 저장을 건너뛰니 기준선이 낡은 채로 남아 다음 수집도 똑같이 걸리고,
그게 무한 반복됐다. 매장이 하루를 닫았다 다시 여는 정상 상황에서도 이 상태에 빠질 수 있다.
→ 3회 연속이면 **저장은 하되 알림은 안 내보내는** 복구 경로를 넣었다 ([아키텍처.md](아키텍처.md) D6).
테스트만으로는 안 나왔고 실제로 돌려 봐서 나온 문제다.

---

## M2.5 — 조회 API + Floduler 연동

**목표:** Floduler 카드가 서버에서 받아온 회차로 채워지고, **매진 회차에 매진 표시가 뜬다.**

Floduler는 `session.soldout` 필드를 이미 갖고 있으면서 채울 방법이 없어 비워 두고 있었다
(그쪽 기획 §4.13). 이 API가 그 빈칸을 채운다 — 그쪽 기획 §4.30 참고.

### 서버 쪽 ✅ 완료
- [x] `GET /api/branches` — 지점 목록 + **고를 수 있는 날짜** (날짜 선택에 추가 호출이 없게)
- [x] `GET /api/schedule?branch=&date=` — 그날 전 테마 회차 + 매진 여부
- [x] 응답 시각을 **자정부터의 분(정수)** 으로 — Floduler `session.t` 형식
- [x] `dur`은 홈페이지 표기값 그대로. 회차 간격으로 보정하지 않음
- [x] **매진 회차를 빼지 않고 전부** 내려보냄 (감시 버튼을 붙일 자리)
- [x] 테마 선택용 메타데이터 — `minPeople`/`maxPeople`, 난이도, 공포도, 장르, 포스터, 안정적인 `id`
- [x] `checkedAt` — "N분 전 기준" 배지용
- [x] 수집 전인 지점·날짜는 **404** (빈 결과와 구분)
- [x] CORS — `/api` 경로에만, `https://choieuihyun.github.io` 허용. 디버그 엔드포인트는 안 열림
- [x] 계약 원본은 Floduler `작업명세서.md` §4.4

> **완료 기준:** `curl`로 받은 JSON을 Floduler가 그대로 먹을 수 있다. ✅ 실기동 확인

### Floduler 쪽 (F-15) ✅ 완료
- [x] `불러오기 (플레이33)` 버튼 — **지원 매장을 버튼에 적었다** (범용 `가져오기` 아님)
- [x] 지점 → 날짜 → 테마 선택 모달. 포스터·장르·인원·난이도·남은 회차 표시
- [x] 인원을 넣으면 안 맞는 테마가 흐려진다 (4명인데 2~3인 테마는 못 간다)
- [x] `name` / `place` / `dur` / `sessions[{t, soldout}]` + `raw` 채우기
- [x] 매진 회차가 카드에 남고, 계산에서는 `excludeSoldout` 이 뺀다
- [x] 손으로 고칠 수 있게 (출처 `불러옴 → 수정함`)
- [x] `checkedAt` 을 "52분 전 기준" 으로 표시
- [x] 서버가 없으면 사유만 안내. **직접 입력·사진은 그대로 동작**
- [x] `server.js` 동적 import — 버튼을 눌러야 처음 내려온다

> **완료 기준:** 매장·날짜를 고르면 카드가 채워지고, 매진 회차가 조합 계산에서 빠진다.
> ✅ 실제 크롬으로 27개 항목 확인 (지점·날짜 전환 → 선택 → 카드 3개 → 조합 370개)

### ⚠️ 순서 주의

Floduler는 GitHub Pages(https)에 있어서 **로컬 서버(http://localhost)를 부를 수 없다.**
따라서 **공개 연동은 M4 배포가 선행되어야 한다.** 로컬끼리는 `http → http`라 지금도 붙는다.

M4 때 할 일: `server.js` 의 `DEFAULT_BASE` 를 배포 도메인으로 바꾼다.
(그전까지는 `localStorage['flod.server']` 로 각자 덮어쓴다.)

### 2026-08-27 로컬 종단 재확인 — 그리고 잡힌 버그

서버를 사기 전에 **전 구간이 실제로 붙는지** 다시 돌렸다.
로컬 서버(8083) ← Floduler(5173) ← 헤드리스 크롬. 전 단계 통과.

```
지점 목록 수신        건대점, 홍대점, 대전점
날짜 목록             8/28(금) 내일, 8/29(토), 8/30(일)
테마 렌더링           4개 · "방금 기준"
카드 추가             매진 2개 취소선 · source=server · 이동시간 0
```

그 화면에서 **난이도가 별 25 개로 찍혀서** 파서 버그를 잡았다 → [아키텍처.md](아키텍처.md) D10.

| | 이전 | 이후 |
|---|---|---|
| 공포 `"3.5"` (홍대 피안화) | `35` | `3.5` |
| 공포 `"0.5"` (좌충우돌 꼬마마법사) | `5` ← **의미가 뒤집힘** | `0.5` |
| 난이도 `size25` (우울해서 빵 샀어) | `25` | `2.5` |

- [x] 파서를 `Double` 로. 세 자리 이상은 `null` (넘겨짚지 않는다)
- [x] `V2` 마이그레이션 — 컬럼 `DOUBLE PRECISION`, 기존 값은 구분 불가라 전부 비움
- [x] **소수가 든 대전점 픽스처 추가** — 건대점 픽스처는 정수뿐이라 이 버그를 통과시켰다
- [x] Floduler `ldStars()` — `'★'.repeat(2.5)` 가 반 칸을 버리던 것을 `★★½` 로

> **교훈:** 픽스처가 한 장이면 그 한 장에 없는 경우는 영원히 안 잡힌다.
> 지점을 늘릴 때마다 픽스처도 같이 늘린다.

---

## M3 — 알림 (판정까지 완료 · 발송 경로만 남음)

- [x] **중복 정책 결정** — 쿨다운 1시간 ([아키텍처.md](아키텍처.md) D11)
- [x] **사용자 식별 결정** — Floduler 의 Firebase 로그인 재사용 ([아키텍처.md](아키텍처.md) D12)
- [x] `watch` · `notification_log` 테이블 (`V3`)
- [x] `NotificationService` — 감시 매칭 + 쿨다운 판정
- [x] `Notifier` 인터페이스 + `LoggingNotifier`
- [x] 수집 → 알림 연결 (**sync 트랜잭션 밖에서**)
- [x] 감시 등록 API — `POST` / `GET` / `DELETE /api/watches`
- [x] Firebase ID 토큰 검증 (`ApiSecurityConfig`) — 조회 API 는 공개 유지
- [x] `GET /api/schedule` 회차에 `id` 추가 — 감시를 걸 자리를 지목하려면 필요하다
- [ ] **FCM 연동** — 파이어베이스 서비스 계정 키가 있어야 한다
- [ ] Floduler 쪽 "이 자리 감시" UI

> **완료 기준:** 취소표가 났을 때 내 폰에 푸시가 온다.
> 지금은 **판정까지 끝나고 로그로 나간다.** `notify.channel=fcm` 으로 갈아 끼우면 된다.

### 실제 Postgres 로 확인한 것 (2026-08-27)

```
V3 마이그레이션            watch · notification_log 생성
인증                       /api/watches 토큰없음·엉터리토큰 → 401, /api/branches → 200
CORS 프리플라이트          POST + Authorization 통과, 허용 안 된 출처는 403
전이 → 알림                slot 172 를 매진으로 되돌리고 수집 → 🔔 발송 1건, 기록 SENT
쿨다운                     같은 자리 재수집 → 발송 0건, 보류 1건, 기록 그대로 1건
브라우저 회귀              시큐리티 추가 후에도 F-15 전 단계 통과
```

> **순서 주의였던 것:** 시큐리티를 넣으면 기존 공개 경로가 401 이 되고,
> `WebMvcConfigurer` 로 열어 둔 CORS 는 필터가 아예 못 본다.
> 그래서 **시큐리티를 먼저 넣고 회귀부터 확인한 뒤에** 감시 API 를 만들었다.

### 남은 것 — FCM

- 파이어베이스 콘솔에서 **서비스 계정 키**(JSON) 발급 → 환경변수로 주입, 커밋 금지
- `FcmNotifier` 구현 (`@ConditionalOnProperty notify.channel=fcm`)
- Floduler 가 기기 토큰을 등록할 자리 (`POST /api/devices` 같은 것)
- 웹 푸시는 사용자 허용이 필요하고 iOS 사파리는 홈화면 추가가 전제다 — 확인 필요

> **왜 M4 보다 먼저인가:** 서버를 사는 값은 "알림이 실제로 온다" 를 본 뒤에 내는 게 맞다.
> 지금 서버가 사주는 건 Floduler 불러오기 편의 하나뿐이고, M3 개발은 로컬에서 다 된다.
> 그동안 오라클 자리가 날 수도 있다.

### M3를 만들 진짜 이유 — 조합 감시

Floduler에서 3연방 조합을 짰는데 한 칸이 매진이면, **그 자리를 감시 등록하고 취소표가 뜨면 알린다.**
"이 조합이 성사되게 지켜봐줘"에 해당한다.

**두 프로젝트를 합쳐야만 나오는 기능**이고, 단순한 슬롯 알림보다 훨씬 구체적인 동기다.
M3 설계 시 `Watch`를 슬롯 하나가 아니라 **조합 단위**로도 걸 수 있는지 함께 검토한다.

---

## ⏸ M4 — 배포 (서버 확보 대기)

### 준비물 ✅ 완료 — 로컬에서 전 구간 확인

- [x] **Flyway** 로 스키마 고정 (`V1__initial_schema.sql`) + `ddl-auto=validate`
      — 빈 DB 에서 처음부터 만들어지는 것, 엔티티와 일치하는 것 확인
- [x] `Dockerfile` — 멀티스테이지, 의존성 레이어 분리, 비루트 실행
      - ⚠️ 컨테이너 안에서는 `SERVER_ADDRESS=0.0.0.0`. 앱 기본값(루프백)이면 Caddy 도 못 붙는다
- [x] `docker-compose.prod.yml` — db + app + caddy. **DB 포트를 호스트에 열지 않는다**
- [x] `Caddyfile` — `/api` 만 프록시, 나머지는 404. HTTPS 자동 발급
- [x] 디버그 엔드포인트 **기본 꺼짐** (`DEBUG_ENDPOINTS`), 배포에서는 Caddy 도 라우팅 안 함 — 두 겹
- [x] `.env.example` — 비밀값은 `.env` 로 주입, 커밋하지 않음
- [x] `restart: unless-stopped`

로컬 실기동 확인: HTTPS 응답 200, `/debug`·루트 404, CORS 는 Floduler 출처만 통과(그 외 403),
DB·앱 포트 비노출, 수집 한 바퀴(28요청) 실패 0건.

- [x] `deploy/bootstrap.sh` — 갓 만든 우분투를 한 줄로 배포까지. 멱등
- [x] **서버 잠그기** (2026-08-27) — SSH 키 전용 · 자동 보안 업데이트 · fail2ban
      - SSH 는 `authorized_keys` 가 **있는 걸 확인하고 나서만** 잠근다. 확인 없이 끄면 자기 자신을 잠근다
      - `sshd -t` · `fail2ban-client -t` 로 **적용 전에 검사**하고, 실패하면 되돌린다
      - 실제 `ubuntu:24.04` 에서 확인: 패키지명 4종 존재, `sshd -T` 로 값 적용 확인,
        드롭인이 본 파일보다 우선(Include 12줄), 깨진 설정을 검사가 잡아냄, fail2ban 설정 통과
- [x] `iptables` 삽입 위치를 찾아서 넣도록 수정 — 6 으로 박아 두면 기본 규칙이 하나만 달라져도
      REJECT 뒤에 들어가 아무 효과가 없다. 증상이 "열었는데 접속이 안 된다" 라 원인 찾기가 어렵다
- [x] `README.md`
- [x] M3 설정을 배포에 반영 (`FIREBASE_PROJECT_ID` · `NOTIFY_CHANNEL` · `NOTIFY_COOLDOWN_MINUTES`)
      — prod compose 는 환경변수를 **일일이 적는 방식**이라, 새 설정을 추가하면 여기도 같이 고쳐야 한다.
      기본값이 있어서 안 고쳐도 뜨지만, 그러면 운영자가 바꿀 방법이 없다

### 남은 것 — 서버가 있어야 한다

- [ ] 서버 준비 (오라클 무료 티어 시도 → 실패 시 저가 VPS)
      - **2026-08-26 막힘.** 오라클 도쿄 A1.Flex 가 `Out of host capacity`.
        무료 ARM 은 상시 만석에 가까워 자리가 나는 순간을 잡아야 한다.
        도쿄는 **가용성 도메인이 AD-1 하나뿐**이라 "다른 AD 로 시도" 라는 해법도 못 쓴다.
        연타하면 `Too many requests` 로 요청 제한까지 걸린다 — 몇 분 간격으로 시도할 것.
      - 시도해볼 것: 사양을 4GB 로 줄이기 / 결함 도메인 지정 해제 / 새벽(한국시간 2~6시)
      - 대안: Vultr 서울(~10ms) 또는 Linode 도쿄(~40ms), **2GB 이상**, 월 8천원선.
        Hetzner 는 유럽이라 ~280ms 라서 뺐다. 2GB 를 고르면 JVM 힙 제한을 넣어야 한다
        (지금 `MaxRAMPercentage=75` 라 2GB 박스에서 Postgres 를 밀어낸다)
- [ ] 도메인 + A 레코드
- [ ] `.env` 채우고 `docker compose -f docker-compose.prod.yml up -d --build`
- [ ] 방화벽 — 22/80/443만. **5432는 절대 열지 않는다**
- [ ] **Floduler `server.js` 의 `DEFAULT_BASE` 를 배포 도메인으로**, `index.html` 의
      `SERVER_DEPLOYED = true` 로. **둘을 함께** 바꿔야 한다
- [ ] Floduler 푸시 → Pages 갱신

> **완료 기준:** 내 노트북을 꺼도 Floduler 에서 불러오기가 된다.

---

## 규모가 커지면 걸리는 것 (미리 재둠)

`지점 50개 · 사용자 40명` 을 가정하고 계산해 봤다. **비용은 문제가 아니다.**

```
한 바퀴   50지점 × 7일  =    350요청 ·  14 MB
하루      × 288바퀴     = 100,800요청 ·  4 GB
한 달                   =   약 300만 요청 · 120 GB (전부 인바운드 — 과금 대상 아님)
서빙      40명 × 20회/일 × 5KB  =  월 120 MB 아웃바운드 (무료 한도의 1/85,000)
```

오라클 상시 무료 한도(4 OCPU·24GB / 10TB 아웃바운드 / 200GB 스토리지) 안에 전 항목이 들어간다.

### ⚠️ 한 바퀴가 주기를 넘기는 문제 — 절반만 풀렸다

순차 수집이면 한 바퀴가 `요청수 × 1.2초` 라, 350요청이면 **7분으로 5분 주기를 넘긴다.**

처음에 **지점별 병렬**로 고쳤다가 되돌렸다. 플레이33은 네 지점이 전부 `play33.kr` **한 대**라,
지점별로 동시에 긁으면 그 서버에 `28요청 / 8초 = 초당 3.5회` 가 갔다 — 우리 규칙은 초당 1회 미만이다.
**우리 화면에서 지점이 넷이라고 상대 서버가 넷이 되지 않는다** ([아키텍처.md](아키텍처.md) D13).

지금은 **사이트 단위 병렬**이고, 사이트가 하나뿐이라 실질 순차(약 33초)다.

```
사이트 1곳 (지금)   28요청    순차 33초             주기 안
사이트 20곳         350요청   동시 4 로 ~1.7분      주기 안
사이트  1곳 · 50지점 350요청   순차 7분             ← 주기를 넘긴다. 이건 병렬로 못 푼다
```

**마지막 줄이 남은 문제다.** 한 브랜드가 지점을 50개 가진 경우, 그 서버 하나에 초당 1회를
지키면서 350요청을 보내려면 물리적으로 7분이 든다. 그때는 병렬이 아니라 **수집 주기를 늘리거나,
날짜 범위를 줄이거나, 자주 바뀌는 날짜만 자주 보는** 쪽으로 풀어야 한다. M5 에서 판단한다.

`Play33CollectorRateTest` 가 **호스트당 요청 간격**을 잰다. 한 바퀴 시간이 아니라 그걸 재야 한다 —
빨라지는 건 얼마든지 만들 수 있고 그게 곧 규칙 위반이다.

### ⏳ 남은 것 — 매 수집마다 전 슬롯 UPDATE

```
슬롯 행    50 × 5 × 11 × 7 = 19,250
하루 쓰기  × 288바퀴        = 554만 건   (초당 64건)
계층 폴링 후 (매 바퀴 3.5일치)  = 277만 건   (초당 32건)   ← 절반
```

계층 폴링(D14)이 절반으로 줄여 주지만 **원인을 없애지는 않는다.**

원인은 `lastCheckedAt` 을 **슬롯마다** 갱신하는 것이다. 값이 안 바뀌어도 "언제 봤는지" 가
바뀌니 전 행이 매번 쓰기가 된다. Postgres 가 못 버틸 양은 아니지만 WAL 과 테이블 부풀림이 쌓인다.

`lastCheckedAt` 을 슬롯이 아니라 `(테마, 날짜)` 단위로 올리면 쓰기가 1/11 로 준다.
**4지점에서는 문제가 아니라 지금 고치지 않는다.** M5 에서 매장을 늘릴 때 같이 한다.

---

## ▶ 다음 작업 — 계층 폴링 + 두 번째 매장 (M5)

**세 커밋으로 나눈다.** 한 번에 하면 수집 속도가 틀어졌을 때 어느 게 범인인지 모른다.
시큐리티 → CORS → 감시 API 를 나눠서 넣은 것과 같은 이유다.

### ✅ ① 매장 어댑터 인터페이스 (완료 · 2026-08-27) — [아키텍처.md](아키텍처.md) D15

- [x] 어댑터가 "이 날짜를 긁으려면 어떤 요청이 필요한가" 를 **목록**으로 내놓는다
      — 작업 하나 = **HTTP 요청 정확히 1회**
- [x] **속도 조절을 `HostRateLimiter` 가 요청마다 직접 건다** — 수집기는 더 이상 `sleep` 하지 않는다
- [x] 계측 지점을 HTTP 계층으로 내렸다. `HostRateLimiterTest` · `StoreCollectorTest` 로 대체
- [x] 플레이33만으로 착지. **동작 변화 없음** — 실측 28요청 · 33초,
      `/api/branches` · `/api/schedule` · `/debug/collect` 200, `/api/watches` 401
- [x] `DaySchedule.branch` → `store: StoreRef` — 저장·조회가 매장 종류를 모르게 된다
- [x] 테스트 61개 통과

> ⚠️ **지금 `Play33CollectorRateTest` 는 곧 거짓말을 시작한다.**
> `fetch()` 호출 간격을 재는데, 키이스케이프 `fetch()` 하나가 요청 3번이 되면
> 간격이 3배로 보여서 **테스트는 통과하고 규칙은 깨진다.** D13 과 똑같은 실수가
> 한 층 아래에서 반복되는 것이라 더 안 보인다. ① 에서 계측 지점을 내리는 게 그래서다.

### ✅ ② 계층 폴링 (완료 · 2026-08-27) — [아키텍처.md](아키텍처.md) D14

- [x] 토·일 매 바퀴 / 금 2바퀴마다 / 그 외 순환 (`PollingSchedule`)
- [x] **매장을 가리지 않고 똑같이 적용** — 플레이33은 필요 없지만 예외를 두면 거기서 어긋난다
- [x] 바퀴 번호를 **시계에서** 뽑는다 — 카운터면 재시작마다 0 으로 돌아가 같은 평일만 본다
- [x] 굶는 날짜가 없다는 것을 테스트로 고정 (`굶는 날짜가 없다`)
- [x] 격리 복구가 "연속 3바퀴" 라 평일은 실제 시간이 15분 → 60분이 된다. **의도한 동작**

```
실측   플레이33 한 바퀴  28요청 33초  →  12요청 13초
주기   토·일 5분 · 금 10분 · 그 외 20분
```

### ③ 키이스케이프 어댑터

- [ ] 테마 목록 캐시 (지점별, 하루 한 번 갱신). **갱신도 줄을 세운다**
- [ ] `info_num` / `theme_num` 을 짝으로 들고 다니고, 응답의 `name` 을 검증한다
      — 섞어 보내면 에러가 아니라 엉뚱한 테마가 온다
- [ ] `level`(난이도)이 `"4.5"` 같은 소수로 오는지 확인 — D10 과 같은 자리다
- [ ] `play_time` 은 `"75분"` 처럼 단위가 붙어 있다
- [ ] 회차가 7일 내내 0개인 테마(에버랜드) 처리 — 수원점 선례대로 경고만
- [ ] 픽스처 테스트 (JSON 응답을 `src/test/resources/` 에)

---

## M5 — 매장 확장

- [ ] 홍대 지역 다른 브랜드 3곳 조사 (요청 구조가 겹치는지 확인)
- [ ] 어댑터 추가 — **3개를 각각 하드코딩한 뒤** 공통점을 뽑아 설정화 검토
  - 처음부터 범용 파서를 설계하지 않는다. 틀린 추상화가 나온다
- [ ] 어댑터 헬스체크 — 마지막 성공 시각, 슬롯 수 0 감지, 자동 격리
- [ ] 매장 제휴 시도 (크롤링보다 먼저 문의)

> **완료 기준:** 매장이 하나 깨져도 나머지가 정상 동작하고, 깨진 걸 자동으로 알려준다.

---

## 상시 규칙

- 새 어댑터를 추가하면 **픽스처 테스트를 같이 만든다** ([아키텍처.md](아키텍처.md) D5)
- 설계 결정을 내리면 [아키텍처.md](아키텍처.md) §4에 `D번호`로 남긴다
- 기획이 바뀌면 [기획.md](기획.md)를 먼저 고치고 코드를 고친다
- 비밀값은 커밋하지 않는다

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-26 | 최초 작성. M1 완료 기록, M2~M5 계획 수립 |
| 2026-08-26 | 자매 프로젝트 Floduler 연동 결정. 조회 API(M2.5)를 알림보다 먼저 내보내기로. Floduler §4.10·§4.13 번복 |
| 2026-08-26 | M2 완료 — Postgres 실기동·전이·격리·복구 전 구간 확인. Colima 도입 |
| 2026-08-26 | M2.5 서버 쪽 완료 — 조회 API 2종, 매진 회차 유지, 테마 메타데이터 추가 |
| 2026-08-26 | M2.5 완료 — Floduler F-15 구현. 지점·날짜·테마 선택 모달, 매진 회차 유지, 신선도 표시 |
| 2026-08-26 | M4 준비물 완료(부트스트랩·README). 서버 확보 실패로 보류, M3 를 먼저 진행하기로 |
| 2026-08-27 | 로컬 종단 재확인 통과. 0.5 단위 평점 파서 버그 수정(D10) — `V2` 마이그레이션·대전점 픽스처·Floduler 반 칸 표기 |
| 2026-08-27 | M3 알림 — 감시 등록 API·쿨다운 판정·Firebase 토큰 검증까지 완료. 실제 DB 로 전이→발송→쿨다운 확인. FCM 발송 경로만 남음 |
| 2026-08-27 | 수집을 매장 사이 병렬로 (D13) — 한 바퀴 40초 → 8초. 50매장 규모 계산 및 남은 병목 기록 |
| 2026-08-27 | 병렬 단위를 지점 → 사이트로 정정. 지점별 병렬은 같은 서버에 초당 3.5회를 보내 수집 규칙을 어겼다 (D13 재작성) |
| 2026-08-27 | bootstrap 보안 강화 — SSH 키 전용·자동 보안 업데이트·fail2ban. iptables 삽입 위치 하드코딩 제거 |
| 2026-08-27 | 계층 폴링(D14)·어댑터 인터페이스(D15) 결정. 키이스케이프 조사 완료. 다음 작업을 세 커밋으로 분할 |
| 2026-08-27 | ① 매장 어댑터 인터페이스 완료 — 속도 규칙을 HostRateLimiter(HTTP 계층)로 내림. 동작 변화 없음 |
| 2026-08-27 | ② 계층 폴링 완료 — 주말 5분·금 10분·평일 20분. 한 바퀴 28요청 33초 → 12요청 13초 |
