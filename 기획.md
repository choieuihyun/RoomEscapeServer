# 기획

> **이 문서의 역할** — 무엇을 왜 만드는지. 기획이 바뀌면 **이 문서를 먼저 고치고 그다음 코드를 고친다.**
> 최종 수정: 2026-08-26

---

## 한 줄 정의

**방탈출 예약 정보를 모아, 자리가 나면 알려주고 다른 도구에 먹여주는 서버.**

수집한 데이터가 두 곳으로 나간다.

1. **알림** — `예약 불가 → 예약 가능` 전이를 감지해 푸시 (이 서버의 원래 목적)
2. **[Floduler](#자매-프로젝트--floduler) 피드** — 연방 시간표 계산기에 회차와 매진 여부를 제공

2번이 먼저 나간다. 이미 만든 수집기에 조회 API만 얹으면 되고, 1번은 저장·감지·알림이 다 있어야 하기 때문이다.

---

## 왜 만드나

인기 방탈출 테마는 예약이 열리자마자 매진된다. 지금 자리를 잡으려면 두 가지 방법뿐이다.

1. 예약이 새로 열리는 시각에 맞춰 대기하다 클릭
2. 취소표를 노리고 예약 페이지를 수동으로 새로고침

둘 다 사람이 계속 붙어 있어야 한다. **이건 기계가 대신할 수 있는 일이다.**

그리고 이건 매장에게도 손해가 아니다. 취소로 비는 자리가 매출로 바뀌는 것이라, 이해가 대립하지 않는다.

---

## 타겟

- 특정 인기 테마를 꼭 하고 싶은데 자리를 못 잡은 사람
- 주말/저녁 시간대를 노리는 사람
- 갑자기 일정이 비어 "오늘 갈 수 있는 곳"을 찾는 사람

---

## 핵심 기능 (MVP)

| # | 기능 | 설명 |
|---|---|---|
| 1 | **슬롯 감시** | 매장·테마·날짜별 예약 가능 여부를 주기적으로 수집 |
| 2 | **전이 감지** | `예약 불가 → 예약 가능` 으로 바뀐 순간만 골라냄 |
| 3 | **알림 신청(Watch)** | 사용자가 "이 테마, 이 날짜 범위, 이 시간대"를 등록 |
| 4 | **알림 발송** | 조건에 맞는 전이가 생기면 푸시 |
| 5 | **조회 API** | 수집한 시간표를 외부(Floduler)에 내려줌 |

**1 → 2 까지가 이 서비스의 심장이다.** 3, 4는 그 위에 얹는 껍데기다.
그래서 개발 순서도 이 번호를 따른다. 단 **5는 1만 있으면 되므로 먼저 나간다.**

---

## 자매 프로젝트 — Floduler

`~/RoomEscapeScheduler` · <https://choieuihyun.github.io/RoomEscapeScheduler/>

하루에 방탈출을 3연방 할 때 **겹치지 않는 회차 조합을 전부 계산해 주는** 정적 웹 도구다.
이미 만들어져 배포까지 끝나 있고, 회차 입력은 **직접 입력 / 예약 화면 사진(브라우저 내 OCR)** 두 가지다.

### 왜 이 서버가 필요한가 — Floduler가 직접 적어둔 이유

Floduler `기획.md`는 **두 군데에서 "서버가 없어서 못 한다"** 고 명시해뒀다.

> **§4.10** — 관리의 핵심인 "신테마가 나왔는지"는 이 구조에서 알 방법이 없다 —
> **예약 사이트를 주기적으로 긁어야 하고 그건 서버가 필요하다.**

> **§4.13** — (매진 제외 옵션은) 체크박스가 성립하려면 매진 여부가 데이터에 들어와 있어야 하는데,
> 두 입력 경로 어느 쪽에서도 안 들어온다. (…) **예약할 수 없는 조합을 1위로 믿게 만든다.**

**이 서버가 정확히 그 빈칸이다.** 두 결정은 Floduler `기획.md` §4.30 으로 번복됐다.

### 가장 큰 소득은 회차가 아니라 매진이다

Floduler의 조합 계산에는 이미 `session.soldout` 필드가 있고 화면도 그린다.
§4.13 때문에 **채울 방법이 없어 비어 있었을 뿐이다.** 이 서버가 그 값을 채우면
"계산해 준 1위 조합이 사실 예약 불가"라는 구조적 결함이 사라진다.

### 연결 방식

- **저장소는 합치지 않는다.** Pages는 루트 배포가 필요하고, Kotlin/Gradle과 툴체인·배포 주기가 겹치지 않는다.
  잇는 것은 **API 계약**뿐이다
- **이 서버는 Floduler의 선택 사항이다.** 서버가 없거나 멎어도 직접 입력·사진은 그대로 동작한다.
  Floduler가 Firebase 계정 기능에 이미 쓰고 있는 패턴(§4.18)과 같다
- **HTTPS 필수.** Pages가 https라 http 응답은 브라우저가 차단한다. CORS 허용도 서버 몫이다

계약 상세는 Floduler `작업명세서.md` §4.4.

### 소요시간은 홈페이지 값을 그대로 쓴다

실측하면 회차 간격(75~80분)이 홈페이지 표기(60~65분)보다 길다. 브리핑·정리·퇴장 때문인데,
**그 여유는 Floduler의 매장 간 이동시간(§4.7)과 최소 공백 경고가 이미 흡수한다.**
따라서 서버는 홈페이지에 적힌 값을 가공 없이 내려준다.

### 다음 단계 (아직 안 함)

짜여진 조합 안에 매진 회차가 있으면 그 자리를 감시 등록하고, 취소표가 나면 알린다.
**두 프로젝트를 합쳐야만 나오는 기능이고, 이게 알림 기능(M3)을 만들 진짜 이유다.**

---

## 범위 밖 (지금은 안 한다)

의도적으로 안 하는 것들. 나중에 필요해지면 이 문서를 고치고 옮긴다.

- **대리 예약** — 알림만 보낸다. 예약은 사용자가 직접 한다
- 리뷰·평점·커뮤니티
- 탈출 기록/통계 (재미있지만 다른 서비스다)
- 전국 모든 매장 — 아래 마일스톤 참고
- 로그인/회원가입 (알림 대상 식별이 필요해지는 시점에 다시 판단)

---

## 도메인 지식 (조사해서 확인한 것)

### 방탈출 예약의 성질

- 취소표는 **몇 분 안에** 나간다. 감지 지연이 곧 서비스 가치의 손실이다
- 그래서 **1시간 주기 폴링은 무의미하다.** 알림이 갔을 때 이미 없다
- 공공 API 같은 건 없다. 매장마다 사이트가 제각각이다

### 플레이33 (첫 대상 매장) — 2026-08-26 확인

브랜드: PLAY33 / 미스터리 주식회사. 프랜차이즈라 지점이 여러 개다.

| 항목 | 내용 |
|---|---|
| 조회 방법 | `GET https://play33.kr/reservation?branch={id}&date={yyyy-MM-dd}` |
| 인증 | **불필요.** 쿠키·토큰·헤더 없이 200 |
| 응답 | 서버 렌더 HTML (JSON API 아님) |
| 한 요청의 범위 | **그 지점의 전 테마 시간표가 한꺼번에** 옴 |
| 지점 | 건대점(1) 홍대점(4) 대전점(5) 수원점(7) — `6`은 "테스트"라 제외 |
| 테마 수 | 건대 3, 홍대 1, 대전 4, 수원 1 = **총 9** |
| 예약 오픈 범위 | **7일 = `[오늘, 오늘+6]`** — 8/26 기준 9/1은 200, 9/2는 302로 확인 |
| 매진 슬롯 | **목록에 그대로 표시됨** (`예약 불가`) |
| 범위 밖 날짜 | HTTP 302 → 홈으로 리다이렉트 |

위는 전부 실제 요청으로 확인한 값이다. 아래는 아직 **확인하지 않았다.**

| 항목 | 근거 | 상태 |
|---|---|---|
| 매일 20:00에 새 날짜(D+7)가 열린다 | 페이지의 숨은 필드 `reservation_range_open_time=20:00:00` | ⚠️ **미검증 — 추정** |

`open_time`이라는 이름만으로는 "새 날짜가 열리는 시각"인지, "그날 예약 마감 시각"인지, 다른 무엇인지 알 수 없다.
**확인 방법:** 20:00 이전과 이후에 `date=오늘+7`을 각각 조회한다. `302 → 200`으로 바뀌면 추정이 맞다.
확인 전까지 이 값에 기대는 기능은 만들지 않는다.

**여기서 나오는 두 가지 결론:**

1. **전체 크롤이 가볍다.** `4지점 × 7일 = 28요청`이면 플레이33 전부다.
   5분마다 전량 수집해도 초당 0.1회 수준이라, 감시 대상만 골라 긁는 최적화가 필요 없다.

2. **(가설) 신규 오픈 순간이 두 번째 기회일 수 있다.** 취소표와 별개로, 새 날짜가 열리는 순간은
   인기 테마를 잡을 수 있는 큰 창이다. 다만 위 표에 적었듯 **20:00이라는 시각 자체가 아직 미검증**이라,
   먼저 확인한 뒤에 별도 기능으로 만들지 판단한다.

---

## 데이터 수집 원칙

**지켜야 할 선.** 기술적으로 가능한 것과 해도 되는 것은 다르다.

- ✅ 매장당 **한 번에 하나의 요청**, 초당 1회 미만 유지
- ✅ `User-Agent`는 **빈 값으로 보낸다.** 이름도 연락처도 남기지 않는다
  - 헤더를 아예 빼면 자바가 `Java-http-client/21.0.11` 을 대신 보내 JDK 버전까지 나간다.
    빈 값이 실제로 제일 덜 남긴다 (play33 이 200 으로 받는 것도 확인)
  - 브라우저 UA 를 흉내내지는 않는다
- ✅ `429`/`503` 응답에는 즉시 백오프
- ✅ `robots.txt`와 이용약관을 대상 추가 전에 확인
- ❌ **캡차·Cloudflare·WAF 우회는 하지 않는다.** 그건 "오지 마"라는 명확한 의사표시다
  - 그런 매장은 크롤 대상에서 빼거나 제휴로 전환한다
- ❌ 예약 자체를 자동화하지 않는다

### 제휴라는 대안

매장에 직접 "취소 자리 채워드리는 서비스"를 제안하는 길이 있다.
사이트가 매장마다 제각각이라 **어차피 매장당 어댑터 하나씩 드는 비용은 같다.**
그럴 바엔 허락받고 데이터를 받는 쪽이 더 정확하고, 안 깨지고, 명분도 좋다.
거절당해도 잃는 게 없으므로 매장 확장 시 항상 먼저 시도한다.

---

## 마일스톤

| 단계 | 범위 | 완료 기준 |
|---|---|---|
| **M1** | 플레이33 한 지점 수집 | 슬롯 목록을 코드로 읽는다 ✅ **완료** |
| **M2** | 저장 + 전이 감지 | `불가→가능` 전이가 로그에 찍힌다 |
| **M2.5** | **조회 API + Floduler 연동** | Floduler 카드가 서버 회차로 채워지고 매진이 표시된다 |
| **M3** | 알림 | 내 폰으로 푸시가 온다 |
| **M4** | 배포 | 24시간 무인 동작 |
| **M5** | 매장 확장 | 홍대 지역 다른 브랜드 추가 |

**M2.5가 첫 실사용이다.** 알림보다 먼저 쓸 사람이 생기고, 그게 이 서버가 살아 있을 이유가 된다.
(M2.5는 M4 배포가 선행되어야 실제로 쓰인다 — 로컬 서버는 Pages에서 못 부른다.)

---

## 열려 있는 결정

아직 정하지 않았고, 해당 단계에서 정한다.

- **알림 중복 정책** — 한 슬롯당 평생 1회(once-ever) vs 쿨다운 재발송
  - 현재 기울어진 쪽: **쿨다운(6시간).** "놓쳤어도 다음 기회"가 이 서비스의 가치라서
- **사용자 식별 방법** — 로그인 vs 기기 토큰만
- **신규 오픈 알림을 별도 기능으로 뺄지** — 오픈 시각 검증이 선행되어야 판단 가능

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-26 | 최초 작성. 플레이33 조사 결과 반영, 감시 대상 선별 폴링 → 전량 수집으로 방침 변경 |
| 2026-08-26 | 자매 프로젝트 Floduler 연동 결정. 조회 API(M2.5)를 알림보다 먼저 내보내기로. Floduler §4.10·§4.13 번복 |
