# 배포 절차 — 오라클 클라우드

집에서 혼자 보고 따라 할 수 있게 적었다. **순서를 지키는 게 중요한 곳이 두 군데** 있고, 표시해 뒀다.

> 서버가 왜 필요한지는 [README](README.md), 왜 오라클인지는 아래 "왜 이 조합인가" 를 본다.

---

## 한눈에

```
① MFA 켜기            ← 유료 전환 **전에**
② 유료(PAYG) 전환
③ 예산 알림 $1
④ 인스턴스 생성        A1.Flex · 1 OCPU · 6GB · Ubuntu 24.04
⑤ VCN 보안 목록 80·443 ← 빼먹으면 접속이 안 된다
⑥ DuckDNS 도메인
⑦ bootstrap.sh 한 줄
⑧ 수집 켜기 · Floduler 연결
```

④까지가 콘솔 작업, ⑦부터가 터미널이다. 넉넉히 한 시간.

---

## 왜 이 조합인가

```
오라클 상시 무료 A1   4 OCPU · 24GB 를 공짜로 준다 (유료 환산 월 $100~150)
우리가 쓰는 것        1 OCPU · 6GB — 한도의 1/4
청구                 $0.00
```

**유료(PAYG)로 전환해도 상시 무료 리소스는 계속 0원이다.** 전환하는 이유는 요금이 아니라
**A1 인스턴스가 잡히기 때문**이다. 무료 계정은 `Out of host capacity` 로 막히는 일이 잦은데
유료 계정은 배정 우선순위가 높다.

대신 **상한이 없는 계정**이 된다. 그래서 아래 네 가지를 지킨다.

| | |
|---|---|
| MFA 켜기 | 계정 탈취를 막는 유일한 방어 |
| **API 키 만들지 않기** | 공개 저장소에 새면 몇 초 만에 털린다. 우리 배포는 SSH 로만 한다 |
| `상시 무료 적격` 라벨만 고르기 | 실수로 유료 리소스를 만드는 것을 막는다 |
| 예산 알림 $1 | 앞의 셋이 뚫렸을 때 **빨리 아는** 용도 |

> 예산 알림은 **잠금이 아니라 알림이다.** 한도를 넘겨도 리소스를 끄지 않는다.
> 하드 캡을 주는 클라우드는 사실상 없다 — 결제 문제로 서비스가 갑자기 죽는 게 더 큰 사고라서다.

---

# 콘솔 작업

> **메뉴를 못 찾겠으면 상단 검색창을 쓴다.** 오라클 콘솔은 메뉴 위치와 한글 표기가 자주 바뀐다.
>
> | 단계 | 검색어 |
> |---|---|
> | ① MFA | `내 프로필` |
> | ② 유료 전환 | `업그레이드 및 결제` (우상단에 버튼이 떠 있기도 하다) |
> | ③ 예산 | `예산` |
> | ④ 인스턴스 | `인스턴스` |
> | ⑤ 보안 목록 | `가상 클라우드 네트워크` |

## ① MFA 켜기 — 유료 전환 **전에**

**콘솔 화면마다 이름이 다르다. 전부 같은 기능이다.**

```
MFA = 다중 요소 인증 = 2단계 확인 = 2단계 인증
```

메뉴 위치도 계정 종류에 따라 다르다. **상단 검색창에 `내 프로필` 을 치는 게 제일 빠르다.**

```
신형(Identity Domains)   우상단 프로필 → 내 프로필 → [보안] 탭 → 2단계 확인
구형                     우상단 프로필 → 사용자 설정 → 왼쪽 [다중 인증]
```

인증 앱은 아무 TOTP 앱이나 된다 (Oracle Mobile Authenticator · Google Authenticator · Authy).

> **복구 코드가 나오면 텍스트로 따로 저장한다.** 폰을 잃어버리면 그게 유일한 길이고,
> 오라클은 계정 복구가 까다롭다.

전환하고 나서 켜면 그 사이가 비어 있다. 5분이면 끝난다.

## ② 유료(PAYG) 전환

```
결제 및 비용 관리 → 업그레이드 및 결제
```

카드가 등록된다. 상시 무료 리소스만 쓰면 청구는 `$0.00` 이다.

## ③ 예산 알림

```
청구 및 비용 관리 → 예산 → 예산 생성
  금액   $1        (오라클이 허용하는 최저)
  알림   20% · 50% · 100%   →  $0.2 · $0.5 · $1 에 각각 울린다
```

**정상이면 1센트도 안 나온다.** 그래서 금액이 얼마냐가 아니라 **"0 이 아니다"** 가 신호다.
첫 알림이 오는 순간 이미 뭔가 잘못된 것이다.

## ④ 인스턴스 생성

```
컴퓨트 → 인스턴스 → 인스턴스 생성

이미지   Canonical Ubuntu 24.04          ← Minimal 말고 일반
셰이프   VM.Standard.A1.Flex             ← "상시 무료 적격" 라벨 확인
         1 OCPU · 6GB
네트워크  기존 VCN / 퍼블릭 서브넷 · 퍼블릭 IPv4 주소 지정 ✅
SSH 키   공개키 붙여넣기
```

공개키를 붙여넣을 때 **줄바꿈이 들어가면 안 된다.** 화면에서 줄이 접혀 보여서 그대로 복사하면
깨진다. 이렇게 복사한다:

```bash
tr -d '\n' < ~/Desktop/클라우드\ 키/ssh-key.key.pub | pbcopy
```

> **프라이빗 키는 처음 만들 때 한 번만 받을 수 있다.** 인스턴스를 다시 만들어도
> 이미 받아 둔 키를 계속 쓰면 된다 — 공개키를 붙여넣기만 하면 같은 키로 접속된다.

## ⑤ VCN 보안 목록 — **이거 빼먹으면 접속이 안 된다**

```
네트워킹 → 가상 클라우드 네트워크 → (내 VCN) → 서브넷 → 보안 목록 → 수신 규칙 추가

소스 0.0.0.0/0 · IP 프로토콜 TCP · 대상 포트 80
소스 0.0.0.0/0 · IP 프로토콜 TCP · 대상 포트 443
```

> **지름길:** 인스턴스를 먼저 만들고, 인스턴스 상세 화면의 **서브넷 링크**를 타고 들어가면
> VCN 을 헤맬 필요가 없다. 거기서 바로 보안 목록으로 갈 수 있다.

**오라클은 방화벽이 두 겹이다.**

```
바깥쪽  VCN 보안 목록   ← 콘솔에서만 열 수 있다 (여기)
안쪽    서버 iptables   ← bootstrap.sh 가 연다
```

둘 다 열려야 통한다. 한쪽만 열고 "왜 접속이 안 되지" 로 헤매기 쉬운 곳이다.
**80 을 빼먹으면 HTTPS 인증서 발급부터 실패한다** — Let's Encrypt 가 80 으로 확인한다.

## ⑥ DuckDNS 도메인 (무료)

```
duckdns.org → GitHub/구글 로그인 → 서브도메인 만들기
  예) roomescape-hyune
토큰도 복사해 둔다 (페이지 위쪽에 있다)
```

도메인이 있어야 Caddy 가 HTTPS 인증서를 받는다. 그래야 Floduler(GitHub Pages, https)가
이 서버를 부를 수 있다 — **https 페이지는 http 주소를 못 부른다.**

---

# 터미널 작업

## ⑦ 접속 확인

```bash
chmod 600 ~/Desktop/클라우드\ 키/ssh-key.key      # 권한이 넓으면 ssh 가 거부한다
ssh -i ~/Desktop/클라우드\ 키/ssh-key.key ubuntu@<공인IP>
```

사용자는 `ubuntu` 다 (`opc` 아님 — 그건 오라클 리눅스). 들어가지면 `exit` 하고 나온다.

## ⑧ 배포 — 한 줄

```bash
ssh -i ~/Desktop/클라우드\ 키/ssh-key.key ubuntu@<공인IP> 'bash -s' -- \
  <도메인> <DuckDNS토큰> < deploy/bootstrap.sh
```

예:

```bash
ssh -i ~/key ubuntu@158.180.1.2 'bash -s' -- \
  roomescape-hyune.duckdns.org a1b2c3d4-... < deploy/bootstrap.sh
```

하는 일:

```
도커 설치 → 서버 안쪽 방화벽 → 서버 잠그기 → DuckDNS → 코드 →
.env 생성 → 빌드·기동 → 응답 확인
```

**여러 번 실행해도 안전하다**(멱등). 첫 빌드는 소스에서 컴파일해서 **10분쯤** 걸린다.

> **서버 잠그기** 는 셋이다 — SSH 키 전용 · 자동 보안 업데이트(04:00 재부팅) · fail2ban.
> SSH 는 `authorized_keys` 가 있는 걸 확인하고 나서만 잠근다. 확인 없이 끄면 자기 자신을 잠근다.

끝나면 이렇게 나온다:

```
배포 끝났다.
  서버 내부 확인 :  curl -sk https://localhost/api/branches
  바깥에서 확인   :  curl -s https://<도메인>/api/branches
```

## ⑨ 수집 켜기

`bootstrap.sh` 가 만든 `docker-compose.prod.yml` 은 이미 `COLLECTOR_ENABLED=true` 다.
확인만 한다:

```bash
ssh ... 'cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml logs --tail 30 app'
```

`수집 한 바퀴 완료 — …초 (날짜 [...] · 사이트 3곳 · 요청 …건 …)` 이 5분마다 찍히면 정상이다.

## ⑩ Floduler 연결

Floduler 저장소(`~/RoomEscapeScheduler`)에서 **두 곳**을 고친다.

```js
// server.js
const DEFAULT_BASE = 'https://<도메인>';

// index.html
const SERVER_DEPLOYED = true;
```

커밋·푸시하면 GitHub Pages 가 알아서 반영한다. **둘 다 고쳐야 한다** —
`SERVER_DEPLOYED` 가 false 면 버튼이 숨겨진다.

---

## 확인

```bash
curl -s https://<도메인>/api/branches | head -c 300      # 200 · 16지점
curl -s https://<도메인>/debug/collect                    # 404 여야 한다 (Caddy 가 막는다)
curl -s https://<도메인>/                                 # 404 여야 한다
```

그리고 브라우저로 `https://choieuihyun.github.io/RoomEscapeScheduler/` 에서
**`회차 불러오기`** 버튼이 보이고 눌리면 끝이다. (콘솔에 `flod.server` 를 넣을 필요가 없어진다)

---

## 막혔을 때

| 증상 | 원인과 대처 |
|---|---|
| `Out of host capacity` | A1 자리가 없다. **PAYG 면 대부분 뚫린다.** 그래도 안 되면 몇 시간 뒤 재시도. 도쿄는 AD 가 하나뿐이라 "다른 AD" 선택지가 없다 |
| `Too many requests for the user` | 너무 빨리 여러 번 눌렀다. **몇 분 쉬고** 다시 |
| SSH 가 안 붙는다 | 키 권한(`chmod 600`) · 사용자명(`ubuntu`) · 공인 IP 확인. 공개키에 줄바꿈이 들어갔을 수도 있다 |
| SSH 는 되는데 `https://도메인` 이 안 열린다 | **VCN 보안 목록(⑤)** 을 안 열었을 가능성이 제일 크다. `bootstrap.sh` 는 안쪽만 연다 |
| 인증서 발급 실패 | **80 포트**가 막혀 있다. 443 만 열면 안 된다 |
| Pages 에서 버튼이 안 보인다 | `SERVER_DEPLOYED = true` 를 안 고쳤다 |
| 앱이 안 뜬다 | `sudo docker compose -f docker-compose.prod.yml logs app` 를 본다 |

---

## 나중에 옮길 때

우리 스택은 통째로 컨테이너라 **이사 비용이 거의 0** 이다.

```bash
# 옛 서버에서
sudo docker compose -f docker-compose.prod.yml exec -T db \
  pg_dump -U roomescape roomescape > dump.sql

# 새 서버에서
ssh <새서버> 'bash -s' -- <도메인> <토큰> < deploy/bootstrap.sh
cat dump.sql | ssh <새서버> 'cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml exec -T db psql -U roomescape roomescape'
```

DuckDNS 도메인만 새 IP 로 바꾸면 Floduler 쪽은 손댈 게 없다.

---

## 운영 중 자주 쓰는 것

```bash
# 로그
cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml logs -f app

# 취소표만 보기
… logs app | grep 🎟️

# 코드 갱신 후 재배포 (멱등이라 그냥 다시 돌리면 된다)
ssh <서버> 'bash -s' -- <도메인> <토큰> < deploy/bootstrap.sh

# 상태
sudo docker compose -f docker-compose.prod.yml ps
```

> `.env` 는 **덮어쓰지 않는다.** DB 비밀번호가 거기 있고, 바뀌면 기존 DB 를 못 연다.
