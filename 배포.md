# 배포 절차 — 오라클 클라우드

> **2026-08-28 실제로 이 절차대로 배포했다.** → `https://floduler.duckdns.org`
> 그때 걸린 것 셋을 아래 "실제로 겪은 것" 에 적었고, 스크립트는 이미 고쳐 뒀다.

혼자 보고 따라 할 수 있게 적었다. **순서를 지키는 게 중요한 곳이 두 군데** 있고, 표시해 뒀다.

> 서버가 왜 필요한지는 [README](README.md), 왜 오라클인지는 아래 "왜 이 조합인가" 를 본다.

---

## 한눈에

```
① MFA 켜기            ← 유료 전환 **전에**
② 유료(PAYG) 전환
③ 예산 알림 $1
④ 인스턴스 생성        A1.Flex · 1 OCPU · 6GB · Ubuntu 24.04
⑤ VCN 보안 목록 80·443 ← 빼먹으면 접속이 안 된다
⑥ DuckDNS 도메인
⑦ bootstrap.sh 한 줄
⑧ 수집 켜기 · Floduler 연결
```

④까지가 콘솔 작업, ⑦부터가 터미널이다. 넉넉히 한 시간.

---

## 왜 이 조합인가

```
오라클 상시 무료 A1   4 OCPU · 24GB 를 공짜로 준다 (유료 환산 월 $100~150)
우리가 쓰는 것        1 OCPU · 6GB — 한도의 1/4
청구                 $0.00
```

**유료(PAYG)로 전환해도 상시 무료 리소스는 계속 0원이다.** 전환하는 이유는 요금이 아니라
**A1 인스턴스가 잡히기 때문**이다. 무료 계정은 `Out of host capacity` 로 막히는 일이 잦은데
유료 계정은 배정 우선순위가 높다.

대신 **상한이 없는 계정**이 된다. 그래서 아래 네 가지를 지킨다.

| | |
|---|---|
| MFA 켜기 | 계정 탈취를 막는 유일한 방어 |
| **API 키 만들지 않기** | 공개 저장소에 새면 몇 초 만에 털린다. 우리 배포는 SSH 로만 한다 |
| `상시 무료 적격` 라벨만 고르기 | 실수로 유료 리소스를 만드는 것을 막는다 |
| 예산 알림 $1 | 앞의 셋이 뚫렸을 때 **빨리 아는** 용도 |

> 예산 알림은 **잠금이 아니라 알림이다.** 한도를 넘겨도 리소스를 끄지 않는다.
> 하드 캡을 주는 클라우드는 사실상 없다 — 결제 문제로 서비스가 갑자기 죽는 게 더 큰 사고라서다.

---

# 콘솔 작업

> **메뉴를 못 찾겠으면 상단 검색창을 쓴다.** 오라클 콘솔은 메뉴 위치와 한글 표기가 자주 바뀐다.
>
> | 단계 | 검색어 |
> |---|---|
> | ① MFA | `내 프로필` |
> | ② 유료 전환 | `업그레이드 및 결제` (우상단에 버튼이 떠 있기도 하다) |
> | ③ 예산 | `예산` |
> | ④ 인스턴스 | `인스턴스` |
> | ⑤ 보안 목록 | `가상 클라우드 네트워크` |

## ① MFA 켜기 — 유료 전환 **전에**

**콘솔 화면마다 이름이 다르다. 전부 같은 기능이다.**

```
MFA = 다중 요소 인증 = 2단계 확인 = 2단계 인증
```

메뉴 위치도 계정 종류에 따라 다르다. **상단 검색창에 `내 프로필` 을 치는 게 제일 빠르다.**

```
신형(Identity Domains)   우상단 프로필 → 내 프로필 → [보안] 탭 → 2단계 확인
구형                     우상단 프로필 → 사용자 설정 → 왼쪽 [다중 인증]
```

인증 앱은 아무 TOTP 앱이나 된다 (Oracle Mobile Authenticator · Google Authenticator · Authy).

> **복구 코드가 나오면 텍스트로 따로 저장한다.** 폰을 잃어버리면 그게 유일한 길이고,
> 오라클은 계정 복구가 까다롭다.

전환하고 나서 켜면 그 사이가 비어 있다. 5분이면 끝난다.

## ② 유료(PAYG) 전환

```
결제 및 비용 관리 → 업그레이드 및 결제
```

카드가 등록된다. 상시 무료 리소스만 쓰면 청구는 `$0.00` 이다.

## ③ 예산 알림

```
청구 및 비용 관리 → 예산 → 예산 생성
  금액   $1        (오라클이 허용하는 최저)
  알림   20% · 50% · 100%   →  $0.2 · $0.5 · $1 에 각각 울린다
```

**정상이면 1센트도 안 나온다.** 그래서 금액이 얼마냐가 아니라 **"0 이 아니다"** 가 신호다.
첫 알림이 오는 순간 이미 뭔가 잘못된 것이다.

## ④ 인스턴스 생성

```
컴퓨트 → 인스턴스 → 인스턴스 생성

이미지   Canonical Ubuntu 24.04          ← Minimal 말고 일반
셰이프   VM.Standard.A1.Flex             ← "상시 무료 적격" 라벨 확인
         1 OCPU · 6GB
네트워크  기존 VCN / 퍼블릭 서브넷 · 퍼블릭 IPv4 주소 지정 ✅
SSH 키   공개키 붙여넣기
```

공개키를 붙여넣을 때 **줄바꿈이 들어가면 안 된다.** 화면에서 줄이 접혀 보여서 그대로 복사하면
깨진다. 이렇게 복사한다:

```bash
tr -d '\n' < ~/Desktop/클라우드\ 키/ssh-key.key.pub | pbcopy
```

> **프라이빗 키는 처음 만들 때 한 번만 받을 수 있다.** 인스턴스를 다시 만들어도
> 이미 받아 둔 키를 계속 쓰면 된다 — 공개키를 붙여넣기만 하면 같은 키로 접속된다.

## ⑤ VCN 보안 목록 — **이거 빼먹으면 접속이 안 된다**

```
네트워킹 → 가상 클라우드 네트워크 → (내 VCN) → 서브넷 → 보안 목록 → 수신 규칙 추가

소스 0.0.0.0/0 · IP 프로토콜 TCP · 대상 포트 80
소스 0.0.0.0/0 · IP 프로토콜 TCP · 대상 포트 443
```

> **지름길:** 인스턴스를 먼저 만들고, 인스턴스 상세 화면의 **서브넷 링크**를 타고 들어가면
> VCN 을 헤맬 필요가 없다. 거기서 바로 보안 목록으로 갈 수 있다.

**오라클은 방화벽이 두 겹이다.**

```
바깥쪽  VCN 보안 목록   ← 콘솔에서만 열 수 있다 (여기)
안쪽    서버 iptables   ← bootstrap.sh 가 연다
```

둘 다 열려야 통한다. 한쪽만 열고 "왜 접속이 안 되지" 로 헤매기 쉬운 곳이다.
**80 을 빼먹으면 HTTPS 인증서 발급부터 실패한다** — Let's Encrypt 가 80 으로 확인한다.

## ⑥ DuckDNS 도메인 (무료)

```
duckdns.org → GitHub/구글 로그인 → 서브도메인 만들기
  예) roomescape-hyune
토큰도 복사해 둔다 (페이지 위쪽에 있다)
```

도메인이 있어야 Caddy 가 HTTPS 인증서를 받는다. 그래야 Floduler(GitHub Pages, https)가
이 서버를 부를 수 있다 — **https 페이지는 http 주소를 못 부른다.**

---

# 터미널 작업

## ⑦ 접속 확인

```bash
chmod 600 ~/Desktop/클라우드\ 키/ssh-key.key      # 권한이 넓으면 ssh 가 거부한다
ssh -i ~/Desktop/클라우드\ 키/ssh-key.key ubuntu@<공인IP>
```

사용자는 `ubuntu` 다 (`opc` 아님 — 그건 오라클 리눅스). 들어가지면 `exit` 하고 나온다.

## ⑧ 배포 — 한 줄

```bash
ssh -i ~/Desktop/클라우드\ 키/ssh-key.key ubuntu@<공인IP> 'bash -s' -- \
  <도메인> <DuckDNS토큰> < deploy/bootstrap.sh
```

예:

```bash
ssh -i ~/key ubuntu@158.180.1.2 'bash -s' -- \
  roomescape-hyune.duckdns.org a1b2c3d4-... < deploy/bootstrap.sh
```

하는 일:

```
도커 설치 → 서버 안쪽 방화벽 → 서버 잠그기 → DuckDNS → 코드 →
.env 생성 → 빌드·기동 → 응답 확인
```

**여러 번 실행해도 안전하다**(멱등). 첫 빌드는 소스에서 컴파일해서 **10분쯤** 걸린다.

> ### ⚠️ 서버에 도착하는 경로가 둘이다. 헷갈리면 사고가 난다
>
> ```
> Kotlin 코드 · compose · Caddyfile · Flyway    서버가 GitHub 에서 git pull   →  푸시해야 반영된다
> deploy/bootstrap.sh                          내 컴퓨터 파일을 stdin 으로   →  푸시 없이 바로 돈다
> ```
>
> 명령 끝의 `< deploy/bootstrap.sh` 는 **내 컴퓨터의 파일**을 읽어 파이프로 보내는 것이다.
> `'bash -s'` 는 "표준입력으로 들어오는 걸 스크립트로 실행해라" 라는 뜻이라,
> 서버는 받아서 실행만 한다 — 저장소에서 꺼내지도, 서버에 파일을 남기지도 않는다.
>
> **그래서 커밋하지 않은 `bootstrap.sh` 수정이 프로덕션에서 돌 수 있다.**
> 2026-08-31 에 실제로 그랬다. 편하지만 위험한 성질이다 —
> **저장소만 봐서는 서버에서 무엇이 돌았는지 알 수 없다.**
> 고쳤으면 **돌린 그 커밋에서 같이 커밋한다.**
>
> **자동 배포는 없다.** CI 도 웹훅도 없어서 `git push` 는 "서버가 가져갈 수 있게 둔 것" 까지다.
> 가져오는 것은 이 명령을 칠 때다.

> **서버 잠그기** 는 셋이다 — SSH 키 전용 · 자동 보안 업데이트(04:00 재부팅) · fail2ban.
> SSH 는 `authorized_keys` 가 있는 걸 확인하고 나서만 잠근다. 확인 없이 끄면 자기 자신을 잠근다.

끝나면 이렇게 나온다:

```
배포 끝났다.
  서버 내부 확인 :  curl -sk https://localhost/api/branches
  바깥에서 확인   :  curl -s https://<도메인>/api/branches
```

## ⑨ 수집 켜기

`bootstrap.sh` 가 만든 `docker-compose.prod.yml` 은 이미 `COLLECTOR_ENABLED=true` 다.
확인만 한다:

```bash
ssh ... 'cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml logs --tail 30 app'
```

`수집 한 바퀴 완료 — …초 (날짜 [...] · 사이트 7곳 · 요청 …건 …)` 이 5분마다 찍히면 정상이다.
**사이트 수는 매장을 추가하면 늘어난다** — 지점 수가 아니라 호스트 수다 (D13).

## ⑩ Floduler 연결

Floduler 저장소(`~/RoomEscapeScheduler`)에서 **두 곳**을 고친다.

```js
// server.js
const DEFAULT_BASE = 'https://<도메인>';

// index.html
const SERVER_DEPLOYED = true;
```

커밋·푸시하면 GitHub Pages 가 알아서 반영한다. **둘 다 고쳐야 한다** —
`SERVER_DEPLOYED` 가 false 면 버튼이 숨겨진다.

---

## ⑪ 푸시 알림 켜기 (선택 · 나중에 해도 된다)

**기본은 꺼져 있다** (`NOTIFY_CHANNEL=log`). 이 상태에서도 취소표 판정은 다 돌고 로그로 남는다.
진짜 푸시를 켤 때만 아래를 한다.

### 콘솔에서 받아올 것 — **두 개**

파이어베이스 콘솔(https://console.firebase.google.com) → `roomescapescheduler` 프로젝트.

| 무엇 | 길 | 어디에 쓰나 |
|---|---|---|
| **서비스 계정 키** (JSON 파일) | ⚙️ 프로젝트 설정 → **서비스 계정** → `새 비공개 키 생성` | **서버.** 이게 있어야 발송한다 |
| **웹 푸시 인증서** (긴 문자열) | ⚙️ 프로젝트 설정 → **클라우드 메시징** → 웹 구성 → 키 쌍 생성 | **브라우저.** 이게 없으면 토큰을 못 만든다 |

> **왜 둘 다인가.** 서버 키는 "내가 이 프로젝트의 주인이다" 를 구글에 증명하는 것이고,
> VAPID 공개키는 브라우저가 "이 사이트가 나에게 푸시를 보내도 좋다" 는 표를 만들 때 쓴다.
> 서버 키만 있으면 **보낼 주소가 없고**, VAPID 만 있으면 **주소는 있는데 보낼 사람이 없다.**

### 서버에 올리기

```bash
# ① 키 파일을 서버로. **이 JSON 은 비밀번호와 같다** — 아무나 우리 프로젝트로 푸시를 쏠 수 있다
scp -i ~/.ssh/oracle.key ~/Downloads/roomescapescheduler-*.json \
    ubuntu@<서버IP>:~/roomescape/firebase-service-account.json

ssh -i ~/.ssh/oracle.key ubuntu@<서버IP>
cd ~/roomescape
chmod 600 firebase-service-account.json     # 나만 읽게

# ② .env 에 두 줄 추가
echo 'NOTIFY_CHANNEL=fcm' >> .env
echo 'FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json' >> .env

# ③ 다시 띄운다
sudo docker compose -f docker-compose.prod.yml up -d
sudo docker compose -f docker-compose.prod.yml logs --tail 20 app
```

**키가 잘못되면 앱이 아예 안 뜬다.** 일부러 그렇게 만들었다 — 늦게 알아채면 서버는 멀쩡히 떠 있고
몇 주 뒤 진짜 취소표가 났을 때 그제야 조용히 안 간다. 로그에 이유가 한 줄로 나온다:

```
서비스 계정 키 경로가 비어 있다 — .env 에 FIREBASE_CREDENTIALS_PATH 를 넣을 것
서비스 계정 키가 파일이 아니다: … (도커가 없는 경로를 마운트하면 빈 디렉터리를 만든다)
```

> ⚠️ **두 번째 오류가 나오는 이유.** 도커는 없는 경로를 마운트하면 **빈 디렉터리를 만들어 버린다.**
> 키를 올리기 전에 `FIREBASE_CREDENTIALS_PATH` 를 먼저 적고 띄웠다면 그 이름의 디렉터리가 생겼을 수 있다.
> `rm -rf firebase-service-account.json` 으로 지우고 진짜 파일을 다시 올린다.

### 되돌리기 (알림만)

```bash
sed -i '/^NOTIFY_CHANNEL=fcm$/d' .env
sudo docker compose -f docker-compose.prod.yml up -d
```

로그 발송으로 돌아간다. 수집·조회는 영향을 받지 않는다.

### 되돌리기 (코드 전체)

배포한 코드가 서 있질 못할 때. **`.env` 와 DB 는 건드리지 않는다.**

```bash
cd ~/roomescape
git log --oneline -5                 # 돌아갈 커밋을 고른다
git reset --hard <직전커밋>
sudo docker compose -f docker-compose.prod.yml up -d --build
```

⚠️ **Flyway 마이그레이션은 되돌아가지 않는다.** 스키마는 앞으로만 간다 —
옛 코드가 새 테이블을 모를 뿐이면 괜찮지만(추가는 안전),
컬럼을 **바꾸는** 마이그레이션을 넣은 뒤라면 코드만 되돌려서는 안 맞는다.
그래서 마이그레이션은 **더하기만** 한다.

### 아직 알림이 안 오는 게 정상인 시점

서버를 켜도 **브라우저가 토큰을 올려 주기 전까지는 보낼 주소가 없다.**
그때 로그는 이렇게 나온다 — 실패가 아니라 정상이다:

```
알림 판정 — 발송 0건, 쿨다운으로 보류 0건, 실패 0건, 받을 기기 없음 1건
```

Floduler 쪽에 서비스 워커와 `getToken` 을 붙여야 종단이 이어진다 (작업명세서 F-16).

---

## 확인

```bash
curl -s https://<도메인>/api/branches | head -c 300      # 200 · 35지점 (2026-08-31)
curl -s https://<도메인>/debug/collect                    # 404 여야 한다 (Caddy 가 막는다)
curl -s https://<도메인>/                                 # 404 여야 한다
```

그리고 **한 바퀴를 기다려 로그를 본다.** 200 만으로는 새 매장이 실제로 긁히는지 알 수 없다.

```
수집 한 바퀴 완료 — 162초 (… · 사이트 5곳 · 요청 172건 · 동시 5), 전이 5건, 격리 0건, 실패 0건
                                  ↑ 매장 수만큼          ↑ 이 값이 줄면 어댑터가 안 뜬 것이다
```

**실측 (2026-08-31 · 오라클 도쿄)** — 사이트 5곳 · 4일치 · 172요청 · 162초.
⚠️ **초 수는 바퀴마다 다르다.** 날짜 수가 요일 등급에 따라 3~4일치로 바뀌기 때문이다 —
`사이트 N곳` 과 `실패` 를 보고, 초는 두 배쯤 튀지 않는지만 본다. 지구별의 퓨니코드 호스트
`www.xn--2e0b040a4xj.com` 이 오라클에서도 이름 해석된다는 확인을 여기서 한다.

> **`실패 N건` 이 0이 아니어도 정상일 수 있다.** 키이스케이프가
> `예약가능시간이 아닙니다. 예약오픈시간 : 11:30` 으로 거절하는 시간대가 있다 —
> 오픈 시각 전에 그 날짜를 물어서다. 지나면 저절로 사라진다.
> **문구를 보고 판단한다. 숫자만 보고 놀라지 않는다.**

### 이름 바뀐 것이 안 보일 때 — 고장이 아니다

매장 이름(`brand`)은 두 API 가 **서로 다른 시점에** 바뀐다 ([아키텍처.md](아키텍처.md) D19).

```
/api/branches   재기동 즉시        어댑터 enum 을 읽는다
/api/schedule   다음 한 바퀴 뒤     DB 의 store.brand 를 읽는다 (최대 5분)
```

올리자마자 둘을 비교하면 다르게 보인다. **그 사이에 멀쩡한 것을 건드리지 않는다.**

그리고 브라우저로 `https://choieuihyun.github.io/RoomEscapeScheduler/` 에서
**`회차 불러오기`** 버튼이 보이고 눌리면 끝이다. (콘솔에 `flod.server` 를 넣을 필요가 없어진다)

---

## 막혔을 때

| 증상 | 원인과 대처 |
|---|---|
| `Out of host capacity` | A1 자리가 없다. **PAYG 면 대부분 뚫린다.** 그래도 안 되면 몇 시간 뒤 재시도. 도쿄는 AD 가 하나뿐이라 "다른 AD" 선택지가 없다 |
| `Too many requests for the user` | 너무 빨리 여러 번 눌렀다. **몇 분 쉬고** 다시 |
| SSH 가 안 붙는다 | 키 권한(`chmod 600`) · 사용자명(`ubuntu`) · 공인 IP 확인. 공개키에 줄바꿈이 들어갔을 수도 있다 |
| SSH 는 되는데 `https://도메인` 이 안 열린다 | **VCN 보안 목록(⑤)** 을 안 열었을 가능성이 제일 크다. `bootstrap.sh` 는 안쪽만 연다 |
| 인증서 발급 실패 | **80 포트**가 막혀 있다. 443 만 열면 안 된다 |
| Pages 에서 버튼이 안 보인다 | `SERVER_DEPLOYED = true` 를 안 고쳤다 |
| 앱이 안 뜬다 | `sudo docker compose -f docker-compose.prod.yml logs app` 를 본다 |

---

## 실제로 겪은 것 (2026-08-28)

**전부 갓 만든 기계에서만 나는 종류였다.** 로컬에서는 절대 안 나온다 —
이 스크립트가 존재하는 이유가 바로 그 상황인데 거기서 터졌다. 셋 다 고쳐 뒀다.

| 증상 | 원인 |
|---|---|
| DuckDNS 까지 성공하고 **말없이 종료** | 크론탭이 아예 없는 기계는 `crontab -l` 이 1 로 끝난다. `set -euo pipefail` 이 그걸 실패로 보고 스크립트를 죽였다 |
| **`✗ 앱이 응답하지 않는다`** 인데 앱은 멀쩡 | 확인을 `https://localhost` 로 했다. Caddy 는 `{$DOMAIN}` 블록만 있어서 `Host: localhost` 에는 응답하지 않는다 |
| 래빗홀 **전 회차가 매진**으로 들어옴 | `<label>예약가능</label>` 이 실제로 있었다. 매진 표본만 보고 "불가일 때만 label 이 붙는다" 고 잘못 봤다 |

세 번째는 **배포한 서버가 스스로 잡아냈다.** 로그에 이렇게 찍혔다:

```
WARN  모르는 예약 상태 문구 — "예약가능". 불가로 처리한다. 파서 확인 필요
```

모르는 값을 "불가" 로 두고 경고를 남기는 설계가 값을 한 자리다.
반대로 짰다면 감시 걸어둔 사람 전원에게 헛알림이 조용히 나갔을 것이다.

### 배포 직후 상태

```
서버   오라클 도쿄 · A1.Flex · 1 OCPU · 6GB · Ubuntu 24.04 (aarch64) · 디스크 42GB
수집   5분마다 · 111요청 122초 · 3사이트 동시 · 실패 0건
확인   /api/branches 200 · /debug/* 404 · / 404 · Let's Encrypt 인증서
CORS   Pages 출처만 통과, 그 외 403
```

---

## 나중에 옮길 때

우리 스택은 통째로 컨테이너라 **이사 비용이 거의 0** 이다.

```bash
# 옛 서버에서
sudo docker compose -f docker-compose.prod.yml exec -T db \
  pg_dump -U roomescape roomescape > dump.sql

# 새 서버에서
ssh <새서버> 'bash -s' -- <도메인> <토큰> < deploy/bootstrap.sh
cat dump.sql | ssh <새서버> 'cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml exec -T db psql -U roomescape roomescape'
```

DuckDNS 도메인만 새 IP 로 바꾸면 Floduler 쪽은 손댈 게 없다.

---

## 운영 중 자주 쓰는 것

```bash
# 로그
cd ~/roomescape && sudo docker compose -f docker-compose.prod.yml logs -f app

# 취소표만 보기
… logs app | grep 🎟️

# 코드 갱신 후 재배포 (멱등이라 그냥 다시 돌리면 된다)
ssh <서버> 'bash -s' -- <도메인> <토큰> < deploy/bootstrap.sh

# 상태
sudo docker compose -f docker-compose.prod.yml ps
```

> `.env` 는 **덮어쓰지 않는다.** DB 비밀번호가 거기 있고, 바뀌면 기존 DB 를 못 연다.
