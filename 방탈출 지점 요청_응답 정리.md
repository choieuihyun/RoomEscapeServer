# 방탈출 지점 요청·응답 정리

매장을 추가할 때마다 **여기에 먼저 적는다.** 어댑터 코드보다 이 문서가 앞선다 —
사이트가 바뀌면 코드는 조용히 오작동하지만, 여기 적힌 "예전에는 이랬다" 는 남는다.

> 매장별 어댑터는 [아키텍처.md](아키텍처.md) ①~③ 계층에 있다.
> 수집 원칙은 [CLAUDE.md](CLAUDE.md) "수집 윤리" 를 따른다.

---

## 매장을 추가하기 전에 — 확인 순서

**이 넷을 통과하지 못하면 대상에서 뺀다.** 기술적으로 가능한 것과 해도 되는 것은 다르다.

| | 무엇을 | 통과 기준 |
|---|---|---|
| 1 | `robots.txt` | 우리가 긁을 경로가 `Disallow` 가 아닐 것 |
| 2 | 이용약관·개인정보처리방침 | 자동 수집 금지 조항이 없을 것 |
| 3 | 응답 헤더 | Cloudflare·WAF·캡차가 없을 것. **있으면 우회하지 않고 뺀다** |
| 4 | 요청 1회 실측 | 인증 없이 200 이 오고, 우리가 원하는 값이 응답에 있을 것 |

확인 결과를 매장 섹션 맨 위에 날짜와 함께 적는다.

---

## 한눈에 보기

| 매장 | 호스트 | 형식 | 요청 단위 | 지점 | 테마 | 한 바퀴(7일) |
|---|---|---|---|---|---|---|
| 플레이33 ✅ | `play33.kr` | HTML | **지점 × 날짜** | 4 | 9 | 28요청 · 34초 |
| 키이스케이프 ✅ | `www.keyescape.com` | JSON | **테마 × 날짜** | 11 | 32 | 224요청 · 4.5분 |
| 래빗홀 ✅ | `www.rabbitholeescape.co.kr` | HTML | 지점 × 날짜 | 1 | 2 | 7요청 · 9초 |
| 지구별 🔍 | `xn--2e0b040a4xj.com` | HTML | 날짜 | 1 | 9 | 7요청 · 9초 |
| 포인트나인 🔍 | `point-nine.com` | HTML | 날짜 | 1 | 4 | 7요청 · 9초 |
| 비트포비아 🔍 | `xdungeon.net` | HTML | **지점 × 날짜** | 9 | ? | 63요청 · 76초 |
| 제로월드 🔍 | `zeroworldkorea.com` | HTML조각 | **테마 × 날짜** | A 3 · B ? | 11+ | **270요청 · 5.4분** ⚠️ |
| 오아시스 ⚠️ | `oasismuseum.com` | JSON | 테마 × 날짜 | 1 | 4~5 | 35요청 · 42초 |
| 티켓투이스케이프 ❌ | `www.ttescape.co.kr` | JS 위젯 | **미확인** | ? | ? | — |

`🔍` = 조사 끝, 어댑터 미작성 · `⚠️` = 조사 중 막힌 곳 있음 · `❌` = 방법 못 찾음

> **요청 단위가 한 바퀴 시간을 정한다.** 지점 수가 아니다.
> 플레이33은 지점 하나를 물으면 그 지점 테마가 전부 오지만,
> 키이스케이프는 테마마다 따로 물어야 해서 **테마 수만큼 곱해진다.**

> **같은 호스트로 가는 요청은 한 줄로 선다** ([아키텍처.md](아키텍처.md) D13).
> 지점이 11개라도 서버가 하나면 동시에 못 보낸다.

> **위 "한 바퀴(7일)" 은 전량 기준이다.** 계층 폴링(D14)을 넣으면 실제 한 바퀴는
> 매번 7일치가 아니라 평균 3.5일치라 절반쯤 된다 — 키이스케이프 4.5분 → 2.2분.
> 사이즈를 가늠할 때는 이 표(전량)를 보고, 실제 주기를 볼 때는 D14 를 본다.

---

# 플레이33

```
확인일   2026-08-27
robots   User-agent: *  /  Disallow:        ← 값이 비면 "제한 없음" 이다
약관     자동수집 금지 조항 확인 안 됨
보호     없음 (Apache)
```

## 요청

```http
GET https://play33.kr/reservation?branch={id}&date={YYYY-MM-DD}
User-Agent:              ← 빈 값. 헤더를 빼면 자바 기본값이 대신 나간다
```

쿠키·토큰 없이 `GET` 하나로 응답한다. **`theme` 파라미터는 넘기지 않는다** —
생략하면 그 지점 테마가 전부 함께 온다.

### 지점

| `branch` | 이름 | `store_key` |
|---|---|---|
| `1` | 건대점 | `play33-konkuk` |
| `4` | 홍대점 | `play33-hongdae` |
| `5` | 대전점 | `play33-daejeon` |
| `7` | 수원점 | `play33-suwon` |

> `6` 은 "테스트" 지점이라 제외한다.
> 수원점은 2026-08-26 기준 테마가 하나도 없다 — 지점이 빈 것과 파서가 깨진 것을 구분하려고 경고만 남긴다.

## 응답 — HTML

| 우리가 쓰는 값 | 어디서 | 비고 |
|---|---|---|
| **예약 가능 여부** | `.restimes li button` 의 `disabled` 속성 유무 | 버튼 **문구**보다 바뀔 가능성이 낮다 |
| 회차 시각 | 같은 버튼의 `span` 텍스트 | |
| 렌더된 날짜 | `input[name=date].mask-date` 의 `value` | **검증용.** 아래 함정 참고 |
| 예약 오픈 범위 | `#reservation_range_day` 의 `value` | 7 |
| 테마 고유 ID | `select[name=theme] option` 의 `value` | 이름이 바뀌어도 이어붙이는 키 |
| 테마명 | `.reslist-text > strong` | |
| 장르·인원·시간·공포 | `.reslist-text table tr` 의 `th`/`td` | |
| 난이도 | `.resstep` 의 `sizeN` 클래스 | **두 자리는 반 칸** — 아래 참고 |
| 포스터 | `.reslist-img img` 의 `src` | |

## ⚠️ 함정

**범위 밖 날짜는 302 로 홈에 보낸다.**
그대로 파싱하면 "그 날 회차가 전부 사라졌다" 로 읽혀서, 저장 단계가 멀쩡한 데이터를 지운다.
그래서 **요청한 날짜와 렌더된 날짜가 같은지 검증하고, 다르면 예외로 끊는다**
([아키텍처.md](아키텍처.md) D2).

**공포·난이도는 0.5 단위다.**

```
공포   "3.5"   숫자만 남기면 → 35
공포   "0.5"   숫자만 남기면 →  5    ← "거의 안 무섭다" 가 "최고 공포" 로 뒤집힌다
난이도 size25  정수로 읽으면 → 25    ← 별이 25 개 찍힌다
```

`reservation.css` 가 `size1 size15 size2 size25 … size45` 를 각각 다르게 그린다.
**두 자리면 반 칸이고 `size25` 는 2.5 다** ([아키텍처.md](아키텍처.md) D10).

---

# 키이스케이프

```
확인일   2026-08-27
robots   User-agent: *  /  Allow: /        ← 전 경로 허용
약관     개인정보처리방침에 자동수집 금지 조항 없음
보호     없음 (nginx / PHP 7.4). Cloudflare·WAF·캡차 아님
```

> 예약 페이지를 브라우저 개발자도구로 열면 **"개발자 도구 사용이 금지되어 있습니다"** 가 뜬다.
> `devtools-detector` 라이브러리가 **브라우저 안에서** 도는 것이라 서버 요청과는 무관하고,
> 우리 크롤러는 그 JS 를 실행하지 않는다. **우회하지 않는다 — 우회할 일이 없다.**
> 원본 응답은 `curl` 로 보면 되고, 그게 크롤러가 하는 일 그대로다.

## 요청 — 엔드포인트 하나에 `t` 로 구분

```http
POST https://www.keyescape.com/controller/run_proc.php
Content-Type: application/x-www-form-urlencoded
User-Agent:              ← 빈 값
```

| `t` | 파라미터 | 돌려주는 것 |
|---|---|---|
| `get_theme_info_list` | `zizum_num` | 지점의 테마 목록 |
| `get_theme_date` | `num` = **`info_num`** | 테마 상세 (이름·장르·난이도·소요시간·포스터) |
| `get_theme_time` | `date`, `zizumNum`, `themeNum` | **회차 + 예약 가능 여부** |
| `get_caledar_data` | `year`, `month` | 달력 (오타는 사이트 원본 그대로). **미확인** |

### 지점과 테마

| `zizum_num` | 지점 | 테마 (`theme_num`) |
|---|---|---|
| `26` | 에버랜드 | `78` Memory of Poppy · `79` Memory of Tony · `80` Memory of Ricky |
| `23` | 후즈데어 | `71` AYAKO · `69` 투투 어드벤쳐 · `70` 괴록 |
| `22` | STATION | `65` 머니머니부동산 · `66` 내 방 · `67` NOSTALGIA VISTA |
| `19` | LOG_IN 1 | `63` FOR FREE · `60` 머니머니패키지 |
| `20` | LOG_IN 2 | `95` NOT MONKEY PROJECT · `64` A GENTLE MONDAY · `61` BACK TO THE SCENE+ |
| `18` | 메모리컴퍼니 | `57` FILM BY EDDY · `58` FILM BY STEVE · `59` FILM BY BOB |
| `16` | 우주라이크 | `56` WANNA GO HOME · `55` US |
| `14` | 더오름 | `51` 엔제리오 · `48` 네드 |
| `3` | 강남점 | `7` 그카지말라캤자나 · `6` 살랑살랑연구소 · `5` 월야애담 |
| `10` | 홍대점 | `41` 삐릿-뽀 · `45` 홀리데이 · `43` 고백 |
| `9` | 부산점 | `35` 난쟁이의 장난 · `36` 신비의숲 고대마법의 비밀 · `39` 셜록 죽음의 전화 · `37` 정신병동 · `38` 파파라치 |

**총 11지점 · 32테마** (2026-08-27 기준). `movie_mood` 카테고리는 방탈출이 아니라 제외한다.

## 응답 — JSON

### `get_theme_time` — 회차

```json
{"status":true,"msg":"","data":[
  {"num":34,"gubun":"B","theme_num":7,"hh":"13","mm":"20","enable":"Y",
   "sale_ck":"N","sale_price":0,"sale_doc":"6층","sale_one":"N","sale_txt":""}
]}
```

| 값 | 뜻 |
|---|---|
| **`enable`** | **`Y` = 예약 가능, `N` = 불가.** 사이트 자체 코드가 `item.enable === 'N' → disabled` 로 쓴다 |
| `hh` / `mm` | 회차 시각. **문자열이다** (`"08"` 처럼 0 이 붙는다) |
| `num` | 회차 행 ID |
| `sale_doc` | 층수 등 안내 문구 |

> 2026-08-27 실측: 98회차 중 **31개가 `N`** — 살아 있는 신호다.

### `get_theme_date` — 테마 상세

```json
{"num":21,"name":"삐릿-뽀","genre":"어드벤처","level":"4","play_time":"75분",
 "image_url":"https://…/삐릿뽀__sizedown.png","doing":14,"seq":33,"memo":"…"}
```

| 값 | 우리 쪽 |
|---|---|
| `name` | 테마명 |
| `genre` | 장르 |
| `level` | 난이도 |
| `play_time` | 소요시간 (`"75분"` — **단위가 붙어 있다**) |
| `image_url` | 포스터 |
| `doing` / `seq` / `gubun` | **뜻 미확인.** 소요시간이 아니다 (1·7·14 같은 값) |

## ⚠️ 함정

**JSON 을 `text/html` 로 보낸다.**

```
Content-Type: text/html;charset=UTF-8      ← 내용은 JSON 인데
```

스프링이 "이 타입을 읽을 변환기가 없다" 며 거절한다. **문자열로 받아서 직접 파싱한다.**

**`status:false` 가 항상 실패는 아니다.**

```json
{"status":false,"msg":"예약 가능 한 날짜가 아닙니다.","data":null}
```

에버랜드는 2026-08-27 기준 7일 내내 이 응답이다. 이건 **"그 날짜엔 회차가 없다"** 는 답이지
조회 실패가 아니다. 실패로 세면 한 바퀴마다 9건씩 쌓여서 **진짜 고장이 묻힌다.**

알려진 문구 (늘어난다):

```
예약 가능 한 날짜가 아닙니다     아직 안 연 날짜
예약가능한 시간이 없습니다       그 날 남은 회차가 없음 (오늘 날짜에서 나온다)
```

→ 아는 문구면 회차 0개로 보고, **모르는 거절은 예외로 끊는다.**

> **두 번째 문구는 이 설계 덕분에 드러났다.** 첫 수집에서 실패 3건으로 튀어나왔고,
> 모든 거절을 조용히 0개로 뭉갰다면 못 봤을 것이다. 문구가 또 늘어도 같은 방식으로 보인다.

**ID 공간이 둘인데 둘 다 작은 정수다.**

```
info_num  = 21   삐릿-뽀      ← get_theme_date 가 받는 값
theme_num = 41   삐릿-뽀      ← get_theme_time 이 받는 값
```

`get_theme_date&num=41` 을 보내면 **에러가 아니라 엉뚱한 테마(FOR FREE)가 그럴듯하게 온다.**
플레이33의 302 와 같은 종류다 — **틀린 요청이 실패로 보이지 않는다.**

→ 두 값을 반드시 `get_theme_info_list` 응답에서 함께 가져와 짝으로 들고 다니고,
   **회차 응답의 `theme_num` 이 요청한 값과 같은지 검증한다.** 다르면 예외로 끊는다.

**요청 수가 테마 수에 비례한다.**

```
32테마 × 7일 = 224요청 × 1.2초 = 4.5분/바퀴
```

11지점이 전부 같은 호스트라 병렬로 못 줄인다. 계층 폴링이 필요한 이유다.

---

# 래빗홀 (`www.rabbitholeescape.co.kr`) — 2026-08-27 확인

```
robots   User-agent: *  /  Disallow:      ← 제한 없음
보호     없음 (Apache / Laravel)
구조     GET /reservation?branch=1&date=YYYY-MM-DD   ← 플레이33과 같은 관례
규모     1지점(홍대) · 2테마 → 7요청/바퀴
```

**같은 예약 솔루션으로 보인다.** URL 관례가 같고, 별점 클래스가 `res-item-step size2 cba` 로
플레이33의 `resstep size2 cba` 와 **`sizeN cba` 관례까지 똑같다.** 클래스명만 바뀐 것 같다.
`input[name=date]` 로 렌더된 날짜를 검증하는 D2 수법도 그대로 쓸 수 있다.

### 응답 — HTML

| 우리가 쓰는 값 | 어디서 |
|---|---|
| **예약 가능 여부** | `ul.res-times li button` 안의 `<label>` **텍스트**. `예약불가` 면 불가 |
| 회차 시각 | 같은 버튼의 `<span>` |
| 렌더된 날짜 | `input[name=date]` 의 `value` — **검증용** |
| 테마 목록 | `select[name=theme] option` — `5` 행운만물상 · `4` 두껍아 두껍아 |
| 장르·인원·시간·난이도 | 정보 테이블 (`70min` — 단위 표기가 플레이33과 다르다) |
| 난이도 | `res-item-step` 의 `sizeN` — 플레이33과 같은 규칙이면 **두 자리는 반 칸** |

### 예약 여부는 `<label>` **문구**다

```html
<button><label>예약불가</label><span>11:20</span></button>
<button><label>예약가능</label><span>13:05</span></button>
```

**처음엔 "불가일 때만 label 이 붙는다" 고 봤는데 틀렸다.** 매진 상태 표본만 보고 판단해서다.
배포한 서버가 `모르는 예약 상태 문구 — "예약가능"` 경고를 뱉어서 알았고,
그때까지 래빗홀 전 회차를 매진으로 읽고 있었다.

```
예약가능  → 가능
예약불가  → 불가
label 없음 → 가능      (지금은 안 나온다)
그 외      → 불가 + 경고
```

**모르는 문구를 "불가" 로 두는 게 핵심이다.** 잘못 "가능" 으로 읽으면 감시 걸어둔 사람
전원에게 헛알림이 나가는데, 반대로 틀리면 알림이 안 갈 뿐이다. 그리고 경고 덕분에 드러난다.

> 플레이33은 `disabled` **속성**이라 이 문제가 없다. 같은 솔루션인데 이 부분만 다르다.

### 픽스처 두 장

2026-08-27 확인 시점에 8/27·8/28·9/1 **전 회차가 `예약불가`** 였다.
매장이 실제로 매진이었던 것이고(사용자 확인), 평소에는 예약 가능한 회차가 시각으로 뜬다.

**그런데 우리는 "예약 가능" 쪽 마크업을 아직 못 봤다.** 매진 화면만 보고 파서를 쓰면
[D5](아키텍처.md) 가 말한 반쪽 표본 문제가 그대로 반복된다 — 플레이33 픽스처가 정수뿐이라
0.5 단위 버그를 통과시킨 것과 같은 자리다.

```
rabbithole-hongdae-2026-08-29.html   전 회차 매진 (18개)
rabbithole-hongdae-2026-09-02.html   가능 1 · 불가 15      ← 두 상태가 다 있다
```

처음엔 매진 표본만 있어서 판정 규칙을 잘못 잡았다. **반쪽 픽스처는 반쪽만 지킨다.**
매장을 추가할 때 두 상태가 다 든 표본을 구하는 게 그래서 중요하다.

---

# 2026-08-28 조사 — 7곳

**세 계열로 갈린다.** 우연이 아니라 예약 솔루션 업체가 몇 곳뿐이어서다.

| 계열 | 매장 | 표식 |
|---|---|---|
| **신비웹** (`(주)신비웹`) | 포인트나인 · 비트포비아 · 제로월드 | `layout/res/home.php?go=rev.*` · `<meta name="publisher">` 에 신비웹 |
| **래빗홀 계열** (Laravel) | 플레이33 · 래빗홀 · **지구별** | `/reservation?date=` · `eveReservationButton` · `<label>예약가능</label>` |
| 자체 | 오아시스 · 티켓투이스케이프 | — |

> ⚠️ **같은 계열이라고 어댑터를 합치지 않는다.** 신비웹 셋은 **세대가 달라서**
> 마크업도 요청 방식도 다르다 (아래 각 절 참고). 각각 하드코딩한 뒤 공통점이 진짜로
> 남으면 그때 뽑는다 — [작업명세서.md](작업명세서.md) M5 의 방침 그대로다.

---

# 지구별 (`xn--2e0b040a4xj.com`) — 2026-08-28 확인

```
robots   User-agent: *  /  Disallow:        ← 빈 값 = 제한 없음
보호     없음 (Laravel)
구조     GET /reservation?date=YYYY-MM-DD
규모     1지점 · 테마 9개쯤 → 7요청/바퀴
```

**주소가 punycode 다.** `xn--2e0b040a4xj.com` 은 한글 도메인 `지구별.com` 의 실제 이름이다.
코드에는 punycode 를 적는다 — 한글을 그대로 쓰면 자바 HTTP 클라이언트가 변환해 주지 않는다.

## 래빗홀·플레이33 과 **같은 솔루션**이다

```html
<button class="active1 eveReservationButton" type="button">
    <label>예약가능</label> <span class="ff-bhs">17:30</span>
</button>
<button type="button">
    <label>예약불가</label> <span class="ff-bhs">10:40</span>
</button>
```

`eveReservationButton` · `<label>예약가능</label>` · `ff-bhs` 까지 래빗홀과 똑같다.
`input[name=date]` 로 렌더된 날짜를 검증하는 D2 수법도 그대로 쓴다.

**판정은 래빗홀과 같게 `<label>` 문구로 한다** — 이 계열은 매진 버튼에도 `disabled` 가 없어서
구조로는 못 가른다. 모르는 문구는 **불가로 처리하고 경고**를 남긴다
(래빗홀에서 실제로 이 경고가 오독을 잡아냈다).

## ⚠️ 함정

- **`branch` 파라미터가 응답을 안 바꾼다.** `?branch=1&date=…` 와 `?date=…` 의 응답이
  1바이트 차이다. 지점이 하나라서인지 파라미터가 무시되는 건지 아직 모른다 —
  **지점이 늘면 여기부터 다시 본다.**
- 한 페이지에 그날 전 테마가 다 온다 (가능 29 · 불가 36). 요청 단위는 **날짜 하나**다.

---

# 포인트나인 (`point-nine.com`) — 2026-08-28 확인

```
robots   404 — 그런데 응답이 alert('Firewall Alert(404)')      ← 방화벽 제품(nesolution)
약관     자동수집 금지 조항 확인 안 됨
보호     WAF 있음. 단, 예약 페이지는 빈 UA 로 정상 200
구조     GET /layout/res/home.php?go=rev.make&rev_days=YYYY-MM-DD
규모     1지점 · 4테마 → 7요청/바퀴
```

## ⚠️ 함정 — WAF 가 있다

`robots.txt` 를 부르면 **방화벽이 404 를 만들어 돌려준다.** 즉 이 사이트에는 robots.txt 가
없는 것이고, 우리가 차단당한 게 아니다 — 예약 페이지는 빈 UA 로 그냥 200 이 온다.

> **그래도 이 매장은 다른 곳보다 조심한다.** 방화벽이 붙어 있다는 건 비정상 트래픽을
> 보고 있다는 뜻이다. **우회는 하지 않는다** — 차단당하면 그때 대상에서 뺀다.
> 지금 정책(초당 1회 미만, 빈 UA)을 그대로 지키고 더 빠르게 만들지 않는다.

## 응답 — HTML

```html
<li><a class="end">
      <span class="time">11:00 </span><span class="impossible">예약마감</span></a></li>
<li><a href="home.php?go=rev.make.input&rev_days=2026-08-28&theme_time_num=1269">
      <span class="time">15:30 </span><span class="possible">예약가능</span></a></li>
```

## 예약 여부는 **`href` 존재**로 본다 (문구 아님)

예약 폼으로 가는 링크는 **실제로 예약이 될 때만 생긴다.** `class="end"` 도 같이 보지만
근거는 href 다 — [아키텍처.md](아키텍처.md) D1 의 `disabled` 판정과 같은 성격이다.

```
✗  <span> 문구가 "예약마감" 이면 불가
✓  <a> 에 href 가 없으면 불가
```

문구로 보면 사이트가 "마감" 으로만 바꿔도 **전 회차가 가능으로 읽혀 헛알림이 쏟아진다.**
`theme_time_num` 이 회차 고유번호라 안정적인 키로 쓸 수 있다.

## ⚠️ 함정 — 범위 밖 날짜가 **302 가 아니라 200** 이다

| 날짜 | 응답 |
|---|---|
| 2026-08-30 (2일 뒤) | 200 · 16,634바이트 · 회차 32개 |
| **2026-09-05 (8일 뒤)** | **200** · 7,751바이트 · **회차 0개** |

플레이33은 범위 밖이면 302 라서 쉽게 걸렀는데(D2), **여기는 200에 빈 내용이 온다.**
그대로 저장하면 **그날 회차가 전부 사라진 것으로 기록되고, 다음 바퀴에 되살아나면
전 회차가 "자리 남" 으로 읽혀 헛알림이 대량으로 나간다.**

→ 응답에서 `rev_days` 를 되읽어 **요청한 날짜와 같은지 확인하고, 회차가 0개면 저장하지 않는다.**
예약 오픈 범위는 7일로 **추정**(8/28 기준 9/5 가 빔). 정확한 경계는 아직 미확인.

---

# 비트포비아 (`xdungeon.net`) — 2026-08-28 확인

```
robots   User-agent:*  /  Allow:/
보호     없음
구조     GET /layout/res/home.php?go=rev.main&s_zizum=N&rev_days=YYYY-MM-DD
규모     9지점 → 63요청/바퀴 (76초)
```

## 지점 (9곳 · 전부 넣기로 함)

| `s_zizum` | 이름 |
|---|---|
| 1 | 던전101 |
| 2 | 강남던전 |
| 3 | 홍대던전 |
| 4 | 강남던전Ⅱ |
| 5 | 홍대던전Ⅲ |
| 6 | 던전루나(강남) |
| 7 | **서면던전(부산)** |
| 9 | 던전스텔라(강남) |
| 10 | **서면던전 레드(부산)** |

> `8` 이 비어 있다. 없어진 지점으로 보인다 — **연속 번호로 가정하고 순회하면 안 된다.**
> 목록을 하드코딩한다.

## 응답 — 신비웹인데 **포인트나인과 마크업이 다르다**

```html
<li class="dead sale"><a>09:55</a></li>                       ← 매진 (href 없음)
<li class="sale"><a href="home.php?go=rev.make&crypt_data=UUtUYVFB…">  ← 가능
```

같은 업체 제품인데 **세대가 다르다.** 그래서 파서를 공유하지 않는다.

## ⚠️ 함정 — 회차 고유번호가 없다

포인트나인의 `theme_time_num` 에 해당하는 값이 **URL 에 없다.** 대신 암호화된 `crypt_data`
한 덩어리라 안에 뭐가 들었는지 알 수 없고, **매번 값이 달라질 수도 있다.**

→ 자연키는 우리 스키마 그대로 **`(테마, 날짜, 시각)`** 을 쓴다. `crypt_data` 는 저장하지 않는다.
테마 id 는 `javascript:_fun_theme_view('1')` 의 숫자를 쓴다.

> 나중에 이 파일을 읽는 사람이 "URL 에 회차 id 가 있겠지" 하고 찾다가 못 찾을 자리라 적어 둔다.

---

# 제로월드 · 제로월드 다이브 (`zeroworldkorea.com`) — 2026-08-28 확인

```
robots   User-agent:*  /  Allow:/
보호     없음
구조     POST /core/res/rev.make.sel.php   (AJAX)
규모     s_subj=A 3지점 · 지점당 테마 11개 → 지점×날짜×(1+11) 요청
```

**`s_subj` 가 브랜드를 가른다** — `A` = 제로월드, `B` = 제로월드 다이브.
**같은 호스트다.** 따라서 둘을 따로 세어도 요청은 한 줄로 선다 (D13).

## 요청 — 두 단계

```
POST /core/res/rev.make.sel.php
  act=theme_list        zizum_num=1&rev_days=2026-08-29&s_subj=A     → 그 지점의 테마 목록 (11개)
  act=theme_time_list   …&theme_num=14                                → **테마 하나**의 회차
```

`act` 는 다섯 개(`calendar` `theme_list` `theme_select` `theme_time_list` `theme_time_select`)지만
수집에 필요한 건 위 둘이다.

## 응답 — HTML 조각 + 끝에 집계

```html
<a class="choice-time__time disable">10:30</a>
<a class="choice-time__time" href="javascript:fun_theme_time_select('144','1')">11:35</a>
… {@}1/11 가능
```

- `class="disable"` = 매진, `href` 있음 = 가능 → **판정은 href 로**
- 끝의 **`{@}1/11 가능`** 은 사이트가 스스로 센 **가능 1개 / 전체 11개**다

> **이 집계를 검증에 쓴다.** 우리가 파싱한 개수와 `{@}n/m` 이 다르면 파서가 틀린 것이다.
> D2("검증을 통과해야 저장한다")를 공짜로 하나 더 얻는 셈이라 반드시 대조한다.

## ⚠️ 함정 — 여기가 제일 비싸다

```
지점 3 × 날짜 7 × (테마목록 1 + 테마 11) = 252요청 × 1.2초 = 5.0분   ← 5분 주기를 넘긴다
계층 폴링(D14) 적용 시 날짜 3.5 → 126요청 · 2.5분                    ← 겨우 들어간다
s_subj=B 까지 더하면 다시 넘길 수 있다                                ← **확인 필요**
```

**병렬로 못 푼다** — 같은 호스트라 한 줄로 서야 한다([작업명세서.md](작업명세서.md) 의 남은 병목과 같은 모양).
`theme_time_list` 가 테마마다 하나라서 **테마 수만큼 곱해지는 것**이 원인이고,
키이스케이프와 같은 성질이다.

→ 어댑터를 만들 때 **s_subj=B 의 지점·테마 수를 먼저 세고**, 한 바퀴가 주기를 넘기면
이 매장만 날짜를 줄이거나 등급을 한 칸 낮춘다. 어댑터를 다 만든 뒤에 재는 게 아니라 **먼저 센다.**

## 아직 모르는 것

- `s_subj=B` 의 지점 목록과 테마 수 — **미확인**
- 지점 이름 (`branch` 페이지에 `zizum_num` 이 안 나왔다. 다른 경로를 찾아야 한다)
- 예약 오픈 범위 (며칠치까지 여는지)

---

# 오아시스 뮤지엄 (`oasismuseum.com`) — 2026-08-28 확인 · **막힘**

```
robots   404 (없음) — 제한 없음
보호     없음 (CodeIgniter)
구조     POST /ticket/getSchedule   {tm, date}
```

## 요청과 응답

```
POST /ticket/getSchedule   tm=1&date=2026-08-29
→ ["4","2","3","1","6","5","7","8"]
```

**응답이 "예약 가능한 것들의 목록" 이다.** 회차 시각은 안 들어 있고, 시각은 페이지 HTML 쪽에 있다:

```html
<button id="sd_btn1_56" data-tm="1" data-time="09:50" value="56" class="room_btn1 btn-closed">
```

즉 **페이지(시간표) + API(그날 열린 것)** 를 맞춰야 한다.

## ⚠️ 막힌 곳 — 번호가 안 맞는다

페이지 버튼의 `value` 는 `56`, `57` … 인데 API 응답은 `1`~`8` 이다. **둘이 안 맞는다.**
셋 중 하나인데 아직 못 가렸다:

1. 응답이 `value` 가 아니라 **순번**이다
2. 내가 넣은 날짜(2026-08-29)가 범위 밖이라 **엉뚱한 기본값**이 왔다
3. `tm` 이 테마가 아니라 다른 것이다

> **이게 정확히 이 문서가 존재하는 이유인 "틀린 요청이 그럴듯한 응답으로 오는" 경우다.**
> 200 에 JSON 배열까지 왔는데 의미가 다르면, 코드는 조용히 틀린 값을 저장한다.
> **가리기 전에는 어댑터를 쓰지 않는다.**

→ 다음에 할 일: 오늘 날짜로 다시 부르고, 브라우저에서 실제로 눌러 보며 대조한다.

## ⚠️ 함정 — 빈 배열이 "실패" 가 아니다

`[]` 는 **그날 전 회차 매진**이라는 정상 응답일 수 있다.
[CLAUDE.md](CLAUDE.md) 의 "조회 실패와 결과 없음을 같은 값으로 표현하지 않는다" 가 그대로 걸린다.

---

# 티켓투이스케이프 (`www.ttescape.co.kr`) — 2026-08-28 · **방법 못 찾음**

```
robots   Allow:/ · 금지: /login /site_join /shop_cart /admin /?mode*
         → /reservation 은 허용
보호     없음
플랫폼   쇼핑몰 솔루션(아임웹 계열로 보임) + 자체 위젯
```

## 왜 못 긁나

예약 화면이 **자바스크립트 위젯**이다. 서버가 내려주는 HTML 에는 빈 껍데기만 있다:

```html
<div id="tte-avail">
  <button data-tab="byTime">빠른입장시간</button>
  <button data-tab="byTheme">테마별 예약가능시간</button>
  <div id="view-byTheme" class="grid" style="display:none"></div>   ← 비어 있다
</div>
```

내용은 브라우저가 스크립트를 돌려야 채워진다. **HTML 안에서 그 데이터를 가져오는 주소를 못 찾았다.**

## 선택지

| | 방법 | 판단 |
|---|---|---|
| ㉮ | 위젯이 부르는 API 를 찾아 그것만 호출 | **이걸 먼저 시도한다.** 브라우저 개발자도구 네트워크 탭이면 바로 보인다 |
| ㉯ | 헤드리스 브라우저로 렌더링 | ❌ 매장 하나 때문에 크롬을 상시 띄우는 건 비용이 안 맞는다 |
| ㉰ | 대상에서 뺀다 | ㉮ 가 안 되면 이쪽 |

> **㉯ 를 고르지 않는 이유를 적어 둔다.** 나중에 "왜 헤드리스를 안 썼지" 가 되지 않게 —
> 메모리(6GB 중 크롬이 수백 MB)와 실패 지점이 늘어나는 값이,
> 매장 한 곳에서 얻는 것보다 크다. 매장이 여러 곳 이 방식이면 그때 다시 계산한다.

---

# 대상에서 제외한 곳

## 네이버예약 (`booking.naver.com`) — 2026-08-27 확인

```
robots   User-agent: *
         Disallow : /        ← 전 경로 차단
```

**전면 금지다.** 우리 규칙상 이건 "오지 마" 라는 명확한 의사표시고, 논쟁의 여지가 없다
([CLAUDE.md](CLAUDE.md) 수집 윤리). `map.naver.com` 도 마찬가지이며, 거기는 AI 크롤러를
이름까지 지목해 막아 두었다.

**네이버예약으로만 예약을 받는 매장은 수집 대상이 아니다.** 우회하지 않는다.

### 그럼 그런 매장은 어떻게 하나

| | |
|---|---|
| **① 자체 예약 페이지 확인** | 네이버예약과 자체 홈페이지를 **병행**하는 매장이 많다. 자체 쪽이 있으면 그쪽을 본다 |
| **② 제휴** | 매장 입장에서 취소표 알림은 **빈 자리를 매출로 바꿔 주는 것**이라 이해가 대립하지 않는다. [기획.md](기획.md) "제휴라는 대안" |
| **③ Floduler 로 넘긴다** | **알림은 못 줘도 시간표 계산은 된다.** 사용자가 직접 입력(F-06)하거나 사진(F-05)으로 넣으면 그만이다 |

③ 이 중요하다. **이 서버는 Floduler 의 선택 사항**이라, 서버가 못 다루는 매장도
Floduler 에서는 그대로 쓸 수 있다. 잃는 것은 "자동으로 채워지는 편의" 와 "취소표 알림" 이지
기능 자체가 아니다.

---

## 매장 추가 체크리스트

- [ ] 위 "확인 순서" 4단계를 통과했고, 결과를 이 문서에 날짜와 함께 적었다
- [ ] 호스트를 적었다 — **병렬 단위이자 속도 상한**이다 (D13)
- [ ] 요청 단위(지점별/테마별)와 **한 바퀴 요청 수**를 계산해 적었다
- [ ] 예약 가능 여부를 **어느 값으로** 판단하는지 적었고, 그 값이 실제로 두 상태를 다 갖는 것을 확인했다
- [ ] "틀린 요청이 그럴듯한 응답으로 오는" 함정이 있는지 확인하고 적었다
- [ ] 실제 응답을 `src/test/resources/` 에 픽스처로 넣고 파서 테스트를 만들었다
- [ ] [작업명세서.md](작업명세서.md) 와 [아키텍처.md](아키텍처.md) 를 같은 커밋에서 갱신했다
