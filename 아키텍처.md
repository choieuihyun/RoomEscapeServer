# 아키텍처

> **이 문서의 역할** — 이 서버가 어떻게 생겼고, 왜 그렇게 생겼는지.
> **구조를 추가하거나 바꿀 때마다 반드시 갱신한다.** 코드만 바뀌고 이 문서가 그대로면 문서가 거짓말을 시작한다.
> 최종 수정: 2026-08-26 · 기술 스택: Kotlin 2.3 / Spring Boot 4.1 / JDK 21

---

## 0. 서버가 왜 필요한가

이 서비스는 **아무도 앱을 켜지 않은 새벽 3시에도 계속 돌아야 한다.** 클라이언트(앱)만으로는 원천적으로 불가능하다.

| 클라이언트가 직접 하면 | 서버를 두면 |
|---|---|
| 앱이 꺼지면 감시가 멈춤 | 24시간 감시 |
| 사용자 수만큼 매장 사이트를 때림 | 서버 한 대가 한 번만 조회해 전원에게 공유 |
| 파싱 로직이 바뀌면 앱 재배포 | 서버만 고치면 끝 |
| "아까는 매진이었다"를 기억 못 함 | DB에 이전 상태를 보관 |

특히 마지막이 핵심이다. **이 서비스의 본질은 "변화 감지"고, 변화를 감지하려면 과거를 기억해야 한다.**

---

## 1. 전체 흐름

```
  ⑥ Scheduler ─ 5분마다, 이번 바퀴에 볼 날짜를 정해서(D14) 파이프라인 실행 ─┐
                                                                          │
                                                                          ↓
  play33.kr      ──▶ HTML ┐                                       ⓪ StoreAdapter
  keyescape.com  ──▶ JSON ├── ① Client ─ ② Parser ─ ③ Crawler ──  "어떤 요청이 필요한가"
  rabbithole…    ──▶ HTML ┘   HTTP 조회    값 추출     검증           를 목록으로 (D15)
 (매장 사이트)                    ↑                                        │
                        HostRateLimiter                                    ↓
                        호스트당 동시 1개·초당 1회 미만 (D13)         ④ Repository
                                                                   이전 상태와 비교 후 저장
                                                                     │            │
                                          불가 → 가능 전이만 ────────┘            └── 조회 요청
                                                   ↓                                    ↓
                                            ⑤ Notifier                              ⑦ Api
                                            감시와 대조 · 쿨다운 판정                 JSON
                                                   ↓                                    ↓
                                             사용자 폰                        Floduler (GitHub Pages)
                                             (FCM 예정)                        연방 시간표 계산기
```

**전부 구현됐다. 남은 것은 ⑤의 발송 경로(FCM) 하나다** — 판정까지는 끝났고 지금은 로그로 나간다.

출구가 둘이라는 점이 중요하다. ⑤는 이 서버의 원래 목적이고, ⑦은 자매 프로젝트에 데이터를 먹인다.
**⑦이 먼저 나갔다** — ④만 있으면 되고, ⑤는 ⑥까지 다 있어야 하기 때문이다.
([기획.md](기획.md) 자매 프로젝트 절)

**매장이 셋이 되면서 ⓪ 이 생겼다.** 요청 모양이 매장마다 달라서(지점 단위 · 테마 단위)
수집기가 그 차이를 몰라도 되게 어댑터 뒤로 숨겼다.

---

## 2. 왜 이렇게 잘게 나눴나

서버가 처음이면 "그냥 한 파일에 다 짜면 되지 않나" 싶을 수 있다. 나눈 이유는 딱 하나다 —
**매장이 늘어나면 ①②③만 새로 만들면 되고, ④⑤⑥⑦은 그대로 재사용하기 위해서다.**

매장마다 사이트가 제각각이라 "HTTP로 가져오기"와 "HTML 읽기"는 매장 수만큼 필요하다.
반면 "변화를 감지하고 알림을 보낸다"는 매장과 무관하게 하나면 된다.
그 경계선이 지금의 패키지 구분이다.

| 계층 | 하는 일 | 매장마다 필요? |
|---|---|---|
| **Client** | HTTP 요청을 보내 HTML을 받아온다 | ✅ 매장마다 |
| **Parser** | HTML에서 시간표를 뽑는다 | ✅ 매장마다 |
| **Crawler** | Client+Parser를 묶고 **결과를 검증**한다 | ✅ 매장마다 |
| **Repository** | 저장하고 이전 상태와 비교한다 | ❌ 공통 |
| **Notifier** | 알림 신청과 대조해 푸시를 보낸다 | ❌ 공통 |
| **Scheduler** | 주기적으로 위를 실행한다 | ❌ 공통 |
| **Api** | 저장된 시간표를 외부에 JSON으로 내려준다 | ❌ 공통 |

---

## 3. 현재 코드

```
src/main/kotlin/com/my_dream/server/
├── ServerApplication.kt
├── crawler/
│   ├── CrawlResult.kt                매장 공통 모델 (StoreRef/DaySchedule/ThemeSchedule/Slot/ParsedPage)
│   ├── StoreAdapter.kt               ⓪ plan(dates) → List<FetchUnit> · branches   (D15)
│   ├── HostRateLimiter.kt            속도 규칙이 실제로 걸리는 곳 — 호스트별 잠금  (D13)
│   ├── play33/                       ← 매장 하나당 패키지 하나
│   │   ├── Play33Branch.kt           지점 4개 + host + 우리 쪽 키
│   │   ├── Play33Client.kt           ① GET /reservation?branch=&date=
│   │   ├── Play33Parser.kt           ② HTML — disabled 속성
│   │   ├── Play33Crawler.kt          ③ 렌더된 날짜 대조 (D2)
│   │   └── Play33Adapter.kt          ⓪ 지점 × 날짜 = 요청 1회
│   ├── keyescape/                    지점 11 · 테마 32
│   │   ├── KeyescapeBranch.kt        KeyescapeClient.kt      KeyescapeDtos.kt
│   │   ├── KeyescapeThemeCatalog.kt  테마 목록·메타데이터를 하루 한 번만 받는다
│   │   ├── KeyescapeCrawler.kt       ③ 응답의 theme_num 대조 (ID 공간이 둘이다)
│   │   └── KeyescapeAdapter.kt       ⓪ **테마** × 날짜 = 요청 1회
│   └── rabbithole/                   지점 1 · 테마 2. 플레이33과 같은 솔루션에 클래스명만 다르다
│       └── …Branch/Client/Parser/Crawler/Adapter.kt
├── api/                              ⑦ 외부 조회 (Floduler)
│   ├── ScheduleApiController.kt      /api/branches, /api/schedule
│   ├── ScheduleQueryService.kt       DB → 프론트 형식. 지원 지점은 어댑터가 알려준다
│   ├── WatchApi.kt                   감시 등록·해제 (로그인 필요)
│   ├── ApiDtos.kt                    계약 (원본은 Floduler 작업명세서 §4.4)
│   └── ApiCorsConfig.kt              CorsConfigurationSource 빈 — 시큐리티가 봐야 한다 (D12)
├── security/
│   └── ApiSecurityConfig.kt          Firebase ID 토큰 검증 (D12)
├── domain/
│   ├── Entities.kt                   Store / Theme / TimeSlot / Watch / NotificationLog
│   └── Repositories.kt
├── notify/                           ⑤
│   ├── Notifier.kt                   인터페이스 + LoggingNotifier(기본)
│   └── NotificationService.kt        누구에게·지금 보내도 되는지 판정 (D11)  ← 어려운 쪽
├── sync/
│   ├── ScheduleSyncService.kt        ④ upsert + 전이 감지 + 위생 검사   ← M2의 심장
│   ├── ScheduleIngest.kt             저장 + 알림 + 경고 (수집 경로 둘이 공유)
│   ├── StoreCollector.kt             한 바퀴 — 어댑터를 호스트로 묶어 병렬 (D13)
│   ├── PollingSchedule.kt            이번 바퀴에 볼 날짜 (D14)
│   ├── Play33Collector.kt            한 지점·하루 손수집 (확인용)
│   ├── Play33CollectJob.kt           ⑥ 언제 돌지만
│   └── SyncResult.kt                 SlotTransition / QuarantineReport
├── debug/
│   └── Play33DebugController.kt      기본 꺼짐. 배포에서는 Caddy 도 라우팅하지 않는다
└── resources/db/migration/           V1 스키마 · V2 평점 실수화 · V3 감시·알림

src/test/  픽스처 6장 + 테스트 87개
  play33-konkuk-2026-09-01.html · play33-daejeon-2026-08-28.html    (정수 / 0.5 단위)
  rabbithole-hongdae-2026-08-29.html · …-09-02.html                 (전량 매진 / 섞임)
  keyescape-*.json                                                  (목록·상세·회차·거절 2종)
```

### 저장 구조

```
Store       store_key(자연키) / brand / branch_name          play33-konkuk, 플레이33, 건대점
 └ Theme    (store, external_id) 유니크 / name / 메타데이터    18, 목격자, 65분 …
      └ TimeSlot  (theme, slot_date, slot_time) 유니크
                  available     ← 이 값의 변화가 이 서비스의 전부다
                  last_checked_at

Watch            (user_id, time_slot_id) 유니크 — 같은 자리를 두 번 신청할 수 없다
 └ NotificationLog  watch_id / sent_at / outcome(SENT|FAILED)   쿨다운 판정의 유일한 근거 (D11)
```

### 데이터 모델 (현재)

```
DaySchedule            검증까지 끝난 결과. 이것만 믿고 저장한다
 ├ store               StoreRef(key, brand, branchName) — **매장 종류를 모르는 공통 모양**
 ├ date                어느 날짜
 ├ reservationRangeDays 사이트가 밝힌 예약 오픈 범위 (플레이33 7 · 나머지는 안 밝혀서 null)
 └ themes: [ThemeSchedule]
      ├ themeName, genre, capacity, runningMinutes, horrorLevel, difficulty, posterUrl
      └ slots: [Slot]
           ├ time        회차 시작 시각
           └ available   예약 가능 여부  ← 이 값의 변화가 이 서비스의 전부다

ParsedPage           파서가 읽은 날것. 아직 검증 안 됨. Crawler 내부에서만 쓴다
```

---

## 4. 설계 결정과 이유

새로운 결정을 내리면 여기에 번호를 붙여 추가한다.

### D1. 예약 가능 판정은 `disabled` 속성으로 한다

```html
<button class="eveReservationButton">예약 가능 <span>10:35</span></button>   → available
<button disabled>                    예약 불가 <span>18:15</span></button>   → 매진
```

버튼 문구("예약 가능")로 판정할 수도 있지만, **문구는 디자인 변경으로 쉽게 바뀌고 속성은 잘 안 바뀐다.**
`disabled`는 버튼을 실제로 못 누르게 하는 기능적 속성이라 함부로 지워지지 않는다.

### D2. 조회 결과는 반드시 검증을 통과해야 저장된다

플레이33은 예약 범위(7일) 밖의 날짜를 요청하면 **홈페이지로 302 리다이렉트**한다.
그대로 파싱하면 시간표가 하나도 없는 정상 형태의 결과가 나온다 — 즉 **"그날 슬롯이 전부 사라졌다"로 읽힌다.**

DB가 붙은 뒤 이런 일이 생기면 멀쩡한 데이터를 지우고, 사용자에게는 잘못된 알림이 나간다.
그래서 `Play33Crawler`가 **페이지가 스스로 밝힌 날짜**(`input[name=date]`의 value)를
요청한 날짜와 대조하고, 다르면 예외로 끊는다.

> **원칙:** 조회 실패와 "진짜로 결과가 없음"은 절대 같은 값으로 표현하지 않는다.

### D3. 감시 대상 선별이 아니라 전량 수집을 한다

처음엔 "알림 신청이 걸린 슬롯만 긁자"고 설계했다. 그런데 플레이33이 **7일치만** 여는 걸 확인하면서 뒤집혔다.

```
4지점 × 7일 = 28요청 → 9테마 × 7일 = 63개 시간표 = 플레이33 전부
5분마다 전량 수집해도 초당 0.1회
```

**전부 긁는 게 선별 로직을 관리하는 것보다 가볍고 단순하다.**

> **2026-08-27 부분 번복 — [D14](#d14-날짜는-요일로-등급을-나눠-긁는다-계층-폴링) 참고.**
> "예약 범위가 넓은 매장이 오면 다시 본다" 고 적어 뒀는데, 실제로 걸린 건 범위가 아니라
> **요청 단위**였다. 키이스케이프는 7일치인데도 테마마다 요청이라 224요청 · 4.5분이 든다.
> *무엇을* 긁을지는 여전히 전량이고, *얼마나 자주* 긁을지만 요일로 나눈다.

### D4. `RestClient`를 직접 생성한다

Spring Boot 4의 `spring-boot-starter-webmvc`에는 `RestClient.Builder` 빈 자동 설정이 딸려오지 않는다.
그래서 `RestClient.create(BASE_URL)`로 직접 만든다. 타임아웃·로깅·재시도는 여기에 붙인다.

### D5. 파서 회귀는 픽스처 테스트로 막는다

`src/test/resources/`에 **실제로 받아온 HTML**을 넣어두고 파서를 검증한다.
사이트 마크업이 바뀌면 배포된 서버가 조용히 오작동하기 전에 **테스트가 먼저 깨진다.**

### D6. 오알림 방지 위생 검사 — 걸리면 저장도 안 한다

파서가 깨져서 모든 슬롯을 `available`로 읽으면 전 사용자에게 헛알림이 나가고 서비스 신뢰가 끝난다.
**이전에 매진이던 회차가 4개 이상 있는데 그 절반 넘게 한 번에 풀리면** 의심한다.

의심되면 **저장까지 건너뛴다.** 알림만 막고 저장은 하는 선택지도 있었지만,
깨진 값을 넣으면 다음 비교의 기준선까지 오염돼서 그 뒤로 한동안 판단이 전부 틀어진다.
격리된 수집분은 사용자가 아니라 **운영자에게** `ERROR` 로그로 알린다.

표본 하한(4)이 있는 이유: 매진 2개 중 1개가 풀린 것도 "절반"이라 정상 취소가 걸린다.

**그리고 3회 연속이면 저장은 한다.** 실제로 돌려 보고 알았는데, 건너뛰기만 하면
기준선이 낡은 채로 남아 **다음에도 똑같이 걸린다 — 영영 못 빠져나온다.**
매장이 하루를 통째로 닫았다가 다시 여는 것처럼 정상 상황에서도 이 판정이 나올 수 있어서
빠져나올 길이 없으면 안 된다.

그래서 같은 판정이 `MAX_CONSECUTIVE_QUARANTINES`(3)회 반복되면 **저장해서 기준선을 되살리되,
그 회차의 전이는 끝까지 내보내지 않는다.** 수십 건이 한꺼번에 알림으로 나가는 게
D6 이 애초에 막으려던 사고이기 때문이다. 다음 수집부터는 정상으로 돌아온다.

연속 횟수는 메모리에만 둔다. 재시작하면 초기화되지만, 최악이라도 수집 한 바퀴를 더 건너뛸 뿐이다.

### D7. Floduler와는 저장소가 아니라 API 계약으로 잇는다

자매 프로젝트 [Floduler](기획.md)(`~/RoomEscapeScheduler`)는 정적 웹앱이다. 합치는 방법을 셋 놓고 봤다.

| 안 | 왜 안 골랐나 |
|---|---|
| 한 저장소로 통합 | GitHub Pages는 루트 배포가 필요하고, Kotlin/Gradle과 vanilla JS는 툴체인이 안 겹친다. 배포 주기도 다르다(푸시 즉시 vs 도커 재배포) |
| Spring이 프론트를 서빙 | Floduler의 "서버 없이 완결된다"가 깨진다. 서버가 죽으면 계산기까지 죽는다 |
| **API 계약으로 연결** ✅ | 각자 배포·각자 수명. 서버가 없어도 Floduler는 지금처럼 동작 |

**핵심 제약은 Floduler의 설계 철학이다** — 정적 배포, 빌드 없음, 의존성 0, 로그인 없이 완결.
이 서버를 필수로 만들면 그게 전부 무너진다. 그래서 **선택적 데이터 소스**로 붙는다.
Floduler가 Firebase 계정 기능에 이미 쓰고 있는 패턴(그쪽 §4.18)과 같은 형태다.

### D8. 테마의 키는 이름이 아니라 사이트가 준 ID 다

`<select name="theme"><option value="18">목격자</option>` 에서 `18` 을 뽑아 자연키로 쓴다.

이름을 키로 잡으면 **테마명이 바뀌는 순간 같은 테마가 두 행으로 갈라지고**, 과거 이력이 끊긴다.
방탈출은 리뉴얼·시즌제로 이름이 실제로 바뀐다. 사이트가 ID 를 안 주는 경우에만 이름으로 대체한다.

### D9. "수집한다"와 "언제 수집한다"를 분리한다

`Play33Collector`(수집)와 `Play33CollectJob`(스케줄)이 나뉘어 있다.

처음엔 한 클래스였는데, 스케줄러를 끄는 설정(`collector.play33.enabled=false`)을 켜자
**수집 기능 자체가 사라져서** 손으로 한 번 돌려보는 것도 불가능해졌다. 테스트 컨텍스트가 그것 때문에 깨졌다.
"무엇을 한다"와 "언제 한다"는 켜고 끄는 단위가 다르다.

### D10. 공포도·난이도는 `Double` 이다 — 사이트가 0.5 단위를 쓴다

`Int` 로 받다가 값이 조용히 망가지고 있었다.

```
공포   "3.5"   → 숫자만 남김 → 35
공포   "0.5"   → 숫자만 남김 →  5      ← "거의 안 무섭다" 가 "최고 공포" 로 뒤집혔다
난이도 size25  → 정수 변환   → 25      ← 화면에 별이 25 개 찍혔다
```

`reservation.css` 가 `size1 size15 size2 size25 … size4 size45` 를 각각 다르게 그린다.
**두 자리는 반 칸**이고, `size25` 는 25 가 아니라 2.5 다.

셋 다 예외를 던지지 않았다. 범위를 벗어난 숫자가 그럴듯한 얼굴로 API 를 타고 프론트까지 갔고,
**별 25 개가 화면에 찍히고 나서야** 발견됐다. 타입이 좁으면 파서는 실패하는 대신 반올림한다.

세 자리 이상(백분율 등)은 우리가 모르는 표기다. 넘겨짚어 이상한 값을 만드느니 `null` 로 둔다.

기존 DB 값은 `V2` 에서 **전부 비웠다.** 저장된 `5` 가 진짜 5.0 인지 뒤집힌 0.5 인지
구분할 방법이 없어서, 남겨 두면 틀린 값을 옳은 값인 척 계속 들고 있게 된다.

> **D5(픽스처 테스트)의 한계이기도 하다.** 건대점 픽스처는 공포·난이도가 전부 정수라
> 이 버그를 통과시켰다. 그래서 소수가 든 **대전점 픽스처를 따로 넣었다** —
> 표본이 하나면 그 표본에 없는 경우는 영원히 안 잡힌다.

### D11. 알림 중복은 **쿨다운 1시간**으로 막는다 (평생 1회가 아니라)

한 자리에 알림을 보내면 1시간 동안 그 자리는 다시 알리지 않는다. 1시간이 지나면 다시 알린다.

평생 1회(once-ever)로 하면 알림이 절대 안 시끄럽지만, **첫 알림을 놓친 사람은 그 자리를 영영 못 받는다.**
취소표는 났다 사라졌다를 반복해서, "놓쳤어도 다음 기회" 가 이 서비스의 가치다.

판정 근거는 `notification_log` 테이블이다. **메모리에 두지 않는다** — 서버를 재시작하면 기억이 날아가고,
그 직후 수집 한 바퀴에서 방금 보낸 알림이 전부 다시 나간다.

**발송 실패는 쿨다운으로 세지 않는다.** `outcome = SENT` 인 기록만 본다.
실패를 "알렸다" 로 세면 그 자리는 한 시간 동안 조용히 묻힌다.

### D12. 신원은 Floduler 의 Firebase 로그인을 그대로 쓴다

`/api/watches` 만 로그인이 필요하고, 조회 API 는 지금까지처럼 공개다.

Floduler 는 이미 Firebase Auth 를 쓴다. 브라우저가 받은 ID 토큰(JWT)을 `Authorization: Bearer` 로
보내면 서버는 구글 공개키로 서명만 검증한다. **비밀번호가 우리 쪽을 지나가지 않는다.**

- `jwkSetUri` 로 디코더를 만든다. `issuerUri` 를 쓰면 스프링이 **기동 시점에** 디스커버리 문서를
  받으러 나가서, 네트워크가 없으면 앱이 아예 안 뜨고 테스트도 같이 죽는다
- `aud` 를 직접 검증한다. **기본 검증기가 안 보는 항목이라**, 빠뜨리면 남의 Firebase 프로젝트에서
  발급된 토큰도 통과한다
- 기기 토큰만 쓰는 방법도 있었지만, 기기를 바꾸면 감시가 사라지고 남의 토큰을 알면 남의 감시를 지울 수 있다

> **CORS 설정이 `WebMvcConfigurer` 에서 `CorsConfigurationSource` 빈으로 바뀐 것도 이 결정 때문이다.**
> 시큐리티 필터는 MVC 보다 먼저 돌아서 `addCorsMappings` 를 보지 못한다. 그대로 뒀다면
> 프리플라이트가 MVC 에 닿기 전에 막혀 "CORS 를 분명히 열었는데 안 된다" 가 됐을 것이다.

### D13. 병렬 단위는 **지점이 아니라 사이트**다

한 바퀴에서 서로 다른 **사이트**는 동시에 긁고, **같은 사이트로 가는 요청은 끝까지 한 줄로 선다.**

처음엔 지점별로 병렬화했다가 되돌렸다. **틀렸기 때문이다.**

```
play33.kr/reservation?branch=1   건대점
play33.kr/reservation?branch=4   홍대점      ← 전부 같은 서버 한 대다
play33.kr/reservation?branch=5   대전점
play33.kr/reservation?branch=7   수원점
```

지점은 서버가 아니라 **쿼리 파라미터**다. 지점별로 동시에 긁으면 `play33.kr` 한 대에
동시 4개가 간다. 실측으로 `28요청 / 8초 = 초당 3.5회` 였고, 우리 규칙은 **초당 1회 미만**이다.

> 수집 윤리 규칙의 "매장당" 은 **상대 서버 기준**으로 읽어야 한다.
> 우리 화면에서 지점이 넷으로 나뉘어 있다고 상대 서버가 넷이 되지 않는다.
> 지점 수·브랜드 수가 아니라 **호스트 수**가 속도를 정한다.

그래서 `Play33Branch.host` 로 묶고, **그룹끼리만** 동시에 돈다.
지금은 그룹이 하나뿐이라 실질적으로 순차다 — 한 바퀴 `28요청 × 1.2초 ≈ 33초`.

병렬의 이득은 **브랜드가 늘어 사이트가 여러 개가 될 때** 나온다. 그때는 순차로 두면
한 바퀴가 `요청수 × 1.2초` 로 늘어 수집 주기를 넘기기 시작한다.

```
사이트 1곳 (지금)   28요청    순차 33초                  주기 5분 안
사이트 20곳         350요청   순차 7분 → 동시 4 로 ~1.7분
```

`collector.play33.site-concurrency` 로 조절한다. **지점을 늘릴 때가 아니라 사이트가 늘 때** 올린다.

- 스레드 풀은 **바퀴마다 만들고 접는다.** 스케줄러를 꺼 둔 개발 기기에 스레드가 남지 않는다
- 같은 사이트의 요청을 한 스레드가 순서대로 처리하므로, `ScheduleSyncService` 가 동시에
  불려도 사이트가 다르면 건드리는 행이 달라 DB 경합이 없다
  (`quarantineStreaks` 는 이미 `ConcurrentHashMap` 이다)
- ⚠️ **Hikari 커넥션 풀 기본값이 10** 이다. `site-concurrency` 를 그보다 크게 올리면
  저장 단계가 줄을 선다 (연결은 `sync` 동안만 잡고 조회·대기 중에는 놓으므로 막히지는 않는다).
  사이트를 크게 늘릴 땐 `spring.datasource.hikari.maximum-pool-size` 도 같이 본다

> **테스트가 재는 것은 한 바퀴 시간이 아니라 호스트당 요청 간격이다**
> (`Play33CollectorRateTest`). 빨라지는 건 얼마든지 만들 수 있고 그게 곧 규칙 위반이다.
> 지점별 병렬로 되돌리면 이 테스트가 먼저 깨진다 — 실제로 깨지는 것을 확인했다.

### D14. 날짜는 요일로 등급을 나눠 긁는다 (계층 폴링)  ✅ 구현됨

모든 날짜를 똑같이 자주 볼 필요가 없다. 취소표는 **늦게 알면 소용없는 자리**가 따로 있다.

```
토·일     매 바퀴      5분마다      주말은 예약이 빡세고, 풀리면 몇 분 안에 채간다
금        2바퀴마다    10분마다
그 외     순환         20분마다     널널해서 취소표 자체가 드물고, 나도 덜 급하다
```

```
전량 폴링   매 바퀴 7일치   32테마 × 7 = 224요청   4.5분   주기 5분에 여유 30초
계층 폴링   매 바퀴 ~3.5일  32테마 × 3.5 = 112요청  2.2분   여유 2.8분
```

**요일을 기준으로 삼은 근거는 도메인 경험이다.** 주말 자리는 풀리면 몇 분 안에 채가고
평일은 여유가 있다 — 폴링 주기를 정하는 건 매진율이 아니라 **재점유 속도**이고, 그건
실제로 예약을 해 본 사람이 아는 값이다.

우리가 잰 매진율도 같은 방향을 가리킨다 (일반 테마 평일 29% ↔ 주말 53%, 인기 83% ↔ 99%).

> 서버가 돌기 시작하면 "언제·어느 자리가·얼마 만에 다시 찼는지" 가 쌓인다.
> 그때는 등급을 **더 잘게** 나눌 수 있다 (예: 주말 19시대만 따로). 방향을 바꾸는 게 아니라
> 해상도를 올리는 것이다.

**구현 결과 (2026-08-27, 목요일 기준)**

```
 0분  27목 28금 29토 30일     4일치
 5분       29토 30일 31월     3일치
10분  28금 29토 30일  1화     4일치
15분       29토 30일  2수     3일치
20분  27목 28금 29토 30일     ← 20분에 한 바퀴 순환이 끝난다
```

```
토·일    5분마다      금    10분마다      그 외    20분마다
```

`PollingSchedule` 이 정하고, **바퀴 번호를 시계에서 뽑는다** — 카운터를 들고 있으면
재시작할 때마다 0 으로 돌아가서, 개발 중에 자주 껐다 켜면 같은 평일만 계속 보고 나머지가 굶는다.

실측: 플레이33 한 바퀴가 **28요청 33초 → 12요청 13초**.

매진 여부로 등급을 나누는 안(매진 0 이면 전이가 불가능하므로 제외)도 검토했지만 넣지 않았다.
표본에서 절감이 21% 에 그쳤고, 규칙이 하나 늘어나는 값을 못 한다.

**등급은 매장을 가리지 않고 똑같이 적용한다.** 플레이33은 34초라 계층이 필요 없지만,
매장마다 예외를 두는 순간 다음 어긋남이 거기서 시작된다.

> **부작용 하나를 의도적으로 남긴다.** 위생 검사 격리 복구는 "연속 3바퀴" 기준인데(D6),
> 평일 날짜는 4바퀴에 한 번 방문하므로 실제 시간이 15분 → 60분으로 늘어난다.
> **방문 횟수 기준이 맞다** — 격리는 "볼 때마다 같은 이상이 반복되는가" 를 보는 것이지
> 시계를 보는 게 아니다.

### D15. 매장 어댑터의 작업 단위는 **요청 하나**다  ✅ 구현됨

지금 `Play33Crawler.fetch(지점, 날짜)` 는 **"지점+날짜 = 요청 하나"** 를 가정한다.
키이스케이프는 테마마다 요청이라 그 가정이 깨진다.

그래서 어댑터는 "이 날짜를 긁으려면 어떤 요청들이 필요한가" 를 **목록으로** 내놓고,
속도 조절은 수집기가 **요청 단위로** 한다.

```
adapter.plan(날짜)  →  [작업, 작업, …]      각 작업 = HTTP 요청 정확히 1회
수집기              →  같은 host 의 작업을 한 줄로, 사이에 1.2초
```

**`fetch()` 안에서 여러 번 요청하게 두면 안 되는 이유:** 그러면 속도 규칙이 구조가 아니라
관례가 된다. 지금 `Play33CollectorRateTest` 는 `fetch()` 호출 간격을 재는데,
`fetch()` 하나가 요청 3번이 되는 순간 **간격이 3배로 보여서 테스트는 통과하고 규칙은 깨진다.**
D13 과 똑같은 실수가 한 층 아래에서 반복되는 것이라 더 안 보인다.

→ 계측 지점도 `Play33Client`(HTTP 계층)로 내렸다. 거기가 어긋날 수 없는 유일한 자리다.

**속도 조절 자체가 `HostRateLimiter` 로 내려갔다.** 수집기는 더 이상 `sleep` 하지 않는다.
잠금을 요청 **전체**에 걸쳐 잡아서 두 가지를 동시에 보장한다:

```
같은 호스트   잠금이 같다   →  동시 1개  ·  이전 요청이 끝난 뒤부터 간격을 잰다
다른 호스트   잠금이 다르다  →  서로 기다리지 않는다
```

덕분에 어댑터가 실수로 한 작업 안에서 요청을 세 번 해도 **각각 제한에 걸린다.**
D15 의 "작업 하나 = 요청 하나" 는 이제 정확성이 아니라 **한 바퀴 요청 수를 미리 세기 위한**
약속이다 — 규칙 자체는 구조가 지킨다.

**구현 결과 (2026-08-27)**

```
crawler/play33/            지점 × 날짜 = 요청 1회   HTML   disabled 속성
crawler/keyescape/         테마 × 날짜 = 요청 1회   JSON   enable Y/N + 테마 카탈로그(하루 캐시)

crawler/StoreAdapter.kt    plan(dates) → List<FetchUnit> · branches
crawler/HostRateLimiter.kt 호스트별 잠금 + 간격
crawler/CrawlResult.kt     StoreRef · DaySchedule · ThemeSchedule · Slot  ← 매장 공통으로 승격
sync/ScheduleIngest.kt     저장 + 알림 + 경고 (수집 경로 둘이 공유)
sync/StoreCollector.kt     어댑터를 호스트로 묶어 한 바퀴
```

`DaySchedule.branch: Play33Branch` 가 `store: StoreRef` 로 바뀌었다.
저장·조회가 매장 종류를 몰라도 되게 어댑터 경계에서 끊는다.

동작은 바뀌지 않았다 — 실측 **28요청 · 33초**, 조회 API·디버그·인증 전부 그대로.

**키이스케이프의 테마 목록 조회(`get_theme_info_list`)도 요청이다.** 지점별로 캐시하고
하루 한 번 갱신하되, **그 갱신도 다른 작업과 똑같이 줄을 세운다** —
안 그러면 11지점 × 매 바퀴로 13초가 조용히 붙는다.

---

## 5. 계층별 상세

### ⑤ Notifier — 알림  ✅ 구현됨 (발송 경로는 아직 로그)

```
watch 테이블             누가(Firebase uid) 어느 자리를 보는지. 자연키 (user_id, time_slot_id)
notification_log 테이블  언제 알렸는지. 쿨다운 판정의 유일한 근거 (D11)

NotificationService     누구에게·지금 보내도 되는지 판정한다   ← 어려운 쪽
Notifier (인터페이스)    실제로 내보낸다
  ├ LoggingNotifier     기본. 로그에만 남긴다
  └ FcmNotifier         ⏳ 예정. notify.channel=fcm 으로 갈아 끼운다
```

**발송을 인터페이스 뒤에 둔 이유:** FCM 은 파이어베이스 서비스 계정 키가 있어야 붙는데,
그게 없다고 알림 로직 전체를 못 만들 이유는 없다. 판정이 진짜 어려운 부분이고 키 없이 전부 검증된다.

**호출은 `sync` 트랜잭션 밖에서 한다.** 안에서 보내면 저장이 롤백돼도 알림은 이미 나간 뒤라,
존재한 적 없는 자리를 알린 꼴이 된다. 알림은 취소할 수 없다.

알림 경로가 통째로 터져도 수집 한 바퀴는 계속 돈다 — 수집이 알림에 종속되면 안 된다.

### ⑥ Scheduler — 주기 실행  ✅ 구현됨

`@Scheduled(fixedDelay)` 로 5분마다 한 바퀴. **전량이 아니라 [D14](#d14-날짜는-요일로-등급을-나눠-긁는다-계층-폴링--구현됨) 가 고른 날짜만** 돈다.

```
2026-08-28 실측   3사이트 동시 · 111요청 · 122초 · 실패 0건
                  (전량이면 3.5일치가 아니라 7일치라 두 배)
```

속도는 `HostRateLimiter` 가 요청마다 건다 — 스케줄러는 `sleep` 하지 않는다 ([D15](#d15-매장-어댑터의-작업-단위는-요청-하나다--구현됨)).
`fixedDelay` 라 이전 바퀴가 끝난 뒤부터 재므로 겹치지 않는다.

20:00 전후 집중 폴링은 **아직 검증되지 않은 가설**에 기대고 있다 — [기획.md](기획.md) 참고. 확인 전에는 넣지 않는다.

> **주의:** 서버를 2대 이상 띄우면 스케줄러가 중복 실행된다. 그때 `ShedLock` 같은 잠금이 필요하다.
> **1대일 때는 필요 없다.**

### ⑦ Api — 외부 조회 (Floduler 연동)  ✅ 구현됨

```
GET /api/branches                                   지점 목록 + 고를 수 있는 날짜
GET /api/schedule?branch=play33-konkuk&date=...     그날 전 테마 회차 + 매진 여부 + 메타데이터
```

**호출 두 번으로 화면이 완성되게** 잘랐다. Floduler 의 흐름이 `지점 → 날짜 → 테마 선택` 이라,
지점 목록에 날짜를 같이 실어 날짜 선택에 추가 호출이 생기지 않게 했다.

**저장된 것만 준다. 조회가 크롤을 부르지 않는다** — 그러면 외부에서 우리 크롤 빈도를 좌우하게 된다.
대신 `checkedAt` 으로 언제 기준인지 밝히고, 신선도는 스케줄러가 책임진다.
수집 전인 지점·날짜는 빈 배열이 아니라 **404** 다. "회차가 없는 날"과 "아직 안 본 날"은 다른 말이다.

**매진 회차도 빠짐없이 내려보낸다.** 계산에서 빼는 것은 화면이 할 일이고,
화면에 남아 있어야 나중에 "이 자리 감시"(⑤)를 붙일 수 있다.

**응답은 Floduler의 내부 형식에 맞춘다.** 특히 시각을 `"10:35"`가 아니라 **자정부터의 분(`635`)** 으로
내려준다 — Floduler의 `session.t`가 이미 그 형식이라 프론트에 변환 코드가 생기지 않는다.
소요시간(`dur`)은 홈페이지 표기값을 가공 없이 그대로 보낸다 ([기획.md](기획.md) 참고).

계약 전문은 Floduler `작업명세서.md` §4.4. **양쪽이 같은 계약을 두 벌 적지 않도록 그쪽을 원본으로 삼는다.**

필요한 것:
- **CORS** — `https://choieuihyun.github.io` 허용
- **HTTPS** — Pages가 https라 http 응답은 브라우저가 차단한다 (M4 배포가 선행되어야 실제 연동이 된다)
- 인증 없음. 공개 읽기 전용이다


---

### ⑧ 배포  ✅ 2026-08-28

```
인터넷 ──443──▶ Caddy ──▶ app:8080 ──▶ db:5432
                 │          │            │
                 │          │            └ 호스트에 포트를 열지 않는다
                 │          └ SERVER_ADDRESS=0.0.0.0 (컨테이너 안에서만)
                 └ /api 만 프록시. 나머지는 404. 인증서 자동 발급·갱신
```

`https://floduler.duckdns.org` — 오라클 도쿄 · A1.Flex · 1 OCPU · 6GB · Ubuntu 24.04 (aarch64).

#### 주소가 셋인데 뭐가 다른가

헷갈리기 쉬운 부분이라 따로 적는다. **셋이 하는 일이 전부 다르다.**

| 주소 | 하는 일 | 누가 운영 | 돈 |
|---|---|---|---|
| `choieuihyun.github.io/RoomEscapeScheduler/` | Floduler **화면** (HTML·JS·CSS) | GitHub Pages | 무료 |
| `floduler.duckdns.org` | **이름표** — 이름을 IP 로 바꿔 준다 | DuckDNS | 무료 |
| `161.33.176.233` | **실제 서버** — 크롤·DB·API | 오라클 (우리 계정) | $0 |

브라우저에서 실제로 일어나는 일:

```
① Pages 주소를 연다
     → GitHub 이 index.html · server.js 를 내려준다        파일만 준다. 여기서 끝
② "회차 불러오기" 를 누른다
     → 그 자바스크립트가 floduler.duckdns.org 로 요청
     → DNS 가 161.33.176.233 을 알려준다
     → 오라클 서버가 DB 에서 꺼내 JSON 으로 답한다
③ 화면에 카드가 그려진다
```

**DuckDNS 는 호스팅이 아니다.** 거기엔 우리 코드가 한 줄도 없고,
`floduler.duckdns.org → 161.33.176.233` 을 알려주는 전화번호부 역할만 한다.

**왜 이름이 필요했나** — IP 로 직접 부르면 안 되나?

```
HTTPS 인증서는 도메인 이름에 발급된다        IP 로는 못 받는다
Pages 는 https 라 http 를 못 부른다          그래서 인증서가 필수다
인스턴스를 껐다 켜면 IP 가 바뀔 수 있다       이름이 있으면 자동으로 따라간다
```

마지막 이유 때문에 `bootstrap.sh` 가 **5분마다 현재 IP 를 DuckDNS 에 알리는 크론**을 걸어 둔다.

**왜 Pages 로 서버까지 안 하나** — Pages 는 **파일을 내려주기만** 한다.
계속 돌면서 방탈출 사이트를 긁고 DB 에 쌓는 일은 못 한다. 그래서 서버가 따로 필요하다
([README](README.md) "왜 서버가 필요한가").


**ARM 인데 손댈 게 없었다.** 소스에서 빌드해 JAR 을 만드는 구조라 아키텍처를 타지 않고,
Postgres·Caddy 도 arm64 이미지가 있다.

바깥으로 열린 것은 **80 · 443 · 22** 뿐이고, 앞의 둘은 Caddy 가, 22 는 키만 받는다.
절차와 실제 배포 기록은 [배포.md](배포.md).

> **오라클은 방화벽이 두 겹이다** — VCN 보안 목록(콘솔)과 서버 iptables(`bootstrap.sh`).
> 한쪽만 열고 "왜 접속이 안 되지" 로 헤매기 쉽다.

---

## 6. 용어 정리

서버가 처음이라면 이것만 알아도 위 문서가 읽힌다.

| 용어 | 뜻 |
|---|---|
| **크롤링** | 다른 사이트의 페이지를 프로그램으로 가져오는 것 |
| **파싱** | 가져온 HTML 덩어리에서 원하는 값만 뽑아내는 것 |
| **스케줄러** | "5분마다 이걸 해라"를 담당하는 부품. Spring에선 `@Scheduled` |
| **upsert** | 있으면 수정(update), 없으면 삽입(insert) |
| **자연키** | 데이터 자체로 유일함이 정해지는 키. 여기선 (지점, 테마, 날짜, 시각) |
| **빈(Bean)** | Spring이 대신 만들어서 필요한 곳에 넣어주는 객체. `@Component`가 붙은 것들 |
| **의존성 주입** | 객체를 직접 `new` 하지 않고 생성자로 받는 것. `Play33Crawler(client, parser)` 가 그 예 |
| **픽스처** | 테스트에 쓰려고 저장해둔 실제 데이터 샘플 |
| **백오프** | 상대가 힘들어하면(429/503) 요청 간격을 점점 늘리는 것 |
| **SSRF** | 서버가 공격자가 시키는 대로 아무 주소나 대신 요청하게 만드는 취약점 |

### 배포하면서 나오는 말들

| 용어 | 뜻 | 우리 경우 |
|---|---|---|
| **DNS** | 이름 → IP 로 바꿔 주는 전화번호부. **파일을 보관하지 않는다** | DuckDNS 가 `floduler.duckdns.org` → `161.33.176.233` |
| **도메인** | 사람이 외울 수 있는 서버 이름 | 인증서가 여기 발급되고, IP 가 바뀌어도 이름은 그대로다 |
| **정적 호스팅** | HTML·JS 파일을 그대로 내려주기만 하는 것. **코드를 돌리지 못한다** | GitHub Pages 가 Floduler 를 그렇게 준다 |
| **리버스 프록시** | 바깥 요청을 받아 뒤쪽 앱에 넘겨주는 문지기 | Caddy. `/api` 만 통과시키고 나머지는 404 |
| **HTTPS / 인증서** | 통신을 암호화한다. 인증서는 "이 도메인이 맞다" 는 증명서 | Caddy 가 Let's Encrypt 에서 자동 발급·갱신 |
| **CORS** | 브라우저가 **다른 출처의 응답을 막는** 규칙. 서버가 허락해야 읽힌다 | Pages 출처만 허용. 그래서 브라우저는 남의 사이트를 직접 못 긁는다 |
| **컨테이너 / 도커** | 앱과 필요한 것들을 통째로 담아 어디서든 같게 돌리는 상자 | app · db · caddy 세 개가 한 묶음으로 뜬다 |
| **포트** | 한 서버 안에서 프로그램을 구분하는 번호 | 밖에는 80·443·22 만 열고, 앱(8080)·DB(5432)는 안 연다 |
| **방화벽** | 어떤 포트로 들어오는 것을 허용할지 정하는 규칙 | **오라클은 두 겹이다** — VCN 보안 목록(콘솔) + 서버 iptables |
| **ARM (aarch64)** | CPU 종류. x86 과 다르다 | 소스에서 빌드해 JAR 을 만들어서 우리는 손댈 게 없었다 |
| **멱등** | 여러 번 해도 결과가 같은 것 | `bootstrap.sh` — 재배포할 때 같은 줄을 그냥 다시 돌린다 |
| **크론(cron)** | "몇 분마다 이걸 해라" 를 OS 가 대신 해 주는 것 | DuckDNS 에 5분마다 현재 IP 를 알린다 |

---

## 7. 갱신 규칙

다음 중 하나라도 하면 **같은 커밋에서** 이 문서를 고친다.

- [ ] 새 계층·패키지를 추가했다
- [ ] 데이터 모델을 바꿨다
- [ ] 설계 결정을 내렸다 → §4에 `D번호`로 추가
- [ ] 새 매장(어댑터)을 추가했다
- [ ] 의존성을 추가했다

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-26 | 최초 작성. Client/Parser/Crawler 계층 확립, D1~D5 기록 |
| 2026-08-26 | Api 계층(⑦) 추가, D7(저장소 분리·API 계약) 기록. 계층 번호 재정리 |
| 2026-08-26 | M2 구현 — domain/sync 계층 추가, D6 확정(격리 시 저장도 안 함), D8·D9 기록 |
| 2026-08-26 | D6 보강 — 격리가 스스로 못 빠져나오는 문제를 실행 중 발견, 3회 연속 시 복구 저장 |
| 2026-08-26 | M2.5 — Api 계층(⑦) 구현. 매진 회차를 응답에 남기고 테마 메타데이터를 함께 제공 |
| 2026-08-28 | 배포 구조(⑧) 추가. §1 흐름·§3 코드 트리·데이터 모델·⑥ 스케줄러를 매장 3곳·계층 폴링 기준으로 갱신 |
| 2026-08-28 | 주소 셋(Pages·DuckDNS·오라클)이 뭐가 다른지 설명 추가. 배포 용어집 12개 신설 |
