# CLAUDE.md

## 이 프로젝트

방탈출 예약 사이트를 주기적으로 조회해, **`예약 불가 → 예약 가능` 전이(취소표)를 감지해 알림을 보내는 서버.**
Kotlin 2.3 / Spring Boot 4.1 / JDK 21.

연동된 매장은 **8곳 35지점** — 플레이33(4) · 키이스케이프(11) · 토끼굴(1) · 지구별(3) · 포인트나인(3) · 비트포비아(9) · 제로월드(3) · 제로월드 다이브(1).
어댑터를 못 만든 곳이 2곳 남았다 — 오아시스 · 티켓투이스케이프 (둘 다 조사가 막혔다).
**매장 이야기는 전부** [방탈출 지점 요청_응답 정리.md](방탈출%20지점%20요청_응답%20정리.md) **에 있다.**

```
M1 수집 ✅   M2 저장·전이 ✅   M2.5 조회 API ✅   M4 배포 ✅
M3 알림  서버 완료(FCM 발송까지) · 키 발급 + Floduler 등록 UI 만 남음
M5 확장  8매장 35지점 연동 · 남은 2곳은 조사가 막혀 있다
```

**지금 이어서 할 일은 [작업명세서.md](작업명세서.md) 맨 위 "▶ 다음 작업" 을 본다.**

## 자매 프로젝트 — Floduler (`~/RoomEscapeScheduler`)

방탈출 3연방 시간표를 계산해 주는 **정적 웹앱**(`app/` — Vite + React + TS · CI 가 GitHub Pages 로 배포).
~~vanilla JS 단일 파일~~ 은 **옛말이다** — 2026-08-31 에 `app/` 으로 갈아엎었다 ([아키텍처.md](아키텍처.md) D7 정정).
이 서버가 회차와 **매진 여부**를 먹여준다 — Floduler가 스스로 "서버가 없어서 못 한다"고 적어둔
§4.10·§4.13 의 빈칸이 이 서버다. 번복 기록은 그쪽 `기획.md` §4.30.

- **저장소는 합치지 않는다.** API 계약으로만 잇는다 ([아키텍처.md](아키텍처.md) D7)
- **화면 코드는 `app/`** (Vite + React · `main` 푸시하면 CI 가 Pages 로 배포).
  루트 `index.html` 은 **백업이고 안 쓴다** — 거기 고쳐 봐야 배포에 반영되지 않는다
- **양쪽 다 푸시하면 자동으로 올라간다.** Floduler 는 CI 가 즉시, **이 서버는 크론이 5분 안에**
  ([배포.md](배포.md) "자동 배포"). 다만 **반영 시점이 다르므로**, 계약을 양쪽에서 바꿀 때는
  **서버를 먼저 푸시하고 5분 뒤 프론트를 푸시한다** — 서버가 필드를 *더* 주는 것은
  옛 프론트에 해가 없지만, 반대는 화면이 깨진다
- **이 서버는 Floduler의 선택 사항**이다. 없어도 직접 입력·사진 경로는 그대로 동작해야 한다
- **API 계약의 원본은 Floduler `작업명세서.md` §4.4.** 양쪽에 두 벌 적지 않는다
- 한쪽 기획을 바꾸면 **다른 쪽 문서도 같이 본다**

## 문서 (읽고 나서 작업할 것)

| 문서 | 내용 | 갱신 시점 |
|---|---|---|
| [기획.md](기획.md) | 무엇을 왜 만드나 | **기획이 바뀌면 코드보다 먼저** |
| [아키텍처.md](아키텍처.md) | 어떻게 생겼나, 왜 그렇게 했나 | **구조를 추가·변경하면 같은 커밋에서** |
| [작업명세서.md](작업명세서.md) | 어디까지 했고 다음은 뭔가 | **작업을 끝낼 때마다** |
| [방탈출 지점 요청_응답 정리.md](방탈출%20지점%20요청_응답%20정리.md) | 매장별 요청·응답 형식과 함정 | **매장을 추가·조사할 때마다. 어댑터 코드보다 먼저** |
| [배포.md](배포.md) | 오라클에 올리는 절차 전체 · **요금이 나가는 자리** | 배포 방식·구성이 바뀌면 · **요금 구조가 바뀌면** |
| **CLAUDE.md** (이 파일) | 프로젝트 요약·규칙 | **현황 블록이 바뀔 때마다** |
| [README.md](README.md) | 남에게 보여주는 첫 화면 | **매장·구조·설정이 바뀌면** |

> **마지막 두 줄이 늦게 추가됐다.** 2026-08-28 에 문서를 훑어 보니 정작 이 두 개가
> 제일 오래 썩어 있었다 — 표에 없으니 아무도 갱신 시점을 안 봤다.
> **CLAUDE.md 는 매번 읽히는 파일이라 틀리면 그 거짓말을 계속 믿게 된다.**

### 갱신 규칙 (중요)

**작업이 끝나면 문서를 고친다. "나중에" 는 없다 — 같은 커밋에서 함께 고친다.**

무엇을 바꿨나 → 어디를 고치나:

- 새 계층·패키지·엔티티 추가, 데이터 모델 변경, 의존성 추가 → **아키텍처.md**
- 설계 판단을 내렸다면 → **아키텍처.md §4에 `D번호`로 이유까지** 기록
- 기능 범위·대상 매장·정책 변경 → **기획.md**
- 작업 완료/추가 → **작업명세서.md** 체크박스와 변경 이력
- 매장 추가·조사 → **방탈출 지점 요청_응답 정리.md** (어댑터 코드보다 **먼저**)
- **요금이 늘 수 있는 변경** → **배포.md "요금이 나가는 자리"** 를 고치고, 작업 전에 💸 경고를 낸다
- 매장·마일스톤 상태가 바뀜 → **CLAUDE.md 현황 블록 + README.md 매장표**
- Floduler 와 얽힌 것을 바꿈 → **`~/RoomEscapeScheduler` 쪽 문서도 같이 본다**

### 끝내기 전에 훑을 자리 (여기가 먼저 썩는다)

본문은 맞는데 **눈에 먼저 들어오는 줄**이 낡으면, 읽는 사람은 문서 전체가 안 갱신됐다고 판단한다.

1. 머리말 **`최종 수정` 날짜**
2. 다 한 일에 **`- [ ]` 가 남아 있지 않은지.** 끝난 절 제목이 "다음 작업" 인 것도 같다
3. **문서에 박힌 숫자** — 지점 수 · 사이트 수 · 테마 수 · 테스트 수 · 픽스처 수 · 한 바퀴 초
4. **확인 명령의 기대값** (`배포.md` 의 `# 200 · 35지점` 같은 것)
5. **CLAUDE.md 현황 블록 · README.md 매장표**
6. **"아직 없다 / 미구현 / 예정" 이라고 적어 둔 것이 아직도 그런지**

숫자와 낡은 주장은 이렇게 훑는다 (그대로 붙여 넣으면 돈다):

```bash
grep -rnE "[0-9]+(매장|지점|테마)|테스트 [0-9]+|픽스처 [0-9]+|사이트 [0-9]+곳|아직 (미구현|없|안)" *.md
```

**변경 이력 줄은 과거 기록이라 안 고친다.** 그 시점에 맞았던 값이다 — 걸러 읽는다.

> **숫자가 제일 위험하다.** 둘러싼 문장이 여전히 맞아서 **틀린 게 안 보인다.**
> 2026-08-28 에 `배포.md` 의 확인 명령이 `# 200 · 16지점` 을 기대하고 있었다 —
> 그대로 따라 했으면 **정상인데 고장 난 줄 알았을 것이다.**

- **수치를 적을 때는 실측인지 추정인지 밝힌다.** 나중에 "이 숫자 어디서 나왔지" 가 되지 않게
- **틀리게 적었던 것을 발견하면 지우지 말고 정정으로 남긴다.** 왜 틀렸는지가 다음 사람을 구한다
  (예: 지구별 `branch` 파라미터 — "기본값과 비교하면 아무것도 확인이 안 된다")

**코드만 바꾸고 문서를 두면 문서가 거짓말을 시작한다.**

## 실행 장소 규칙

**개발·테스트는 어디서든, 상시 운영은 배포된 서버에서.**

2026-08-28 배포 완료 후로 이 표의 전제가 바뀌었다 — **주기 수집은 오라클에서 돈다.**
크롤 트래픽이 회사·집 네트워크로 나가지 않으므로, 아래 ❌ 는 이제 "로컬에서 굳이 켜지 않는다" 는 뜻이다.

| | 회사 | 집 / VPS |
|---|---|---|
| 코드 작성, `./gradlew test` | ✅ | ✅ |
| Postgres 컨테이너, 앱 기동 | ✅ | ✅ |
| `/debug/collect` 로 테스트 반복 조회 | ✅ | ✅ |
| **주기 수집 스케줄러 상시 가동** | ❌ | ✅ |

회사에서 안 하는 건 **며칠씩 켜두는 것 하나**다. 규칙적인 주기 트래픽이 회사 공인 IP로 계속 나가는 게
불필요한 리스크라서지, 테스트 조회는 얼마든지 해도 된다.

- 주기 수집은 **기본 꺼짐**. 켜려면 `COLLECTOR_ENABLED=true`
- 서버는 **루프백에만** 바인딩된다 (`server.address`). 남에게 보여줄 때만 `SERVER_ADDRESS=0.0.0.0`

## 작업 규칙

- **매장을 추가하기 전에 [방탈출 지점 요청_응답 정리.md](방탈출%20지점%20요청_응답%20정리.md) 에 먼저 적는다.**
  `robots.txt`·약관·보호장치 확인 결과, 요청 단위, 예약 가능 여부를 어느 값으로 판단하는지,
  그리고 **"틀린 요청이 그럴듯한 응답으로 오는" 함정**. 코드보다 이게 앞선다.
- **크롤 어댑터를 추가하면 픽스처 테스트를 같이 만든다.** 실제 받아온 HTML을 `src/test/resources/`에 넣고 파서를 검증한다. 사이트가 바뀌면 배포된 서버가 조용히 오작동하기 전에 테스트가 먼저 깨져야 한다.
- **조회 실패와 "결과 없음"을 같은 값으로 표현하지 않는다.** 빈 리스트가 DB를 지우는 사고로 이어진다.
- **저장은 자연키 upsert.** 전체 삭제 후 삽입 금지.
- **비밀값은 환경변수.** properties에 하드코딩하거나 커밋하지 않는다.
- 디버그 엔드포인트는 **기본 꺼짐**. 로컬에서 볼 때만 `DEBUG_ENDPOINTS=true ./gradlew bootRun`.

## 💸 돈이 나갈 수 있으면 **먼저, 크게** 말한다

**이 서버는 오라클 상시 무료 한도 안에서 돌고 있어 지금 청구가 0원이다.**
그런데 계정이 **PAYG(유료)** 라서, 한도를 넘으면 **막히지 않고 그냥 청구된다.**
막아 주는 것이 없으므로 **넘길 만한 작업인지 판단하는 일이 사람 몫으로 남는다.**

**그래서 요금이 늘 수 있는 작업은 시작하기 전에 알린다. 묻기를 기다리지 않는다.**
2026-08-31 에 "서버 부하 때문에 요금이 늘지 않냐" 는 질문을 받고서야 계산해 줬다 —
**그 계산은 작업을 제안할 때 이미 붙어 있었어야 한다.**

### 이렇게 알린다 (형식을 지킨다)

```
💸 요금 경고 — <무엇이 늘어나나>
   지금        <실측값>
   무료 한도    <값>
   작업 후      <추정값>  ← 실측인지 추정인지 반드시 밝힌다
   넘으면       대략 월 $N
   대안         <더 싼 방법 · 안 하는 선택지>
```

숫자가 **추정이면 추정이라고 적는다.** 요금 이야기에서 어림값을 실측처럼 말하면
안심시키는 쪽으로 틀리기 쉽고, 그 방향의 실수는 청구서로 돌아온다.

### 무엇이 돈을 부르나 — [배포.md](배포.md) "요금이 나가는 자리" 참고

특히 이 셋은 **작업을 제안하는 순간 먼저 계산한다.**

- **인스턴스를 늘리거나 셰이프를 키우는 것** (상시 무료는 A1 합계 4 OCPU · 24GB)
- **디스크가 계속 자라는 구조** (로그·WAL·`lastCheckedAt` 같은 초당 수십 건 쓰기)
- **오라클이 아닌 유료 서비스를 붙이는 것** (관리형 DB · 로드밸런서 · 오브젝트 스토리지)

### 반대로, 겁주지 않는다

**매장을 추가하는 것은 요금과 거의 무관하다.** 2026-08-31 실측으로 CPU 부하 0.10,
앱 메모리 363MB, DB 9.3MB였다 — 상시 무료 A1 은 **얼마나 굴리든 0원**이고,
미터기가 도는 방식이 아니다. **"혹시 몰라서" 경고하면 진짜 경고가 묻힌다.**
이 프로젝트가 로그·알림에서 계속 지키는 기준과 같다.

> 요금이 아니라 **남의 서버**에 부담이 가는 경우는 [수집 윤리](#수집-윤리-타협하지-않는다) 쪽이다.
> 둘을 섞지 않는다 — 대응이 다르다.

## 수집 윤리 (타협하지 않는다)

- 매장당 동시 요청 1개, 초당 1회 미만
- `User-Agent`는 **빈 값**으로 보낸다. 헤더를 빼면 자바 기본값(`Java-http-client/…`)이 대신 나가므로 빈 값이 제일 덜 남긴다. 브라우저 UA 를 흉내내지는 않는다
- `429`/`503` → 즉시 백오프
- **캡차·Cloudflare·WAF 우회는 하지 않는다.** 해당 매장은 대상에서 빼거나 제휴로 전환
- 예약 자체를 자동화하지 않는다

## 사용자 컨텍스트

서버 개발이 처음이다. 서버 용어나 관례를 당연한 것으로 두지 말고, **왜 그렇게 하는지**를 함께 설명한다.
용어집은 [아키텍처.md](아키텍처.md) §6에 있다.

### "왜" 를 문서에도 남긴다 (대화에만 하고 끝내지 않는다)

대화에서 설명한 것이 문서에 없으면 **다음에 또 물어야 한다.** 설명이 필요했다는 건
그 자리가 문서에 비어 있다는 뜻이다.

| 무엇을 설명했나 | 어디에 남기나 |
|---|---|
| 왜 이 구조로 했나 · 다른 선택지를 왜 버렸나 | [아키텍처.md](아키텍처.md) §4 에 `D번호` 로 |
| 파이어베이스(로그인·푸시)가 어떻게 얽히나 | [아키텍처.md](아키텍처.md) **§6** — 토큰 세 가지가 헷갈리는 자리다 |
| 서버 용어·관례 (DNS·프록시·CORS 같은) | [아키텍처.md](아키텍처.md) §6 용어집 |
| 왜 이 매장은 이렇게 긁나 · 함정은 뭔가 | [방탈출 지점 요청_응답 정리.md](방탈출%20지점%20요청_응답%20정리.md) |
| 왜 이 절차인가 · 뭘 빼먹으면 어떻게 되나 | [배포.md](배포.md) |

**코드 주석도 같다.** "무엇을 하는지" 는 코드가 이미 말한다.
주석에는 **왜 그렇게 했는지, 다르게 하면 뭐가 깨지는지**를 쓴다.

```kotlin
// ✗  label 이 "예약불가" 면 불가로 본다
// ✓  문구가 아니라 존재 여부로 보는 이유는 실패 방향이 다르기 때문이다 —
//    문구 비교는 "마감" 으로 바뀌면 전 회차가 가능으로 읽혀 헛알림이 대량으로 나간다
```

수치를 적을 때는 **실측인지 추정인지 밝힌다.** 나중에 "이거 어디서 나온 숫자지" 가 되지 않게.
